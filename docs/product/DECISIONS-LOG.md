# Decisions log (soft)

Informal notes that are **not** ADRs. Use for weekly sequencing, naming ideas, and reversible choices.

| Date | Note |
|------|------|
| 2026-07-22 | Chose Spec-Driven Orchestration (SDO) as process; see ADR 0001. Product docs bootstrapped under `sheet-app/`. |
| 2026-07-22 | Research baseline: ScorePDF feature parity for PdfMode; SmartMode via MusicXML + Verovio; native Transport on iOS. Tech ADRs still `proposed`. |
| 2026-07-22 | Future BackingTrack (play with band) → ADR 0007 multi-lane Transport + SyncMap; design note `TRANSPORT-ARCHITECTURE.md`. Horizons renumbered (backing before OMR). |
| 2026-07-22 | **Early phase = ScorePDF parity first** (ADR 0008 accepted). Checklist `SCOREPDF-PARITY.md`. SmartMode/Transport/OMR deferred until P0–P2 accepted. |
| 2026-07-22 | Provisional product name **StageScore** (lean lock). No obvious iOS/Android sheet-music collision found; nearby concepts: Planning Center “Music Stand”, digitalScore marketing. Confirm domain/trademark before store submit. |
| 2026-07-22 | **Publisher:** BackingScore (backingscore.com). **Bundle ID:** `com.backingscore.scoreapp` (ADR 0009 accepted). |
| 2026-07-22 | Draft logo mark: `docs/brand/stagescore-logo.png` (music stand + score). Iterate before store lock. |
| 2026-07-22 | Logo revised to **flat minimal** stand silhouette (no wordmark on icon). Replaced `docs/brand/stagescore-logo.png`. |
| 2026-07-22 | Logo simplified further: **score page only** (no music stand). |
| 2026-07-22 | Logo color: warm **teal** on cream (avoid black). |
| 2026-07-22 | Logo: yellowed paper background; tighter crop (less top/bottom padding). |
| 2026-07-22 | Logo: **teal full-bleed background**; only staff + notes in yellow (no paper border). |
| 2026-07-22 | **G0 + G2 locked:** VISION accepted; ADR 0005 Flutter-first accepted. Spec **0001** PDF+annotate spike accepted — next build. |
| 2026-07-23 | Spike **0001 go**. H1 open. Spec **0002** library import/open drafted (`proposed`). |
| 2026-07-23 | Spec **0002** accepted (G3). Library + import + PdfMode open implemented — awaiting G4. |
| 2026-07-23 | Spec **0002 G4 pass**. Spec **0003** PageTurn tap/swipe drafted (`proposed`). |
| 2026-07-23 | Spec **0003** accepted (G3). PageTurn tap/swipe implemented — awaiting G4. |
| 2026-07-23 | Spec **0003 G4 pass**. Spec **0004** page layouts drafted (`proposed`). |
| 2026-07-23 | Spec **0004** accepted (G3). Layout modes implemented — awaiting G4. |
| 2026-07-23 | Spec **0004 G4 pass**. **P0 complete.** Spec **0005** pedal/keyboard drafted (`proposed`). |
| 2026-07-23 | Spec **0005** accepted (G3). Pedal/keyboard PageTurn implemented — awaiting G4. |
| 2026-07-24 | Spec **0004** amendment: **Single page** = horizontal one-page slider (ScorePDF), not vertical continuous. Fit width/height remain scroll modes. |
| 2026-07-25 | Orchestrator verified Single-page slider OK. Spec **0006** (page turn delay / P1.3) drafted `proposed`. Spec **0005** still needs G4. |
| 2026-07-26 | Spec **0006** accepted (G3). Default delay Off. Build anti-double-turn lockout. |
| 2026-07-26 | Spec **0006 G4 pass**. P1.3 done. Spec **0007** (page-turn animation / P1.4) drafted `proposed`. |
| 2026-07-26 | Spec **0007** accepted (G3). Animation presets Off/Fast/Normal/Slow; default Normal. |
| 2026-07-26 | Spec **0007 G4 pass**. P1.4 done. Spec **0008** (reverse PageTurn / P1.5) drafted `proposed`. |
| 2026-07-26 | Spec **0008** accepted (G3). Reverse flips slide + horizontal gestures; pedal keys stay document-order. |
| 2026-07-26 | Spec **0008 G4 pass**. P1.5 done. Spec **0009** (page slider + jump / P1.11) drafted `proposed`. |
| 2026-07-26 | Spec **0009** accepted (G3). Bottom page chrome; scrub + jump; draw mode keeps nav; jump skips delay. |
| 2026-07-26 | Spec **0009 G4 pass**. P1.11 done. Spec **0010** (bookmarks / P1.8) drafted `proposed`. |
| 2026-07-26 | Spec **0010** accepted (G3). Bookmark in CONTEXT; per-Score JSON; out-of-range jump clamps. |
| 2026-07-26 | Spec **0010 G4 pass** (after TextEditingController dispose fix). P1.8 done. Spec **0011** (PageOrder / P1.6) drafted `proposed`. |
| 2026-07-26 | Spec **0011** UX: entry via PdfMode **⋯** menu; include **Reset to original**. Still `proposed` until G3 Accept. |
| 2026-07-26 | Spec **0011** accepted (G3). PageOrder editor + PdfMode mapping; custom continuous when non-identity. |
| 2026-07-26 | Spec **0011 G4 pass**. PdfMode app bar: keep Draw (+ Undo while drawing); rest in ⋯ menu. |
| 2026-07-26 | Spec **0005 G4 pass** (confirmed). P1.1 done. Red-screen on device open was dual `xcode`+`flutter run`, not app logic. |
| 2026-07-26 | Spec **0012** (Setlist / P1.7) drafted `proposed`. Remaining P1: gesture map, half-page, jump links. |
| 2026-07-26 | Spec **0012** accepted (G3). Segmented Scores/Setlists; skip missing Scores on open + snackbar. |
| 2026-07-26 | Spec **0012** built: Setlist model/store/nav, library tab, editor, PdfMode boundary PageTurn + title jump. Awaiting G4. |
| 2026-07-26 | Spec **0012 G4 pass**. P1.7 done. Spec **0013** (half-page layouts / P1.9) drafted `proposed`. |
| 2026-07-26 | Spec **0013** accepted (G3). Setlist last-page peek = next Score title; separator Fixed app-wide. |
| 2026-07-26 | Spec **0013** built: HalfPageView + layout prefs ratio; Setlist peek title. Awaiting G4. |
| 2026-07-26 | Clarified ScorePDF: **Half Page layout** (P1.9/0013) ≠ **Turn Amount 1/2** (continuous half-step). Orchestrator wants both. Spec **0014** (P1.12) drafted `proposed`. |
| 2026-07-26 | Spec **0014** accepted (G3) + built: Turn amount Full/Half in PageTurn settings; Fit width/height half = ½ viewport scroll. Awaiting G4. |
| 2026-07-26 | Spec **0014 G4 pass**. P1.12 done. Spec **0015** (gesture map / P1.2) drafted `proposed`. Spec **0013** still G4 pending. |
| 2026-07-26 | Spec **0015** accepted (G3): keep always-visible chrome (no immersive). Show menu opens ⋯. Built — awaiting G4. |
| 2026-07-26 | Spec **0015 G4 pass** (Page turn settings Done button). P1.2 done. Spec **0016** (jump links / P1.10) drafted `proposed`. Spec **0013** still G4 pending. |
| 2026-07-26 | Spec **0016** accepted (G3): JumpLink on-page → PageOrder page; distinct from Bookmark. Building. |
| 2026-07-26 | Spec **0016** built: JumpLink store/overlay; ⋯ Add jump link; tap jump / long-press edit. Awaiting G4. |
| 2026-07-26 | JumpLink: drag button to reposition (avoid covering notes); position persists. |
| 2026-07-26 | Spec **0016 G4 pass**. P1.10 done. Spec **0013** Half Page layout still G4 pending (last open P1 item). |
| 2026-07-26 | Spec **0013 G4 pass**. P1.9 done. **P1 complete.** Spec **0017** (draw tools / P2.1) drafted `proposed`. |
| 2026-07-26 | Spec **0017** accepted (G3): stroke-level eraser; fixed pen/marker presets; persist per Score. Building. |
| 2026-07-26 | Spec **0017** built: Pen/Marker/Eraser toolbar; undo/redo; per-Score annotation JSON. Awaiting G4. |
| 2026-07-26 | Spec **0017 G4 pass** (draw scroll lock). P2.1 done. Spec **0018** (stroke style / P2.2) drafted `proposed`. |
| 2026-07-26 | Spec **0018** accepted (G3): eyedropper = sample ink; palette + widths; straight-line chip. Building. |
| 2026-07-26 | Spec **0018** built: color/width toolbar, Line chip, Dropper (ink sample). Awaiting G4. |
| 2026-07-26 | Draw toolbar compact: icon tools + color dots + S/M/L segmented (fix Wrap chip wall). |
| 2026-07-26 | Draw chrome: one row — color + tool + ⋯; full options in bottom sheet. |
| 2026-07-26 | Spec **0018 G4 pass**. P2.2 done. Spec **0019** (symbols/stamps / P2.3) drafted `proposed`. |
| 2026-07-26 | Spec **0019** accepted (G3): stamp catalog p/f/♯/♭/♮/box/circle/arrow/text; tap place; select delete/move. Building. |
| 2026-07-26 | Stamp size: 0019 keeps pen Width→new stamp only; dedicated size UI + resize selected deferred to later slice. |
| 2026-07-26 | Spec **0019 G4 pass**. P2.3 done. Spec **0020** (hide/export annotations / P2.4) drafted `proposed`. |
| 2026-07-26 | Spec **0020** accepted (G3): hide ink+stamps (session); export flattened PDF via share; source page order; no JumpLinks in export. Building. |
| 2026-07-26 | Spec **0020 G4 pass**. P2.4 done. Spec **0021** (labels/filter / P2.5) drafted `proposed`. |
| 2026-07-26 | Spec **0021** accepted (G3): Labels on Scores only; confirm delete; Any/All/Untagged; no colors. Building. |
| 2026-07-26 | Spec **0021 G4 pass** (Label dialog dispose fix). P2.5 done. Spec **0022** (search / P2.6) drafted `proposed`. |
| 2026-07-26 | Spec **0022** accepted (G3): live title OR Bookmark search; Label filter then search; Scores only. Building. |
| 2026-07-26 | Spec **0022 G4 pass**. P2.6 done. Spec **0023** (library sort / P2.7) drafted `proposed`. |
| 2026-07-26 | Spec **0023** accepted (G3): title A–Z; created/last-viewed newest-first; never-opened last; persist. Building. |
| 2026-07-26 | Brand: applied `stagescore-logo.png` to Library app bar, launcher icons (iOS/Android), and native splash (#0D8B86). Still draft before store lock. |
| 2026-07-26 | Spec **0023 G4 pass**. P2.7 done. Spec **0024** (replace PDF / P2.8) drafted `proposed`. |
| 2026-07-26 | Spec **0024** accepted (G3): Library replace PDF; keep/reset annotations+bookmarks+jumplinks+page order; Labels/Setlists kept. Building. |
| 2026-07-26 | Spec **0024 G4 pass**. P2.8 done. Spec **0025** (color filter / P2.9) drafted `proposed`. |
| 2026-07-26 | Spec **0025** accepted (G3): Off/Sepia/Green/Invert; app-level persist; PdfMode ⋯; ColorFiltered. Building. |
| 2026-07-26 | Spec **0025 G4 pass**. P2.9 done. Spec **0026** (dark mode + theme color / P2.10) drafted `proposed`. |
| 2026-07-26 | Spec **0026** accepted (G3): Light/Dark/System; brand teal + presets + custom; Library ⋯ Appearance; no match-gutter. Building. |
| 2026-07-26 | Spec **0026 G4 pass**. P2.10 done. Spec **0027** (backup/restore ZIP / P2.11) drafted `proposed`. |
| 2026-07-26 | Spec **0027** accepted (G3): full `stagescore/` tree + prefs; full-replace restore; `stagescore-backup.json` marker; Library ⋯. Building. |
| 2026-07-26 | Gap: Library has no Delete Score (Setlist delete exists). Spec **0028** (P0.9) drafted `proposed`; queue after 0027 G4. |
| 2026-07-26 | Spec **0028** accepted (G3): Library Score ⋯ Delete; hard-delete PDF+overlays+Labels; strip from Setlists (keep empty); Library-only. Building. |
| 2026-07-26 | Spec **0028 G4 pass**. P0.9 done. Spec **0027** still G4 pending. |
| 2026-07-26 | Spec **0027 G4 pass**. P2.11 done. Spec **0029** (share-in PDF / P2.12) drafted `proposed`. |
| 2026-07-26 | Spec **0029** accepted (G3): Library+snackbar; iOS+Android; multi-file; `share_handler`; no auto-open. Building. |
| 2026-07-26 | Spec **0029 G4 pass** (ShareExtension CFBundleVersion fix). P2.12 done. Spec **0030** (metronome / P2.13) drafted `proposed`. |
| 2026-07-26 | Spec **0030 deferred** (finish P2.14–P2.15 first). Spec **0031** (zoom lock / page scale / P2.14) drafted `proposed`. |
| 2026-07-26 | Spec **0031** accepted (G3): scale+lock only (no DPI); lock disables pinch; PdfMode ⋯ Page scale…; Fixed/PerScore/PerPage. Building. |
| 2026-07-26 | Spec **0031 G4 pass**. P2.14 done (scale+lock; DPI deferred). Spec **0032** (page borders / status bar / notch / P2.15) drafted `proposed`. |
| 2026-07-26 | Spec **0032** accepted (G3): Display… sheet; border off by default; separator shares style; status bar hidden; avoid notches on; app-wide. Building. |
| 2026-07-26 | Spec **0032 G4 pass**. P2.15 done. Spec **0030** metronome re-opened `proposed` (last open P2 row). |
| 2026-07-26 | Spec **0032 G4 reopened**: page border invisible (drawn under PdfPageView image). Overlay border fix; G4 pending again. |
| 2026-07-26 | Spec **0032 G4 pass** (border overlay). P2.15 done. Spec **0030** metronome awaiting G3. |
| 2026-07-26 | Jump Links UX (0016 revision, Orchestrator pass): PdfMode ⋯ → **Jump Links** list sheet (like Bookmarks); Add/Edit in-sheet (no stacked modal); long-press on-page edit kept. Spec 0016 updated. |
| 2026-07-26 | Quality review → [IMPROVEMENT-ROADMAP.md](./IMPROVEMENT-ROADMAP.md) drafted. P0.7 marked **reopen** (pinch/PageTurn). Polish Q1–Q7 appendix. Recommend A1 (0033) then A2 (0030). |
| 2026-07-26 | Improvement roadmap **accepted**. Sequence A1→A2→B→C→D. Spec **0033** (P0.7 reopen) drafted `proposed`; **0030** waits for 0033. |
| 2026-07-26 | Spec **0033** accepted (G3): multi-touch IgnorePointer pass-through; double-tap fit↔2×; PageTurn single-finger. Building. |
| 2026-07-26 | Spec **0033 G4 pass**. P0.7 / Q1 done. Spec **0030** metronome awaiting G3 (roadmap A2). |
| 2026-07-26 | Spec **0030** accepted (G3): app-wide persist; ⋯ Metronome…; tick+accent; mute/visual-only. Building. |
| 2026-07-26 | Spec **0030 G4 pass**. P2.13 done. Phase A complete. Next: Phase B — Spec **0034** Performance mode (then 0035 menu IA). |
| 2026-07-26 | Spec **0034** accepted (G3): reopens 0015 “chrome always visible”; Display… toggle, app-wide, default off; `showChrome` reveals then opens ⋯; auto-hide 5 s fixed; overlay chrome over constant-size viewer; no page indicator while hidden (B3). **PerformanceMode** added to CONTEXT (G1). Building. |
| 2026-07-26 | Spec **0034** default flipped to Performance mode **on** (Orchestrator) — including installs whose prefs predate the key. Build fix: PageNavBar stretched full-screen in `Scaffold.bottomNavigationBar` (Slider fills bounded height); slider height pinned + layout regression tests. |
| 2026-07-26 | Spec **0034** first-open rule (Orchestrator): first Score on an install opens with chrome up + reveal-gesture snackbar (one-shot `performanceHintShown`), then Scores open hidden. Full onboarding still C3 / 0039. |
| 2026-07-26 | Score surface unified for 0034: gutters / margins / safe-area bands use `pdfSurfaceColor` = `surfaceContainerLowest` (paper white in light, dark in dark) instead of `surfaceContainerHighest`. Page edges now rely on the 0032 page border, not a grey gutter. |
| 2026-07-26 | Color filter (0025) now tints the Score surround too: `applyPageColorFilter(Color, mode)` applies the same matrix to gutter / bands / blank pages, which sit outside the `PageColorFiltered` subtree. |
| 2026-07-26 | Spec **0034** chrome moved out of the Scaffold `appBar` / `bottomNavigationBar` slots into a `Stack`: those slots inflate the body's `MediaQuery.padding` by the chrome height, so the viewer's `SafeArea` was clearing an invisible AppBar (103 px, not the 47 px notch). Viewer now also drops the bottom inset while hidden, so the bottom-right PageTurn tap zone reaches the screen edge. |
| 2026-07-26 | GestureMap **bottom edge default → Off** (was Draw, 0015): stray bottom-edge taps entering Draw confused users, worse now the PageNavBar hides there. Superseded same day: **Draw removed from GestureMap altogether** (Orchestrator) — bad UX in and out of Performance mode, so `GestureMapAction` is now Show menu / Off only and Draw is entered from the toolbar. Saved `enterDraw` reads back as Off, so no migration step is needed. |
| 2026-07-26 | PdfMode chrome heights (Orchestrator): AppBar **64 px** (was 56), PageNavBar **48 px** (was 64). The footer holds one scrubber; the header holds the title and every action. |
| 2026-07-26 | Spec **0034 G4 pass** (device run: first-open hint, reveal / auto-hide / pin, pedal with chrome hidden, Draw, zoom + Half Page + Jump Links, 0032 prefs, restart). Q2 / B1 done. Phase B continues with **0035** (⋯ menu IA) then **0036** (stage preset + Page scale copy). |
| 2026-07-26 | Spec **0035** drafted (B2 / Q3), awaiting G3: group the eleven flat ⋯ entries, show layout / filter / zoom-lock state inline. Five G3 questions, the load-bearing one being popup-with-headers vs bottom sheet. `CONTEXT.md` GestureMap definition corrected — it still listed “enter draw”. |
| 2026-07-26 | Spec **0035** accepted (G3) with the drafted recommendations: bottom-sheet **ScoreMenu** replaces the ⋯ popup; groups Go to / Marks / View / Playing; Page order… under *Go to*; inline state on Layout / Color filter / Page scale; nothing promoted to the AppBar this slice. **ScoreMenu** added to CONTEXT (G1). Built; awaiting G4 on device. |
| 2026-07-26 | Settings sheets size to content (0035 follow-up): Layout and Page turn were `SizedBox(height: 90% of screen)`, so Layout's six chips opened a near-full-screen panel that read as a pushed route. Now `ConstrainedBox(maxHeight: 90%)` + `shrinkWrap`; Layout drops from 588 px to 349 px on an 800×600 surface. List sheets (Bookmarks, Jump Links) keep fixed heights on purpose — they must not jump as rows are added or deleted. |
| 2026-07-26 | Post-G4 fix on **0035**: the ScoreMenu's eleven entries filled the phone screen, so no barrier was left to tap and the only way out was choosing an entry. Fixed by giving it the Layout / Page turn header — title + **Done** — with a 90% cap, so the list keeps the height and the exit does not depend on a barrier strip (Orchestrator's call). Test covers leaving via Done. |
| 2026-07-26 | Draw stays on the AppBar, not in the ScoreMenu (Orchestrator): it is a mode, so the icon must double as indicator and exit. **Undo / Redo / Delete stamp moved from the AppBar into the DrawToolbar** instead — they were Draw-only actions one divider away from the bar holding every other Draw control, and the AppBar no longer changes shape on entering Draw. |
| 2026-07-26 | DrawToolbar gains a **Size** button showing the active width as a dot at that width, with a three-step picker anchored like the Tool button (0018 widths were only reachable through More…). Pen and marker keep their own width, as they always have. |
| 2026-07-26 | Spec **0035 G4 pass**. Q3 / B2 done. Phase B has one slice left: **0036** (B3 — stage preset + Page scale vs pinch copy), which also owns the page-indicator question 0034 deferred. |
| 2026-07-26 | Library UI/UX review (Orchestrator asked). Findings, worst first: **a Score cannot be renamed** — Spec 0002 wrote "rename can wait" and nothing collected it, so share-in (0029) PDFs keep names like `doc_2024-11-03_18-22` while Bookmarks and Labels both got rename; rows carry a generic glyph and no page count; date and labels are joined with the same `·` into one run-on; an active label filter shows only as a filled icon while sort next to it prints its value. Noted but **cut from the slice**: the Scores AppBar carries four flat controls, Library's ⋯ is still a popup while PdfMode's is now a ScoreMenu sheet (0035), no multi-select, Setlists has no search. Spec **0040** drafted `proposed` (roadmap C4 / parity Q8), recommended to run before C1 duplicate Score. Numbering: 0036–0039 are reserved by the accepted roadmap, so 0040 is the next free number. |
| 2026-07-26 | Spec **0040** accepted (G3) with all seven recommendations, and built. Rename lives on `ScoreLibrary.renameScore` (metadata only — the PDF keeps its path, so annotations / Bookmarks / Setlists are untouched) and the bookmark rename dialog was extracted to `promptForTitle` so both share one. `Score.pageCount` is written at import, recounted on Replace PDF, and backfilled after the list is drawn. Rows now carry a first-page thumbnail, "Opened today · 4 pages", and label chips capped at `+N`; the active filter is spelled out in removable chips under the search field. Setlists lost its duplicate AppBar "New setlist". |
| 2026-07-26 | 0040 build deviation from the G3 note: thumbnails cache in the **OS cache directory**, not `<libraryRoot>/thumbs`. Excluding a subfolder from the backup ZIP (0027) would mean walking the tree instead of `addDirectory`, and derived data belongs somewhere the system can reclaim. Cache file is `<scoreId>-<pdf mtime>.png`, so Replace PDF (0024) and Restore both miss by construction rather than needing an invalidation call. |
| 2026-07-26 | Widget-testing LibraryScreen: `dart:io` futures never complete inside a `testWidgets` fake-async zone, so awaiting a file there **hangs** the run rather than failing it — the first attempt sat for minutes with no output and no timeout. The test now alternates `runAsync` windows with `pump` (each round advances the screen's load by one `await`) and wraps every fixture write. Worth remembering: this is the first widget test in the repo that drives a file-backed screen. |
| 2026-07-26 | Spec **0040 G4 pass**. Q8 / C4 done. Phase C has three slices left: **C1 — 0037** (duplicate Score), **C2 — 0038** (annotated export/share discoverability), **C3 — 0039** (first-run tips). Phase B still owes **B3 — 0036** (stage preset + Page scale vs pinch copy, and the page indicator 0034 deferred). |
| 2026-07-26 | Spec **0036** drafted (B3 / Q4), awaiting G3 — the last Phase B slice. Bundles three loose ends that are all the same complaint: a **preset** for getting into and out of playing shape, rewritten **Page scale** copy (scope, and what pinch does to a kept scale), and the **transient page indicator** 0034 deferred. Eight G3 questions; the load-bearing ones are preset-as-action-with-Undo vs preset-as-mode, and two presets (play / practise) rather than one, since 0034's defaults already made the app stage-shaped and what is missing is the way back. |
| 2026-07-26 | Spec **0036** accepted (G3) with one revision to the draft: the stage preset is **one ScoreMenu entry whose label flips with the current state**, sitting in **Playing**, rather than two sibling entries in View. Two entries would have pushed View past 0035's four-entry ceiling and ignored the Show/Hide annotations idiom already in the menu; "get ready to play" is also a Playing verb. Still an action with Undo, not a mode — no second source of truth over the five switches it sets. Zoom lock stays in the bundle, flagged as the first thing to cut if device testing dislikes it. Building. |
| 2026-07-26 | Spec **0036** built. **StagePreset** added to CONTEXT (G1): one Playing entry, label derived from the prefs, Undo snackbar naming what moved ("chrome hidden · status bar hidden · scale kept") and staying silent when the preset was a no-op. **PagePositionPill** shows "12 / 56" for 1.5 s on a page change while chrome is hidden — its own timer, never touching `PerformanceChrome`'s countdown, and wrapped in `IgnorePointer` so the bottom-right PageTurn zone still takes the tap (test taps *through* it rather than asserting the widget flag). Page scale sheet rewritten: "Applies to" with a per-scope sentence, lock relabelled **Keep this scale** with the mechanism demoted to the subtitle. 215 tests green. Awaiting G4 on device. |
| 2026-07-26 | Device fix on **0036**: the Undo snackbar never went away. `SnackBar.persist` defaults to `action != null` in Flutter 3.44, so *any* actionable snackbar stays until something replaces it — on a stand that means a bar parked over the Score and over the bottom PageTurn zone 0034 kept live. Extracted `undoSnackBar()` with `persist: false` (4 s, Undo still tappable) and a `hideCurrentSnackBar()` before showing so two presets in a row do not queue. Bisected with a throwaway harness: the action, not the preset's Scaffold reshuffle, was the whole cause. |
| 2026-07-26 | Three device findings from the 0036 run, all older than 0036. (1) **Scrubber drag lost to the OS**: 0034 shortened the PageNavBar to 48 px with no padding, leaving it flush on the home-indicator inset, and a horizontal drag there switches apps. The 8 px it used to have is now the explicit `kPageNavBarGestureGap`. (2) **Landscape switched off swipe PageTurn**: `_atFitZoom` (0033) compared the zoom against pdfrx's `minScale`, which is the scale that fits one whole page — in landscape that is a third of the fit-width zoom the viewer opens at, so an untouched Score read as zoomed in and swipes went to pan instead. (3) **Two pages cut the bottom off in landscape**: pdfrx fits document *width*, and a spread is then taller than a phone on its side. Both zoom bugs now go through one pure `pdfFitZoom(mode, viewSize, documentSize, spreadHeight)`, used for the reading check, for the viewer's initial zoom, and to re-fit after rotation when the musician had not pinched. |
| 2026-07-26 | Layout UI/UX review (Orchestrator asked whether the six modes are practical at all). Finding: **which layout is right is arithmetic, not taste** — compare the viewport's aspect against the page's √2 and the questions answer themselves. The peek is *free* until it eats the vertical slack: 35% on a 393×852 phone against a 28% default, so Half page (top/bottom) draws the music at exactly Single page's size and adds a quarter of the next page — but only 20% is free on a 375×667 phone, where the same default shrinks the music 15%. Single page wastes **51% of a landscape tablet**, where Two pages costs 2% of page size for twice the music. **Fit height has no describable job**: a duplicate of Single page on a portrait tablet, of Two pages on a landscape one, three tiny pages on a phone in landscape, and a 591-pt page inside a 393-pt viewport on a phone in portrait. Turn gestures are one global set across six layouts with different motion axes — Fit width scrolls vertically but turns horizontally, and Fit height's scroll drag *is* its turn gesture. Spec **0041** drafted `proposed` (polish **Q9**; P0.5 / P0.6 reopen candidates) with nine G3 questions; load-bearing ones are cutting Fit height and making **Auto** a real mode. It absorbs the Two-pages-in-portrait question, which was never in this log — the 0036 finding above is the landscape one. |
| 2026-07-26 | Spec **0041** accepted (G3) with all nine recommendations as drafted. **Fit height leaves the picker** — the enum value stays so 0027 restores and existing `layout_prefs.json` still load, mapping to Scroll, and P0.6 gets `wont` on that half rather than a quiet `done`. **Auto** is a real mode (default for new installs only), resolving from **LayoutFit** at build time, never written back to prefs, re-resolving on rotation and announcing itself once through the 0036 pill — a layout that changes under your hands mid-piece with no explanation is worse than a wrong layout. Tap zones and Swipe gain a **Match layout** default instead of six copies of the settings. Two pages on a viewport too narrow falls back to one page and says so, thresholded by `LayoutFit` so a portrait tablet keeps its usable spread. **LayoutFit** added to CONTEXT (G1). Building. |
| 2026-07-26 | Spec **0041** built. One refinement the arithmetic forced: **"a spread fits" and "a spread is free" are two different thresholds**, and the draft had only one. Fits is an absolute floor (`kMinSpreadPageWidth` 240 pt, fenced between the 196 pt a portrait phone gives and the 278 pt 0036 made work) and governs the Two-pages fallback, so a portrait tablet keeps its usable 417-pt spread. Free is `viewAspect ≥ 2 × pageAspect` and governs what **Auto** picks, so the portrait tablet is *not* pushed into a spread that halves its page size — the exact mirror of the free-peek rule, and what the G3 criteria already demanded. Cutting Fit height dissolved the scroll-vs-turn gesture collision instead of arbitrating it: no drawable layout scrolls horizontally any more, so nothing had to be taken away from 0033. Layout sheet rows are one line each after the first build came out at the 0035 height cap; the 0035 height test was rewritten to assert the property it was defending (a taller screen must not make a taller sheet) rather than a fraction the honest new content outgrew. Also caught in build: the notice pill fired on *opening* a Score, because prefs arrive a frame after the first resolve — `_announcedLayout` is cleared whenever prefs load, so only a rotation talks. 261 tests green. Awaiting G4 on device. |
| 2026-07-26 | Device finding on **0041**, older than 0041 and the more damaging half of it: **the peek was a whole page fitted into the band**, so on a phone the next page was drawn at 42% of the page beside it — the notes were all there and none of them readable, which is the Orchestrator's report that "one page + peek" is pointless. The free-peek arithmetic was true and irrelevant: it proved the *current* page kept its size and said nothing about what the peek showed. Fixed by clipping instead of fitting — the next page is laid out at the current page's own width, top-aligned, and whatever exceeds the band is cut off, so a peek shows the first system or two at the size they are about to be played. Recorded as 0041 decision 10. The side peek was never broken (a narrow strip is width-bound, so it already fitted by width) but goes through the same `halfPagePeekPageWidth` in `layout/half_page.dart`, which also caps the top peek at the current page's width for screens where the current page is height-bound — a band-wide peek there would draw the *next* page bigger than the music being played. Widening the separator now shows more of the next page rather than a bigger picture of all of it, which is what the slider always claimed to do. 267 tests green. |
| 2026-07-26 | Device finding: **the Score jumped back to page 1** on a layout change, on rotation with Auto, and on entering Draw mode. Three viewers, one hole — `_navPage` was described as authoritative but only `HalfPageView` was ever told what it was, so any rebuild opened `SinglePageSlider`, `ContinuousPageOrderView` and pdfrx at the front, and the fresh viewer then reported page 1 back over `_navPage`. All three now take the page at construction (pdfrx has `initialPageNumber`), which also closes the report-back race by construction rather than by filtering. Draw mode was a different fault with the same symptom: **`PerformanceChrome.active` is `_enabled && !_drawing`**, and the screen answered the two chrome states with two different Scaffold shapes, so the viewer was a different element in each and got rebuilt from nothing. Now one Scaffold with the Score always first in the same Stack, chrome either in the slots or floating over it. First guess — that the DrawToolbar arriving at index 0 shifted the viewer — was **wrong and worth remembering**: Flutter's child sync scans from the bottom as well as the top, so inserting a sibling above an unkeyed child does not lose its State. Proved it with a throwaway test before changing anything. Half page also stopped keying on layout mode: moving the peek from top to side is a rebuild, not a reason to reopen the PDF. One thing found on the way: `ContinuousPageOrderView._goToPage` used `ensureVisible` on a GlobalKey, which a lazy list has not built for a page forty rows away — scrubber jumps in a custom PageOrder were setting the number and not scrolling. It now lands near the row using the list's own extent estimate first. 268 tests green. |
| 2026-07-26 | Spec **0036 G4 pass** and Spec **0041 G4 pass**, both on device. Q4 / B3 and Q9 done; **Phase B is complete** and the next slice is **C1 — 0037** (duplicate Score), with C2 (0038) and C3 (0039) behind it. 0036 kept zoom lock: the run gave no reason to cut it, which was the one thing G3 flagged as expendable. 0041 closed with two things the arithmetic forced rather than the draft anticipating them — fits and free are separate spread thresholds (decision 6), and the peek clips instead of fitting (decision 10) — plus the page-reset fixes that Auto's rotation re-resolve exposed. Parity: P0.5 annotated with the two-page fallback that finally implements 0004's "when width allows", P0.6 keeps `wont` on fit height, Q9 done. |
| 2026-07-27 | Rename **StandScore → StageScore** finished on the user-visible surface: the Android launcher label and the iOS ShareExtension display name were both still "StandScore" (the half of the rename commit that shipped a wrong name to the launcher), brand assets renamed to `stagescore-logo*.png` along with the pubspec icon/splash/asset paths, stale `standscore/ios` and `standscore.iml` leftovers dropped, `AGENTS.md` retitled and its status line corrected (it still said "next Phase B"), and the `stagescore/README.md` spec table replaced with a pointer to `docs/specs/` rather than a second list that had drifted back to 0005. **Deliberately not renamed:** the on-device library root `<documents>/standscore/` and `LibraryBackup.formatId` / `markerFileName` — renaming either orphans every existing library and makes pre-rename backup ZIPs unrestorable, and neither string is ever read by a musician. Commented at the constants and in the README so it does not get "cleaned up" later. 268 tests green. |
| 2026-07-27 | Release track opened (not in the roadmap, which covers product slices only): [RELEASE-CHECKLIST.md](./RELEASE-CHECKLIST.md) for the `1.0.0+1` bump. Two real blockers found by inspection, neither previously noted. **R1:** Android release builds still sign with the **debug** keystore (`signingConfigs.getByName("debug")`, no keystore in the repo), which Play rejects outright. **R2:** there is no `PrivacyInfo.xcprivacy` at all, and the app has a concrete required-reason API use — 0040's thumbnail cache keys on the PDF's mtime — plus four plugins whose manifests need auditing. Four Orchestrator-only items are parked there: the trademark check on "StageScore" and the logo lock (both open since 2026-07-22), the monetization decision, and the load-bearing one — **ship v1 now or finish Phase C first**, since parity P0–P2 is done and roadmap Exit A is met, so v1 is defensible today. |
| 2026-07-27 | Orchestrator: **ship v1 before Phase C**. Phase C (0037–0039) becomes 1.1. |
| 2026-07-27 | Both release blockers closed. **R1:** upload key is a PKCS12 at `~/keystores/stagescore-upload.jks` (outside the repo) with a gitignored `android/key.properties`; the `release` build type now builds a real signing config from it and, when the file is missing, leaves the build **unsigned rather than debug-signed** — the old fallback shipped a debug-signed artifact that *looks* releasable, which is the trap worth removing rather than merely fixing. Verified by a real `bundleRelease` + `jarsigner -verify`. **R2:** two `PrivacyInfo.xcprivacy` files (app and ShareExtension, each its own bundle), wired in through the `xcodeproj` gem so the `project.pbxproj` diff is 8 added lines and nothing is reformatted. Reasons came from grepping the four manifest-less plugins instead of guessing: `C617.1` file timestamp (0040's thumbnail cache keys on PDF mtime, plus miniaudio), `1C8F.1` App Group user defaults (share_handler, 0029), `35F9.1` boot time (miniaudio's timing under the metronome, 0030). Verified by finding both files inside the built `Runner.app`. |
| 2026-07-27 | Two findings the release builds exposed, both invisible to every debug run we have ever done. **The iOS release build was broken by the folder rename:** `build/` and `.dart_tool/` had absolute paths into `sheet-app/standscore/`, so Xcode hunted `pdfium_flutter`'s XCFramework in a directory that no longer exists; `flutter clean` is the whole fix, but nothing short of a release build reports it. **The bundled sample Score still said "StandScore spike" on every staff** — it ships in `assets/`, the Library's sample action imports it into the musician's library, and no text search could ever have found it because a PDF's content streams are compressed (the earlier rename sweep was text-only, so it passed clean while the wrong name sat in a shipped asset). Regenerated from a new committed `tool/generate_sample_score.dart` — six A4 pages of empty staves, our own text — so the asset is reproducible rather than an orphan binary, which also settles the "do we own the sample PDF" question. |
| 2026-07-27 | Two release-checklist rows dissolved on inspection rather than needing work. **R15 was my own misreading:** the 13.0 iOS deployment target sits at *project* level and every target already overrode it with 14.0, matching the Podfile — so there was never an app-vs-extension mismatch. Aligned the project default to 14.0 anyway so a future target cannot inherit 13.0. **R14** clears untouched: Flutter 3.44.6 gives `targetSdk`/`compileSdk` 36 and `minSdk` 24. Left open for the Orchestrator: trademark check, logo lock, monetization, `wont` list, store listing content, privacy/support URLs, and a device smoke test of the release builds. |
| 2026-07-27 | Follow-on from keeping the old on-device folder name: the string `'standscore'` was a literal in **three** screens (`main`, Library, PdfMode), so the "do not rename this" rule was documentation over a duplicated fact — rename one and part of the app quietly reads an empty folder while the rest still works, which is data loss that looks like a bug. Now one `libraryRootDirName` + `openLibraryRoot()` in `lib/library/library_root.dart`, and the old name survives in exactly two files. 268 tests green, analyze clean. |
| 2026-07-28 | Ecosystem messaging review (Orchestrator asked): **nothing a musician reads in StageScore says it comes from Backing & Score** — zero brand hits in `lib/`, the Library app bar is a 32 px mark with the product name only in its `Semantics` label, and the About screen where ADR 0009 parked the publisher does not exist, so the app also shows no version anywhere. The store listing (R12), still unwritten, is the largest surface of all. Found while checking the other side: `backingscore.com`'s codebase mentions StageScore **nowhere**, so the ecosystem claim is currently one-directional. Also found a spelling split — the repo says *BackingScore*, the shipped web product says **Backing & Score** in all nine locales, emails and auth screens, and the one-word form appears nowhere in its `src/`. Cheap to settle now because neither the listing nor an About screen is written yet. Drafted ADR **0010** (`proposed`, G2) fixing the human-readable spelling while leaving `com.backingscore.*`, the domain, and the upload cert untouched, and Spec **0042** (`proposed`, G3) for the messaging slice: About sheet on the 0035 idiom with bundle-sourced version and the R10 privacy/support links, one quiet line in the Scores empty state, an R12 listing draft, and a guard test keeping every brand string out of PdfMode. Seven G3 questions; the load-bearing ones are the spelling (blocks the rest), tappable links (`url_launcher` + an Android 11 `<queries>` entry, or the release build no-ops silently), and whether this rides v1 or waits for 1.1. Localisation — English-only app vs nine-locale web — noted as a real gap and explicitly cut to its own slice. |
| 2026-07-28 | ADR **0010 accepted** (G2) and Spec **0042 accepted** (G3, ships in **v1**), then built. Brand is **Backing & Score** for humans, `backingscore` for identifiers — no bundle ID, package name, on-device path or keystore moves. Shipped: an **About sheet** under Library ⋯ on the 0035 idiom (mark, product name, version *and* build read from the bundle via `package_info_plus`, the publisher line and one sentence about the web side, then site / privacy / support rows through `url_launcher`), one caption in the **Scores** empty state — not Setlists, by then they know — and the R12 listing copy in a new `STORE-LISTING.md`. Three build deviations from the draft, all recorded in the Spec: the PdfMode silence **guard is a source-level test**, because `PdfModeScreen` cannot be pumped (pdfrx) and a widget assertion would have characterised a stub; **support is a `mailto:`**, since the site has `/privacy` and `/terms` but no support page; and brand strings live in their own `lib/brand/brand.dart` with a test that fails if any of them appears outside the Library — the invariant the StandScore launcher bug actually needed, since that was duplication rather than a missing rename. Two Android `<queries>` entries (`https` VIEW, `mailto` SENDTO) or the links no-op in the release build only. Both new dependencies were already transitive under `share_plus`, so the graph is unchanged. 280 tests green, analyze clean. Two checklist rows added: **R19** done, and **R20** `human` — `backingscore.com` still does not mention StageScore anywhere, so the app now claims an ecosystem its own site does not list. R10 is closer than it read: `/privacy` is live and unprefixed. |
| 2026-07-28 | Orchestrator **accepted the store listing copy** (R12) as drafted; only the six screenshots remain there, and they wait on the logo lock (R6) or they get shot twice. **Cloudflare Email Routing enabled** the same day, so `support@backingscore.com` — the address StageScore's About sheet has been opening since 0042 — now actually receives: MX resolves to `route1/2/3.mx.cloudflare.net` and the root SPF is Cloudflare's. Two things that turned up while verifying it. Apple's **Support URL field takes a URL, not an address**, so R10 still needs a `/support` page on the web even though the mailbox works. And `_dmarc.backingscore.com` is **empty** — filed as **R21**: not a store requirement, but Gmail and Yahoo have demanded DMARC from bulk senders since 2024 and every verification mail the web app sends leaves under this domain, so `p=none` with a `rua=` and a look at the reports is the cheap first move. Outbound is unaffected by the new MX: Resend signs with `resend._domainkey` and uses the `send.` subdomain's SPF, which the root record does not touch. |
| 2026-07-28 | Web-side work for R10 and R20, written in the `backing-and-score` repo (branch `main`, uncommitted, typechecks clean, no new lint errors). The reason this turned out to matter more than "add a support page": **the live privacy policy contradicted the app.** `/privacy` describes a platform that rigorously logs which sheet music you open and how long you play, runs Mixpanel, and mails you when you have not practised — all true of the web product and none of it true of StageScore, whose `PrivacyInfo.xcprivacy` declares no tracking and no collection and whose Data Safety answers say the same. Submitting with that URL would have handed App Review a three-way disagreement between the policy, the manifest and the form; filed as **R22**. Fixed with a scoped section 7 at the *same* URL, because the app already ships `Brand.privacyUrl` pointing there and moving the URL means shipping a new build. Also written: a **`/support`** page (Apple's Support URL field wants a URL, and a mailbox is not one) that covers both products and tells musicians to paste the version from ⋯ → About; `Support` added to the cross-link row on privacy / terms / refund; and a **StageScore section on `/about`** plus a StageScore block on `/support` for R20, labelled "coming soon to the App Store and Google Play" since v1 is not published. i18n keys went into `en` and `vi` only — `src/i18n/request.ts` deep-merges English as a fallback, which is already how the other seven locales render most of that page, and inventing unreviewed German is worse than falling back. Nothing is deployed; both rows stay open until it is live. |
| 2026-07-28 | ADR **0011 accepted** (G2): StageScore's web home is the **path `backingscore.com/stagescore`**, not a subdomain and not a domain of its own — built the same day. Three findings shaped it. **`stagescore.com` is taken** (registered 2013) and currently parked *for sale* through Atom, so it is a broker negotiation rather than a registration; nobody is running a product there, which is mildly reassuring for R5. **`stage.` was the wrong name even though it is free** — `stage.`, `staging.` and `app.` are all unused and staging runs on Vercel Preview URLs, so nothing collides today, but "stage" reads as pre-production to everyone who deploys, and that ambiguity would be permanent. And the decision turned out **not to block submission**: the app has `Brand.siteUrl` and `Brand.privacyUrl` compiled in, but `backingscore.com` keeps serving `/privacy` and `/support` under any of the three options, so a later move is a 301 and never a forced rebuild. The deciding argument against a separate site was the one Spec 0042 just spent itself on — a separate domain works against the ecosystem legibility we were buying — plus R22's lesson that a second privacy policy is the document you least want to keep in sync. Built: the landing page as a **server component** with `getTranslations` (the rest of the site's i18n pages are all `"use client"`, which cannot export metadata, and a marketing page wants metadata and no client JS), a 27-key `StageScore` namespace in `en` and `vi`, `/about` now linking to it, and `/support` doing the same. Typecheck clean; the only eslint errors on the touched files are the seven pre-existing `as any` on `/about`. Rendered and checked at `/en/stagescore` and `/vi/stagescore`. Still nothing deployed. |
| 2026-07-28 | Orchestrator's brief: "the web app collects nothing and never will — update privacy and terms." **The first half did not survive the code.** The schema stores account name / email / avatar / bio, **IP address and user-agent on every sign-in** (`sessions`, better-auth's default), practice records with duration, tempo, Wait Mode score and input type (`practice_sessions`, written from `actions/v5/gamification.ts`), XP and streaks, social content, and Polar subscription state. Writing "we collect nothing" over that would have been a false statement in a legal document — the same failure as R22, pointed the other way, and the direction regulators care about more. Filed as **R23**. What the audit *did* confirm is that the intent is real and the old policy was fiction: **no analytics package of any kind** in the dependency tree (the policy named Mixpanel), billing is **Polar** and not Paddle or RevenueCat, **nothing anywhere reports play history** to publishers or Hal Leonard, and Wait Mode's pitch detection runs in the browser with no upload path near `getUserMedia`. LiveKit has no egress or recording configured. Sentry is production-only, sampled at 0.1, without `sendDefaultPii`. So `/privacy` was rewritten from evidence: a plain-language summary, an itemised list of what is stored *with the feature that needs it*, an explicit list of what is not done, and StageScore as section 7 — still collecting nothing. `/terms` corrected in two places: the merchant of record, and the royalty clause that implied per-user play reporting. Typecheck and lint clean, both pages rendered. **Not a lawyer, and this is a legal document** — it is accurate to the code today and should still be read by someone qualified before money moves, especially with EU users. |
| 2026-07-28 | Orchestrator confirmed the four facts R23 was blocked on, and each one shortened the policy. **Sentry** was never configured, so it left the provider list entirely and `/privacy` now states that no error monitoring runs at all — a stronger claim than describing a sampled, PII-free Sentry, and true. **Polar** is still sandbox, so payments are written as not yet open rather than as an active processor relationship. **Social feed** is off, verified rather than assumed: `app/[locale]/_feed` is an underscore-private route Next never serves, and `featureFlags.socialFeed` defaults false — so posts and comments came out of the list of stored content. Two things the same check said to *keep*: **LiveKit** is still reachable through `/classroom/[id]/live`, and **favourites** still run from the player, discover cards and collections, so both stay. Net effect is that the provider list is down to six and the "what we do not do" section grew. Left open as **R24**: `practice_sessions` is the only remaining row that records how well someone played rather than what their account needs, and disabling streak / XP would let the policy drop it. Better decided before deploy than after — a public privacy policy is an awkward thing to revise. |
| 2026-07-28 | R24 resolved the good way: **keep streak / XP, drop the storage behind it.** The deciding fact came out of the code rather than the debate — `practice_sessions` had exactly one writer (`processPracticeSessionV5`) and **no reader anywhere in the app**. It was a write-only table: every run recorded which piece was played, the tempo reached, the Wait Mode score and whether the input was mic or MIDI, and nothing ever read a row back. Streak and XP are computed from `user_stats` alone — four running totals plus `lastPracticeDate` — so deleting the table costs the musician nothing and removes the only data we held that an account does not need. Both inserts gone, table gone from the schema, `sessionId` dropped from the action's return (no consumer — `PlayShell` reads only `data.addedXP`), dead `APPWRITE_PRACTICE_SESSIONS_COLLECTION_ID` constant gone, and migration `0028_chemical_emma_frost.sql` generated as a single `DROP TABLE`. **Deliberately not applied** — dropping a table with real rows in it is the Orchestrator's call, not an agent's. A comment sits on the action and where the table used to be in the schema saying not to add the row back, because the next person to want practice analytics will reach for exactly that. `/privacy` now reads "the run itself is thrown away … there is no practice history to hand over, because we never write one" — a claim the code enforces rather than promises. |
| 2026-07-28 | **Correction to the audit above: RevenueCat is real.** The earlier entry recorded "billing is Polar and not Paddle or RevenueCat", which was drawn from `package.json` — and RevenueCat is not a dependency because it needs no SDK. It is a fully implemented webhook at `/api/webhooks/revenuecat` that grants entitlements from Apple and Google in-app purchases, and it surfaced in the production build output, not in any dependency list. Currently inert: the route rejects every request unless `REVENUECAT_WEBHOOK_SECRET` is set, and it is not. `/privacy` now names it alongside Polar, both marked as not yet switched on. The lesson for the next audit is that a dependency list is evidence of what a codebase *imports*, not of what it *integrates* — webhooks leave no trace there. Also confirmed while checking deployability: `build` is a plain `next build` with no migrate step, there is no `vercel.json` and no runtime `migrate()`, so **deploying does not drop `practice_sessions`** — the writes stop immediately but the old rows survive until migration 0028 is applied by hand. Three commits on `main` of the web repo, not pushed. |
| 2026-07-28 | Web changes live on production and verified by fetching the pages rather than trusting the deploy: `/privacy`, `/terms`, `/support`, `/stagescore` all 200, the new privacy text present, and Paddle, LemonSqueezy and Hal Leonard gone from the terms. That closes **R10**, **R22** and **R23**. The app now has two real URLs for the store forms — `backingscore.com/privacy` and `backingscore.com/support` — which was the actual blocker behind R10. **R24 stays open on purpose:** deploying stopped the writes, but migration `0028` has not run, so the historical `practice_sessions` rows are still in Turso while the policy says no practice history is kept. Nothing is broken by that — no code reads the table — but it is a gap between the text and the database that only a data request would expose. |
| 2026-07-28 | **Review UI/UX so với ScorePDF** (Orchestrator: "StageScore vẫn cảm thấy basic so với ScorePDF"). Kiểm tra đầu tiên là đây có phải khoảng cách năng lực không — không phải: checklist parity vẫn `done` xuyên suốt P0–P2, và bản thân ScorePDF khoá Label / Sort / Search / Metronome sau một gói Pro trả phí, trong khi StageScore cho miễn phí. Vậy đợt audit chuyển hướng sang cách trình bày và IA: đọc code `lib/ui/*` (library, PdfMode, `score_menu.dart`, draw toolbar, settings sheets) cộng ảnh chụp thật từ máy màn hình Home và ScoreDetail của ScorePDF — ảnh marketing App Store xem trước đó hoá ra lại mô tả một drawer phụ, không phải IA chính của ScorePDF, nên bị gạt sang một bên khi có ảnh thật. Phát hiện, tệ nhất trước: **mọi công cụ PdfMode trừ Draw đều nằm sau một icon ScoreMenu `⋯` duy nhất** (`score_menu.dart:74-143`), trong khi ScoreDetail của ScorePDF giữ một **tab-strip có nhãn cố định ở đáy** (Bookmark · Page · Gesture · Tools · Annotation) luôn hiển thị dưới thanh trượt trang; các sheet cài đặt của StageScore hiện ra ở **năm kiểu trọng lượng thị giác khác nhau** (Done+titleLarge / chỉ titleMedium / không-Done / fixed-55%-height / full Scaffold) trong khi ScorePDF dùng lại một hàng icon+tiêu đề+mô tả phụ+switch ở mọi nơi; toolbar vẽ tự tint mình bằng **sai màu brand teal** (`0xFF0D9488` trong `draw_toolbar.dart:63-65` so với `0xFF0D8B86` trong theme); không có lớp design-token (`app_appearance.dart:50-57` chỉ là seed-color, không có thang spacing/radius/elevation); và không có onboarding nào ngoài một snackbar. Một giả thuyết ban đầu "thêm grid thumbnail vào Library" đã bị **rút lại** khi ảnh Home thật của ScorePDF cho thấy hàng chỉ có title, không có thumbnail trong thực tế dùng — không có bằng chứng cho ý đó, và hàng của StageScore vốn đã dày hơn (thumbnail, ngày, số trang, label). Đã drafted **Phase E** trong `IMPROVEMENT-ROADMAP.md` (`proposed`, song song an toàn với Phase C) và các ID polish **Q10–Q16** trong `SCOREPDF-PARITY.md`. Về câu hỏi tải trọng nhất — đặt tab-strip mới cạnh `PageNavBar` hay thay hẳn nó — Orchestrator chọn **thay hẳn**: hàng thanh trượt giữ nguyên, nằm ngay trên hàng tab có nhãn mới đúng như ảnh chụp của ScorePDF, và `⋯` trên AppBar biến mất. Điều này mở lại lựa chọn có chủ đích của Spec 0035 (một sheet gộp thay vì tab-bar); bốn nhóm của 0035 (Go to / Marks / View / Playing) trở thành bốn tab, nội dung không đổi. Đánh số: 0037–0039 vẫn dành cho Phase C, 0040–0042 đã `done`, nên Phase E lấy **0043–0049** cộng dùng lại **0039** cho onboarding, xếp lại thứ tự trong phase. Spec đầu tiên cần soạn là **0043** (cơ chế tab-strip); **0044** (design token) theo sau trước khi chạm chi tiết thị giác của E3–E7, vì chúng hiển thị đẹp hơn một khi token tồn tại. |
| 2026-07-28 | Spec **0043** (tab-strip thay icon `⋯` ScoreMenu, hạng mục E1 của Phase E) đã drafted `proposed`. Sáu câu hỏi G3; hai câu tải trọng nhất là mỗi tab mở sheet riêng hay panel nội tuyến (khuyến nghị: sheet riêng, dùng lại nguyên nội dung theo nhóm của 0035) và hàng mới cộng thêm vào ngân sách chiều cao chrome hay thay chỗ của `PageNavBar` (khuyến nghị: cộng thêm ~48px, vì nhồi tab vào hàng thanh trượt là kiểu chật chội mà 0035 đã từng phải sửa). Nội dung bốn nhóm (Go to / Marks / View / Playing) không đổi so với 0035 — Spec này chỉ đổi cách vào. Đang chờ G3. |
| 2026-07-29 | Spec **0043** accepted (G3) với cả sáu khuyến nghị như bản nháp: sheet riêng cho mỗi tab dùng lại nguyên code dựng nội dung theo nhóm của 0035; không badge trạng thái trên tab; `showChrome` khi chrome đã hiện mở tab **Go to**; hàng tab-strip cộng thêm ~48px vào ngân sách chrome đáy, `PageNavBar` giữ nguyên vị trí; icon Material có sẵn cho slice này; Draw không trở thành tab thứ năm, giữ nguyên trên AppBar. Building — cập nhật định nghĩa **ScoreMenu** trong `CONTEXT.md` (lối vào là tab-strip, không còn `⋯`) là việc bắt buộc ở G1 trong lúc build. |
| 2026-07-29 | Spec **0043** built. Icon `⋯` bỏ khỏi AppBar; `ScoreMenuTabStrip` mới (`lib/ui/score_menu_tab_strip.dart`) là sibling của `PageNavBar`, cả hai fade cùng nhau như một khối trong overlay chrome của 0034. Mỗi tab tra nhóm của 0035 bằng `title` (không phải vị trí danh sách) rồi mở `showScoreMenu` chỉ với nhóm đó — một test khoá tập `ScoreMenuTab.values` khớp đúng thứ tự bốn nhóm của `buildScoreMenu`, để lỡ có Spec sau đổi thứ tự nhóm thì test đỏ thay vì tab âm thầm mở nhầm sheet. Hai lệch so với bản nháp G3, đều ghi trong Spec: (1) dòng kỹ thuật nhắc tới `_overflowMenuKey.currentState?.showButtonMenu()` không khớp code thật (AppBar gọi thẳng một hàm mở `showModalBottomSheet`) — sửa lại mô tả, không đổi quyết định; (2) `PageNavBar` không còn là widget chạm đáy màn hình nên gọi với `avoidNotches: false` (cờ 0032 đã có sẵn) để khỏi cộng dồn khoảng đệm home-indicator hai lần — tab-strip nhận cờ đó thay. Không đụng vào nội bộ `page_nav_bar.dart`. `CONTEXT.md` ScoreMenu cập nhật ở G1: lối vào là tab-strip, không còn nhắc `⋯`. `test/pdf_mode_chrome_layout_test.dart` (đặc tả hình Scaffold, `PdfModeScreen` không pump được) viết lại theo cấu trúc `Column([pageNav, tabStrip])` mới. 285 tests xanh, analyze sạch. Chờ G4 trên thiết bị thật. |
| 2026-07-29 | Spec **0043 đảo lại, cùng ngày, trước G4** (Orchestrator). Lý do: nhãn tab-strip ("Go to"/"Marks"/"View"/"Playing") là chữ tiếng Anh cố định trong một hàng phải vừa bốn tab trên chiều rộng điện thoại nhỏ nhất — app English-only hôm nay (khoảng cách 0042 đã ghi), nhưng một bản dịch với từ dài hơn làm vỡ hàng đó ngay khi có localisation, không phải rủi ro giả định. Chốt lại qua ba câu hỏi: (1) nhóm nào "hay dùng" đủ để có lối tắt riêng — chỉ **View** (Layout/Display/Color filter/Page scale), không phải Go to hay Playing như agent khuyến nghị; (2) `⋯` sau đó mở gì — vẫn đủ bốn nhóm như trước 0043, không rút gọn; (3) hình thức lối tắt — **chỉ icon, không nhãn**, miễn nhiễm hoàn toàn với độ dài chữ dịch, giống hệt Draw/Metronome đã có. Tab-strip, enum `ScoreMenuTab`, widget `ScoreMenuTabStrip` và test riêng của nó bị xoá; `⋯` và `_openScoreMenu()` trở lại đúng như trước Spec 0043; thêm một `IconButton` mới (`_openViewMenu`) cạnh Draw. `page_nav_bar.dart` và `pdf_mode_chrome_layout_test.dart` quay lại nguyên bản trước 0043 — bản tab-strip đã đổi `PageNavBar` sang `avoidNotches: false`, rút lại luôn. `CONTEXT.md` ScoreMenu sửa lại: lối vào chính vẫn `⋯`, cộng một câu về icon-tắt View không nhãn. Spec 0043 giữ nguyên số, phần "Revision" chép lại quyết định G3 đầu để làm bằng chứng chứ không xoá. 280 tests xanh (285 trừ 5 test của `ScoreMenuTabStrip` đã xoá), analyze sạch. Vẫn chờ G4. |
| 2026-07-29 | Spec **0043 Revision 2, cùng ngày, trước G4** (Orchestrator): tab-strip trở lại — nhưng icon-only, không có nhãn chữ nào cả, nên không còn mang rủi ro i18n mà Revision 1 (dòng trên) vừa dùng để đảo bản đầu. Yêu cầu cụ thể: Draw và các menu item khác nên hiện dạng tab-strip **ngay dưới `PageNavBar`**, không gom vào một icon con-mắt duy nhất; mỗi item cũng cần một icon phù hợp — Orchestrator trỏ tới opensvg.dev/icons làm nguồn tham khảo. Ba câu hỏi chốt trước khi build: (1) nội dung tab-strip — 3 icon cũ (Metronome khi chạy, Draw, View) **cộng** Bookmarks và Layout, 5 icon-tắt cho action hay dùng khi đang chơi, không phải bốn nhóm 0035 hay toàn bộ 12 action; (2) `⋯` — giữ nguyên, vẫn liệt kê đủ cả 12 action kể cả những action đã có icon-tắt riêng, giống hệt cách Metronome/View đã làm; (3) nguồn icon — **Material Symbols có sẵn trong Flutter**, không vendor SVG hay thêm package mới, vì opensvg.dev tuy tìm-kiếm-được thật (đã kiểm bằng browser, có kết quả "metronome" thật) nhưng là trang bán gói truy cập trọn đời để export SVG hàng loạt, không phải nguồn free-and-clear cho việc copy code hàng loạt. Build: `ScoreMenuEntry` có thêm trường `icon` bắt buộc, `buildScoreMenu` gán icon cho cả 12 action (hằng số `kBookmarksIcon`…`kStagePresetIcon` trong `score_menu.dart`, dùng Material Symbols outlined); `score_menu_sheet.dart` hiện icon đó làm `leading` mỗi `ListTile`. Widget mới `ScoreMenuQuickBar` (`lib/ui/score_menu_quick_bar.dart`) — hàng icon-only ngay dưới `PageNavBar`, cả hai đi cùng nhau thành `bottomChrome = Column([pageNav, quickBar])`, dùng chung cho slot `bottomNavigationBar` và overlay PerformanceMode. AppBar rút về chỉ còn `⋯` — Draw, Metronome, View dời hết xuống quick-bar; Bookmarks/Layout trên quick-bar gọi thẳng `_onScoreMenuSelected`, bỏ qua sheet. `PageNavBar.avoidNotches` → `false`, quick-bar nhận trách nhiệm giữ khoảng đệm home-indicator thay. `CONTEXT.md` ScoreMenu cập nhật. `test/pdf_mode_chrome_layout_test.dart` viết lại hình học theo `bottomChrome` mới; thêm `test/score_menu_quick_bar_test.dart` và một test icon-theo-trạng-thái trong `score_menu_test.dart`. 285 tests xanh (280 + 5 mới), analyze sạch. Vẫn chờ G4. |
| 2026-07-29 | Hai sửa nhỏ trên bản Revision 2 (Spec 0043), trước G4. (1) Icon Metronome trong sheet `⋯` — Orchestrator: icon Material chung (`Icons.speed_outlined`) "không hợp lý", sửa lại vẽ đúng `MetronomeIcon` (glyph riêng của app) như quick-bar đã dùng, thay vì `entry.icon`. (2) Vùng `ScoreMenuQuickBar` "nằm sát mép quá, đang conflict với vùng control của OS" — Orchestrator đề xuất bọc SafeArea toàn app; từ chối đề xuất đó vì PdfMode có thiết kế edge-to-edge cố ý khi PerformanceMode hiện (test đã khoá "runs to the bottom edge so PageTurn taps land there") — bọc SafeArea toàn app sẽ phá chính thiết kế đó. Sửa đúng chỗ thay vì bọc rộng: thêm `kScoreMenuQuickBarGestureGap` (12px, không điều kiện, cùng lý do `kPageNavBarGestureGap` của 0034 — `MediaQuery.padding` báo một inset mỏng hơn vùng ngón tay cần né trên máy Android cử chỉ) làm đệm dưới hàng icon, bên trong `SafeArea` đã có. `pdf_mode_chrome_layout_test.dart` cập nhật công thức khoảng cách. 285 tests xanh, analyze sạch. Vẫn chờ G4. |
| 2026-07-29 | **Musician báo cáo tap-to-turn-page chậm rõ so với ScorePDF.** Root cause tìm được bằng cách đọc `PageTurnInteractionLayer`: `onTap` (PageTurn) và `onDoubleTap` (zoom, Spec 0033) sống chung một `GestureDetector`, và `doubleTapZoomEnabled: !_pageScalePrefs.locked` bật mặc định (vì `locked = false` cho mọi user chưa từng đụng Page scale). Hai handler đó cùng chỗ buộc Flutter's gesture arena giữ **mọi** single-tap trong `kDoubleTapTimeout` (~300ms) chờ tap thứ hai trước khi bắn `onTap` — cơ chế nội bộ framework, không sửa được nếu vẫn giữ cả hai. ScorePDF không có double-tap-zoom cạnh tranh nên tap của nó không bao giờ đợi. Đưa ra ba hướng xử lý (optimistic-tap-rồi-undo, bỏ hẳn double-tap giữ pinch, hoặc rút ngắn timeout bằng custom recognizer) qua `AskQuestion` — Orchestrator không chọn qua đó mà quyết định thẳng ở lượt sau. |
| 2026-07-29 | **Bỏ double-tap-to-zoom, giữ pinch; pinch scale giờ chia sẻ qua các trang** (Orchestrator, tiếp theo phát hiện trên, tái mở một phần Spec 0033). `PageTurnInteractionLayer` không còn `onDoubleTap`: tap turn page tức thì, không còn chờ disambiguation. Cộng thêm cùng lúc (yêu cầu riêng, không phải hệ quả bắt buộc): một khi zoom chỉ còn qua pinch, scale đã pinch trên một trang phải theo sang trang khác thay vì mỗi trang tự trả về fit. `SinglePageSlider` / `HalfPageView` / `ContinuousPageOrderView` (ba viewer giữ `TransformationController` riêng theo trang, vì `PageView`/`ListView` builder theo index) đều gọi ba hàm thuần mới trong `lib/pdf/shared_zoom.dart` từ `InteractiveViewer.onInteractionEnd` (một lần khi gesture kết thúc — không phải mỗi frame khi đang pinch, tránh rebuild thừa các trang khác đang không tương tác); trang mới mở khởi tạo transform đã có sẵn scale chia sẻ, pinch về fit trên bất kỳ trang nào cũng lan ra các trang còn lại. Continuous "identity" viewer (layout mặc định, `PdfViewer` của pdfrx trực tiếp) vốn đã chia sẻ zoom tự nhiên — toàn tài liệu là một canvas cuộn liên tục — nên không cần sửa. `zoom_toggle.dart` giữ `isInteractivelyZoomed`, bỏ `toggledZoomMatrix`/`toggleTransformationZoom` (chỉ phục vụ toggle fit↔2× của double-tap, nay chết). Page scale sheet bỏ nhắc "double-tap" trong subtitle "Keep this scale". Ghi vào Spec 0033 phần "Cập nhật sau G4 (tái mở một phần)". |
| 2026-07-29 | **Mở rộng cùng ngày: chia sẻ cả pan, không chỉ scale** (Orchestrator). Lý do cụ thể: một bản scan lề trái rộng lề phải hẹp thì sau khi zoom, musician còn phải pan lệch khung nhìn để bù lề — và pan đó nên theo sang trang khác giống hệt scale, vì các trang cùng tài liệu thường lệch lề như nhau. `shared_zoom.dart` đổi từ `double? scale` sang `Matrix4? transform`: `nextSharedZoomTransform` (đọc cả scale lẫn translation từ `TransformationController.value`, trả `null` khi về fit dù đang pan dở — pan chỉ có nghĩa khi có zoom), `sharedZoomTransformChanged` (so hai `Matrix4` theo từng phần tử `storage`, ngưỡng nổi 0.005, nên **cùng scale khác pan vẫn tính là đổi** — đây là điểm khác biệt cốt lõi so với bản chỉ-scale trước đó), `sharedZoomMatrix` (luôn `.clone()`, vì `Matrix4` mutable — hai trang không bao giờ được trỏ chung một instance, nếu không một trang chỉnh sửa transform tại chỗ sẽ làm hỏng transform của trang kia). `SinglePageSlider`/`HalfPageView`/`ContinuousPageOrderView` đổi tên biến `_sharedZoomScale` → `_sharedZoomTransform`, gọi API mới, không đổi luồng propagate (vẫn qua `onInteractionEnd`). Test: `shared_zoom_test.dart` viết lại toàn bộ cho `Matrix4`, thêm ca "same scale but a meaningfully different pan is still a change" và "returns a fresh copy each time" khoá đúng hai điểm mới. 300 tests xanh, analyze sạch. Spec 0033 "Cập nhật sau G4" cập nhật để nói rõ transform, không chỉ scale. |
| 2026-07-29 | **Sửa bug cùng ngày: "vị trí page không chuẩn" sau khi next/prev** (musician báo cáo, đúng ngày tính năng chia sẻ pan vừa xong). Root cause đọc thẳng từ source Flutter (`interactive_viewer.dart`): `onInteractionEnd` bắn **ngay lúc buông tay**, nhưng doc-comment của chính widget cảnh báo "a pan may cause an inertia animation after this is called as well" — `_handleInertiaAnimation` còn ghi tiếp vào cùng `TransformationController` thêm vài frame cho hiệu ứng trôi quán tính (fling) sau khi thả tay. Code cũ đọc `t.value` đúng lúc `onInteractionEnd` bắn, tức là **giữa lúc đang trôi**, không phải vị trí musician thật sự dừng — nên trang chia sẻ sang lệch khỏi ý định. Sửa bằng debounce trên listener của `TransformationController` (không còn tin `onInteractionEnd`): mỗi lần giá trị đổi (kể cả do chính animation quán tính ghi) thì huỷ timer cũ, đặt lại timer `kZoomSettleDelay` = 150ms (vượt xa khoảng cách một frame ở 60–120fps); hết timer mà không có thay đổi nào thêm mới coi là "đã dừng hẳn" rồi mới chia sẻ. `onInteractionEnd` bỏ hẳn khỏi `PerformancePageSlot` (không còn cần). Ba view (`SinglePageSlider`/`HalfPageView`/`ContinuousPageOrderView`) đổi tên hàm `_onZoomGestureEnd` → `_onZoomSettled`, thêm `Timer? _zoomSettleTimer` huỷ trong `dispose()`. `kZoomSettleDelay` đặt trong `shared_zoom.dart` để cả ba dùng chung một con số. Test: một ca mới khoá `kZoomSettleDelay` đủ lớn hơn frame gap. 301 tests xanh, analyze sạch. Vẫn cần xác nhận trên thiết bị thật — độ trễ animation quán tính thực tế trên máy có thể khác giả định từ đọc source. |
| 2026-07-29 | **Spec 0043 Revision 3, cùng ngày, trước G4** (Orchestrator: "các item trên tabStrip vẫn chưa ổn, còn các màn hình to nhỏ khác nhau thì thế nào"). Cả hai câu đều đo được, và phép đo tìm thêm một lỗi thật. **Vì sao các item chưa ổn: hàng đó chưa từng có luật chọn** — ba revision trong một ngày đều đổi danh sách bằng khẩu vị, nên bản thứ tư cũng sẽ vậy. Bốn hệ quả cụ thể: **View là một *nhóm* đứng cạnh bốn *action*, và Layout — con của chính nhóm View — cũng trên cùng hàng** (một sheet, hai lối vào, hai độ sâu); `kViewGroupIcon` và `kAnnotationsVisibleIcon` **cùng là `Icons.visibility_outlined`** (một con mắt, hai nghĩa, một màn hình); Metronome hiện-khi-chạy làm **cả hàng reflow đúng lúc đang chơi** (đo: Draw nhảy 64→124, Bookmarks 152→198, Layout 240→272, View 328→346 trên iPhone 15 dọc) *và* không cho **bật** metronome từ lối tắt — nó chỉ xuất hiện sau khi đã bật bằng đường khác; Layout là việc lúc mở bài. **Vì sao kích thước là vấn đề:** Spec chỉ ràng buộc *chiều rộng* điện thoại hẹp nhất, nhưng chỗ vỡ là *chiều cao* — chrome xếp chồng chiếm **54% chiều cao điện thoại nằm ngang (Score còn 180pt/393)**, trong khi iPad Pro ngang rải icon **cách nhau 283pt** trên một dải gần trống; cộng 20pt `kPageNavBarGestureGap` mồ côi nằm *giữa* hai hàng chrome vì `PageNavBar` không còn chạm mép máy. **Lỗi thật:** `bottomChrome = pageNav == null ? null : …` nên quick-bar biến mất cùng thanh trượt khi `!_prefsReady || _pageCount == 0` — kể cả **Draw, thứ 0035 quyết định 6 cố ý không đưa vào `⋯`**, nên Draw mất lối vào duy nhất; và `_switchToPiece` clear `_prefsReady`, nên mỗi lần đổi bài Setlist là 162pt chrome sụp rồi mọc lại. Bốn chốt: (1) luật **"tay đang trên nhạc cụ"** — chỉ Bookmarks, Draw, Metronome (thứ tự theo nhóm của `⋯`), Layout và View về lại `⋯`; (2) hình dạng do **`QuickBarFit`** đo, cùng idiom `LayoutFit` của 0041 — gộp vào hàng thanh trượt khi chrome xếp chồng vượt 25% chiều cao *và* hàng gộp còn để thanh trượt ≥160pt, nên điện thoại ngang gộp (còn 420pt) còn điện thoại dọc và split-screen thì không; (3) Metronome **luôn hiện**, trạng thái nói bằng tint; (4) nhãn chữ **chỉ khi `quickBarLabelsFit` đo thấy vừa** — đo đúng những chữ sắp vẽ ở đúng text scale, nên bản dịch dài hay text 200% tự rơi về icon-only. Quyết định 4 là cách thoát khỏi vòng tranh luận nhãn: Revision 1 bỏ nhãn vì chính sách, Revision 2 chọn icon-only vì chính sách, mà lý lẽ i18n đó vốn đã bị `DrawToolbar` đang ship phản bác (sáu nhãn tiếng Anh trong một hàng cố định). Build: file mới `lib/ui/quick_bar_fit.dart`; `page_nav_bar.dart` thêm `trailing` + `bottomGestureGap` (bar nào chạm đáy thì giữ inset và gesture gap); quick-bar tách vòng đời khỏi `PageNavBar`, prefs chưa về thì `enabled: false` chứ không biến mất; `kViewGroupIcon` và `_openViewMenu` bị xoá. Kết quả đo lại: điện thoại ngang **Score từ 180pt lên 240pt (61% chiều cao)**. Một hằng số suýt sai: `kPageNavBarControlsWidth` tính tay ra 200pt, widget test đo bar thật ra **283pt** với số trang bốn chữ số → đặt 288; không có phép đo đó thì ngưỡng gộp sẽ sai theo hướng tệ nhất, tức gộp trên đúng những màn hình không đủ chỗ. 322 tests xanh (301 trước), analyze sạch. Vẫn chờ G4 trên thiết bị thật — điều test không chứng minh được là nhãn có thật sự hiện trên máy, vì phông của môi trường test vẽ mỗi ký tự rộng đúng một em. |
| 2026-07-29 | **Metronome sheet tràn ở landscape (musician báo cáo) — và hai sheet nữa vỡ y hệt mà chưa ai báo.** Xoay ngang, bấm Metronome: `A RenderFlex overflowed by 144 pixels on the bottom`, và hậu quả nặng hơn cái sọc vàng đen là nút **Start** ở cuối `Column` **không thể chạm tới** — mở được metronome mà không bật được. Root cause: `_MetronomeSheet` là `SafeArea > Padding > Column(min)`, không có gì cuộn được; phone nằm ngang chỉ cho bottom sheet ~336pt trong khi nội dung cần ~480. Spec 0035 đã lo nửa đầu của tính chất này ("sheet kết thúc ở chỗ nội dung kết thúc") nhưng chỉ cho những sheet nó viết lại — Layout/Page turn có `Flexible` + `ListView` nên vẫn cuộn được, ba sheet phẳng hơn thì không. Viết test mở **từng** sheet của PdfMode ở `852×393` và bắt `takeException()`, thì lộ thêm **Display tràn 31pt** và **Page scale tràn 63pt** — cùng một hình dạng sai, chỉ khác là chưa ai bấm tới. Sửa một chỗ cho cả ba: `lib/ui/sheet_body.dart` (`SheetBody` = `ConstrainedBox(maxHeight: 90%)` + `SingleChildScrollView`); hai tính chất ở chung một widget có chủ ý — cap mà không cuộn thì che nội dung, cuộn mà không cap thì sheet trùm lên Score. Ghi vào Spec 0030 phần "Cập nhật sau G4". |
| 2026-07-29 | **Dải chấm beat ở lại trên Score khi metronome chạy** (Orchestrator, cùng lượt với bug trên; tái mở một phần Spec 0030). Lý do người dùng nêu: chrome tự ẩn sau vài giây nên đang chơi thì không còn thấy nhịp. Lý lẽ mạnh hơn tìm được khi đọc lại Spec 0030: **"Mute (visual only)" trở thành vô nghĩa ngay khi chrome ẩn** — tắt tiếng rồi, hình cũng mất, metronome không còn output nào; UX notes gốc đã đòi "beat indicator visible without covering the Score center" mà chưa nói sau khi chrome đi thì nó ở đâu. Bốn khuyến nghị, Orchestrator chấp nhận cả bốn: (1) **giữa mép trên** — trên giá nhạc là dải gần tầm mắt nhất và là dải duy nhất của trang không có nốt (lề trên); không dùng góc dưới phải vì đó vừa là vùng tap PageTurn chính khi PageNavBar đã đi (0034) vừa là chỗ `PagePositionPill` (0036) đứng; (2) **switch trong metronome sheet, default bật** — dải chỉ tồn tại khi metronome đang chạy, mà chạy là hành động có ý; installs cũ (JSON chưa có key) cũng bật, giống 0034 làm với PerformanceMode; (3) **đúng dãy chấm theo số phách**, dùng lại cùng widget với sheet (`BeatDots` tách khỏi `metronome_sheet.dart`) để sheet và Score không bao giờ vẽ khác nhau về số phách hay chấm nào là accent, `scale: 1.6` vì đọc ở khoảng cách giá nhạc; (4) **sửa luôn chuyện rebuild mỗi phách** — `PdfModeScreen` đang `setState(() {})` trong listener của engine, tức **cả màn hình kể cả PDF viewer rebuild hơn 3 lần/giây ở 200 BPM ngay trong lúc đang chơi**; có từ trước bản này (do tint icon metronome) nhưng thêm dải nhấp nháy sẽ nặng thêm, và đúng loại nguyên nhân làm tap-to-turn cảm giác chậm mà 0033 vừa đi chữa. Nay listener chỉ `setState` khi `isRunning` **đổi**; nhịp đi qua `ListenableBuilder` bọc riêng `BeatStrip` và `ScoreMenuQuickBar`. `BeatStrip` (`lib/ui/beat_strip.dart`) **luôn** nằm trong `Stack` — child xuất hiện/biến mất là child có thể làm viewer bên cạnh rebuild — và tự quyết định hiện: `!chromeShown && isRunning && showBeatsOnScore`, hai điều kiện sau đọc **bên trong** `ListenableBuilder` nên tắt switch có hiệu lực ngay không cần màn hình rebuild. Giống `PagePositionPill`: `IgnorePointer` nên tap xuyên qua, không reveal, không đụng countdown auto-hide. 335 tests xanh, analyze sạch. **Chưa demo trên thiết bị thật** — dải beat và switch là hành vi mới, chưa qua G4 riêng. |
| 2026-07-29 | **G4 dải chấm beat: pass trên SM X210 (Android 16), kèm một phát hiện timing còn mở.** Dải hiện đúng giữa mép trên sau khi chrome ẩn, switch có tác dụng, tap PageTurn xuyên qua — Orchestrator pass. Nhưng musician nghe ra **tiếng click đến sau dải chấm một chút**, và chẩn đoán xong thì đây không phải chuyện tinh chỉnh mà là **hai đồng hồ không có gì nối với nhau**: chấm hình theo `DateTime` của Dart, tiếng theo sample clock của thiết bị. `start()` đặt `_anchorTime = DateTime.now()` rồi `_emitVisualBeat()` **trước** khi `await _startLoopAudio()` (dựng PCM → `loadMem` → `play()`), nên mọi thứ sau mốc đó là **lệch pha vĩnh viễn** — sau đó hai đồng hồ chạy tự do cùng tốc độ nên sai số không bao giờ tự sửa. Ba số hạng đo/đọc được: (1) mốc hình đặt trước khi tiếng bắt đầu — `synthesizeMetronomeLoopWav` đo trên Mac 1–10 ms tuỳ nhịp (nặng nhất 40 bpm 12 phách: 9,7 ms, buffer 1,5 MiB), cộng `loadMem` và `play()`, và số hạng này **không phải hằng số** vì nó lớn theo kích thước buffer nên tempo chậm lệch nhiều hơn tempo nhanh; (2) độ trễ output của thiết bị, `bufferSize: 512` ≈ 11,6 ms ở 44,1 kHz cộng đường ra nền tảng, không đảo thứ tự nào xoá được; (3) trôi chậm giữa hai đồng hồ vì sample rate thật không đúng 44 100 Hz danh nghĩa, nên dù mốc hoàn hảo thì buổi tập dài vẫn tách ra. **Bản sửa nên làm** (chưa làm, chờ Orchestrator mở slice): cho **play head của audio làm đồng hồ** khi có tiếng — `SoLoud.getPosition(handle)` quay vòng trong loop nên `beat = position / interval` xoá số hạng 1 và 3 *bằng cấu trúc* chứ không bằng hiệu chỉnh, giữ wall-clock riêng cho **Mute (visual only)** vì chỗ đó không có play head nào để theo; kèm cho loop chế độ **Equal** dài đủ một khuông thay vì một phách (`click_wav.dart` đang đặt `beats = 1` khi tắt accent) để `position → beatInBar` là một phép ánh xạ cho mọi nhịp. Instrumentation dùng để đo đã xoá khỏi engine; 335 tests xanh, analyze sạch. Ghi vào Spec 0030 mục "Cập nhật sau G4" phần 3. |
| 2026-07-29 | **Sửa timing metronome ngay trong lượt G4 (Orchestrator chọn sửa luôn, tái mở 0030 lần hai): bỏ đồng hồ thứ hai đi thay vì hiệu chỉnh nó.** Chấm và tiếng không thể lệch nhau nếu đọc **cùng một** đồng hồ, và đồng hồ duy nhất có thẩm quyền là cái đang phát ra tiếng — play head của loop. File mới `lib/metronome/beat_clock.dart`: một hàm thuần `beatInBarFromLoopPosition(position, beatInterval, beatsPerBar)` nhận **vị trí** chứ không nhận số đếm phách, nên **không có trạng thái** — đọc muộn (rớt frame, isolate bận) thì ra phách *đang* kêu, chứ không đẩy lệch số đếm như bộ `Timer` cũ; và lấy **modulo theo khuông**. Modulo không phải phòng xa mà vì đọc source SoLoud thấy hành vi không nhất quán để mà tin: `soloud.cpp:2159` cộng `buffertime` vào `mStreamPosition` mỗi lần mix, còn `WavInstance::rewind()` đặt nó về `0` mỗi lần loop — plugin không hứa gì, nên phép ánh xạ không được phụ thuộc vào đó. `start()` đảo thứ tự: `await _startLoopAudio()` **trước** rồi mới lập đồng hồ, xoá đúng số hạng lớn nhất (mốc hình đặt trước khi tiếng bắt đầu). **Mute (visual only)** là trường hợp duy nhất không có play head nên cũng là trường hợp duy nhất còn đồng hồ riêng: `Stopwatch` **monotonic** (không phải `DateTime`, thứ có thể nhảy khi hệ thống chỉnh giờ) đưa vào **cùng** hàm ánh xạ, với `_silentOffset` mang vị trí cuối của play head sang nên mute giữa khuông không đếm lại từ đầu. Loop chế độ **Equal** giờ dài đủ một khuông thay vì một phách — nghe y hệt, nhưng làm play head là vị trí *trong khuông* với mọi nhịp, nên chỉ còn **một** phép ánh xạ phải bảo trì. `metronomeAudioBeatInterval` cho đồng hồ chia theo độ dài phách *thật của buffer* (đã làm tròn về sample nguyên) thay vì `MetronomePrefs.beatInterval` danh nghĩa: lệch dưới 10 µs mỗi phách, nhưng play head cộng dồn sẽ biến nó thành trôi thấy được. Poll 16 ms nhưng chỉ `notifyListeners()` khi **số phách đổi**, nên nhịp thông báo vẫn đúng bằng số phách/giây — giữ nguyên quyết định 4 của lần tái mở trước. **Cố ý không** đặt hằng số bù độ trễ loa: `getPosition` báo vị trí mixer đã *sản xuất* nên chấm vẫn sớm hơn tiếng đúng bằng khoảng đệm ra loa (`bufferSize: 512` ≈ 11,6 ms cộng phần cứng), nhưng nó **nhỏ và là hằng số** thay vì lớn dần theo tempo chậm / nhịp nhiều; không đo được từ software thì con số bù là phỏng đoán, và bù quá tay làm chấm *trễ* hơn tiếng — hướng sai mà tai người nhạy hơn. `test/beat_clock_test.dart` 13 test, đáng chú ý nhất là ca **play head cộng dồn 50 khuông vẫn đúng** và ca khoá độ dài khuông của đồng hồ **khớp buffer sinh ra thật** ở 40/60/100/120/218 BPM. 349 tests xanh (335 trước), analyze sạch. Chờ demo lại trên thiết bị. |
| 2026-07-29 | **G4 pass cho cả 0030 (bản sửa timing) và 0043, trên SM X210 — Spec 0043 chuyển `done`.** Bản sửa metronome xác nhận bằng tai: chấm khớp tiếng, nên độ trễ output còn lại nằm dưới ngưỡng nghe ra được trên máy này — đúng như kỳ vọng khi nó chỉ còn là một hằng số nhỏ thay vì lớn dần theo tempo chậm / nhịp nhiều. Demo cũng trả lời câu hỏi mà chỉ thiết bị mới trả lời được: play head **có** nhích với source `loadMem`, thứ mà nếu sai thì dải chấm sẽ đứng im — một hồi quy tệ hơn cả lỗi ban đầu. Với 0043, ghi rõ hai ca mà tablet dọc *không* chứng minh được nên vẫn dựa vào phép đo trong widget test chứ không phải mắt người: **điện thoại nằm ngang gộp quick-bar vào hàng thanh trượt** và **nhãn chữ tự rơi về icon-only** — phông môi trường test vẽ mỗi ký tự rộng đúng một em, nên `quickBarLabelsFit` được khoá bằng số đo, không bằng ảnh chụp. Roadmap Phase E mục E1 đóng lại; kế tiếp theo thứ tự đã chốt là **0044** (design token), vì E3–E7 đều hiển thị đẹp hơn một khi token tồn tại. 349 tests xanh, analyze sạch, commit chung một lần theo đúng lựa chọn 'chờ 0043 pass rồi commit'. |
| 2026-07-29 | Spec **0044** (design token & component theme, hạng mục E2 của Phase E) đã drafted `proposed`, dựa trên một đợt kiểm kê `lib/ui/` chạy bằng subagent rẻ (ADR 0016) nên số liệu là đếm được chứ không phải cảm nhận: toàn bộ phần cấu hình hình thức của app là **bảy dòng** trong `AppAppearance.themeData` (`ColorScheme.fromSeed` + `useMaterial3`), không một component theme nào; **sáu** giá trị `BorderRadius.circular` (4/6/8/10/12/14); **bốn** `elevation` (1/2/4/8); mười ba trị số khoảng cách rời rạc; **19** lối vào `showModalBottomSheet` mà **không cái nào** truyền `shape`/`backgroundColor`, tiêu đề chia 7 `titleLarge` / 12 `titleMedium`, 8 có Done / 11 không, 3 dùng `SheetBody` / 16 không, và **0** sheet có drag handle. Kiểm kê cũng làm rõ chuyện màu brand rộng hơn roadmap ghi: `0xFF0D9488` nằm ở **ba** file (`draw_toolbar`, `stamp_painter`, `jump_link`), cộng `display_sheet.dart:8` viết tay lại `0xFF0D8B86` thay vì đọc hằng số — và ba site kia **không phải bề mặt brand**, nên khuyến nghị không trỏ chúng vào hằng số brand mà tách theo nghĩa: toolbar vẽ đọc `colorScheme` (hôm nay nó teal cứng, tức phớt lờ accent musician chọn ở 0026), stamp/JumpLink đổi tên thành mực nội dung. Tám câu hỏi G3; câu tải trọng nhất là **số 8 — lấy gì giữ cho tầng token không mục ruỗng**, vì một file hằng số không ai bắt buộc dùng sẽ có `SizedBox(height: 10)` mới ngay ở slice sau, nên khuyến nghị một test guard tầng source theo đúng idiom test brand của 0042. Một ranh giới được viết thẳng vào Spec để khỏi lấn: token là **khẩu vị**, còn các hằng số đo được của 0043/0034/0041 (`kQuickBarGestureGap`, `kPageNavBarControlsWidth`, `kMinScrubberWidth`…) là **phép đo** — chúng không bị token hấp thụ kể cả khi trị số trùng một bậc, vì `kQuickBarGestureGap` bằng 12 là ngẫu nhiên và một lần chỉnh thang khoảng cách sẽ âm thầm đổi khoảng đệm chống-cử-chỉ-OS. Header sheet (Done, style tiêu đề, hàng icon+mô tả phụ) cố ý để lại cho 0047; Spec này chỉ chốt những gì `ThemeData` diễn đạt được. Đang chờ G3. |
| 2026-07-29 | **Vòng grilling G3 cho Spec 0044 sửa lại chính đợt kiểm kê sinh ra nó — và hẹp phạm vi slice.** Bài học chung trước: số của một subagent rẻ (ADR 0016) là *giả thuyết đọc được*, không phải phép đo, và ba dòng của nó sai theo cùng một hướng — **phóng đại việc cần làm**. Cụ thể: (1) "0 sheet có drag handle" thực ra là **17 trong 19 đã có**, committed từ trước lần rename StandScore→StageScore, nên câu hỏi tải trọng số hai của Spec chỉ còn là housekeeping (dồn cờ vào theme, xoá 17 flag dư, hai sheet còn thiếu — setlist jump và add-scores — nhận handle); (2) "ba nơi override mật độ chip" thực ra là **một** (`library_screen.dart:499`), còn `draw_toolbar.dart:483` là `SegmentedButton`; (3) `SheetBody` có **5** người dùng, không phải 3; và danh sách test cần sửa là **7** file, không phải 14. Chỗ chặn thật thì không nằm trong bất kỳ số nào: **`ChipThemeData` không có field `visualDensity` lẫn `materialTapTargetSize`** (26 field, đã đọc `chip_theme.dart` trong SDK), nên khuyến nghị "theme chốt mật độ chip" không thực thi được — theme chỉ chốt `shape`/`padding`, còn việc một-chiều-cao là quyết định bố cục (chênh thật trên Library là `InputChip` đứng cạnh `_Chip`, một `Container` tự viết) và chuyển sang 0046/0049. Hai thứ bị **cắt khỏi phạm vi** vì cùng một lý do "không có gì đang phân kỳ để hội tụ": `PopupMenuThemeData` (8 site, không site nào style cục bộ; mặc định M3 là bán kính 4, nên ghi đè thành bậc `md` *sinh ra* kiểu thứ bảy) và thang đổ bóng (4 site trong toàn `lib/`, 3 là phép đo của 0043/0034, cái thứ tư mang nghĩa trạng thái 4-khi-kéo/1-khi-không). Câu 1 phân loại lại: `stamp_painter.dart:42` **không phải mực nội dung** mà là **viền chọn** (`if (stamp.id == selectedId)`, `inflate(3)`, stroke 2 — mực thật là `stamp.color`), nên nó nhận màu từ `colorScheme` như draw toolbar chứ không đổi tên thành `kDefaultStampInk`; ngược lại `display_sheet.dart:8` là **preset màu viền trang** đứng cạnh đen và amber, tức đúng loại nội dung mà Spec tự loại ra — cho nó đọc `brandTealValue` là phá ranh giới của chính mình, nên giữ giá trị và đặt tên cạnh `DisplayPrefs.defaultBorderColorValue`. Một tiêu chí chấp nhận cũng sai và đã sửa: "teal brand chỉ định nghĩa một lần" không đạt được vì **ba resource Android** (`colors.xml`, hai `styles.xml`) không đọc được Dart — câu đúng là một lần trong Dart cộng ba bản sao có ghi chú. Cuối cùng, hình dạng diff được viết vào Spec để Orchestrator biết mình chấp nhận cái gì: phần lệch thang chỉ **11 site**, nhưng phần đúng-thang là **~180 lần thay** không sửa lỗi nào — nó tồn tại chỉ để test guard xanh được, và guard đó là một **ratchet, không phải bằng chứng** (không chặn `Positioned(left: 10)`, `Container(margin:)`, biểu thức tính; repo không có CI). Spec vẫn `proposed`, chín câu (thêm 4b), chờ G3. |
| 2026-07-29 | **Spec 0044 accepted (G3) và built cùng ngày.** Tầng token mới `lib/theme/app_tokens.dart` — `AppSpacing` (4/8/12/16/24), `AppRadius` (4/8/12), cộng `kFabScrollClearance` đặt riêng như một **phép đo** (56 + `AppSpacing.lg` × 2) chứ không phải một bậc. `AppAppearance.themeData` nhận `BottomSheetThemeData` (`showDragHandle: true` + `clipBehavior: Clip.antiAlias`) và `ChipThemeData`, cả hai dựng từ `ColorScheme` của brightness đang chạy. Bốn màu literal xử lý theo ba nghĩa khác nhau như G3 chốt: `draw_toolbar` (nền tint + icon Stamp đang chờ) đọc `scheme.primary`; `stamp_painter` nhận `selectionColor` mới truyền từ `page_annotation_overlay` (viền chọn là chrome — mực thật là `stamp.color` — và null ở export vì export không có selection); `jump_link` đổi literal sang teal duy nhất; `display_sheet` không còn tự viết màu mà đọc `DisplayPrefs.borderColorPresetValues`, nơi bốn preset giữ **đúng giá trị cũ** vì chúng là nội dung musician chọn. `showDragHandle` rút khỏi **14 file** (17 lối gọi) vào một dòng theme; hai sheet trước đây thiếu handle — setlist jump và add-scores — nhận được nó miễn phí. **Điều đáng học nhất của slice: guard đỏ ngay ở chính slice sinh ra nó.** `test/design_token_guard_test.dart` tìm ra ba site mà cả kiểm kê subagent lẫn vòng grilling G3 đều bỏ sót, vì cả hai dùng grep một dòng còn `EdgeInsets` thật thì trải nhiều dòng — `page_position_pill` (`vertical: 6`), `draw_toolbar` `SizedBox(width: 196)` và `SizedBox(22×22)`. Hai cái sau là phép đo nên được đặt tên (`_colorPaletteWidth`, `_widthDotExtent`), và việc đặt tên lộ thêm một bản sao ngầm: `clamp(12.0, size.width - 220)` chính là `196 + 12×2` viết lại bằng tay, nay tính từ hằng số. Luật snap khi hai bậc cách đều: **snap xuống**, để không hàng nào nở ra (10→8, 20→16, 6→4); bo góc theo đúng bảng Spec (6→8, 10→12, 14→12). Ba lệch so với Spec đã ghi vào Build notes để hỏi ở G4: (1) `ChipThemeData` hoá ra **trùng đúng mặc định M3** (`shape` 8, `padding` 8) nên được viết dưới dạng "ghim vào thang" chứ không phải ghi đè — xoá được mà không mất gì, cùng lý do đã cắt `PopupMenuThemeData`; (2) `clipBehavior` thêm ngoài Spec, biện minh bằng chính mục Vấn đề đã đếm nó; (3) JumpLink **mới** sau bản này lệch 9/255 xanh so với link cũ, link cũ không đổi. 361 tests xanh (349 + 12 mới), analyze sạch, không một file test cũ nào phải sửa — kể cả `pdf_mode_chrome_layout_test.dart` và `quick_bar_fit_test.dart`, nên các hằng số đo được của 0043/0034/0041 chứng minh được là không bị token hấp thụ. Guard đã thử đỏ có chủ ý rồi revert. Chờ G4 trên thiết bị thật: sáu sheet liền nhau ở hai theme, accent tím trong draw mode, Stamp/JumpLink cũ, text scale 200%. |
| 2026-07-29 | **G4 pass cho 0044 (Spec `done`), và Phase E dừng ở đây: E3–E7 chuyển `hold`.** Orchestrator chạy thử trên máy: "thấy tốt rồi", và "UI như hiện tại tôi thấy OK rồi". Ba lệch trong Build notes được chấp nhận như đang có (`ChipThemeData` giữ dạng ghim-vào-thang dù trùng mặc định M3, `clipBehavior` giữ, JumpLink mới lệch 9/255 xanh so với link cũ). Quyết định dừng đáng ghi lý lẽ vì nó **đảo hướng của cả phase**: E1 và E2 đã đóng đúng hai khoảng cách *có bằng chứng* với ScorePDF (IA của PdfMode, ngôn ngữ thị giác dùng chung), còn E3–E7 còn lại đều là làm cho **giống ScorePDF hơn** — mà chính đợt audit sinh ra Phase E đã kết luận ScorePDF không mạnh hơn về năng lực, nó chỉ khoá Label/Sort/Search/Metronome sau gói Pro. Nên tiếp tục E là tiếp tục đuổi theo một app mà mình đã bằng. Hướng mới Orchestrator chọn: **những thứ nghệ sỹ / teacher / student cần mà ScorePDF không có**, đủ nhỏ để vào ngay v1. Ba ràng buộc lọc danh sách ứng viên, và chúng lọc mạnh hơn tưởng: (1) **ADR 0008** — bất cứ thứ gì cần MusicXML, đi theo nốt, hay phát nhạc theo bản nhạc đều là H3+, tức out; (2) **lời hứa v1** offline và không thu thập gì (R2/R11/R22) — không cloud, không account, không analytics; (3) v1 đang ở cửa store, blocker còn lại đều là việc người (logo R6, screenshot R12, monetization R7, trademark R5), nên mỗi tính năng thêm vào là thêm một vòng screenshot. Ứng viên đưa ra để Orchestrator chọn, kèm kiểm kê code cho từng cái: **practice trainer cho metronome** (prefs hôm nay là app-wide, không có tempo theo Score, không count-in, không tăng tempo dần); **thẻ thông tin theo Score + tổng thời lượng Setlist** (`Score` chỉ có title/createdAt/lastOpenedAt/pageCount, không có ghi chú, tone, tempo); **lesson pack** một file cho một Score/Setlist kèm annotation, bookmark, JumpLink (dùng lại bộ máy ZIP của `LibraryBackup` và share-in của 0029, nhưng là một **format mới** nên cần ADR); **annotation layer** teacher/student (đụng format annotation đã lưu, cần migration); **practice log trên máy** (tension đáng nói: web repo vừa **bỏ** lưu `practice_sessions` vì lý do privacy ở R24, nên một log trên thiết bị phải được kể lại nhất quán ở cả hai repo theo ADR 0012); và ba cái cần một gate riêng — **auto-scroll theo clock metronome** (sát ranh Transport), **ghi âm để nghe lại** (đụng sở hữu audio + form privacy, là hard stop trong `AGENTS.md`), **auto-crop lề cho PDF chụp từ sách** (chưa có bằng chứng ScorePDF thiếu). Chưa Spec cái nào. |
| 2026-07-29 | **Monetization: không quảng cáo, và bản Pro chưa được dựng ở v1 — vì Orchestrator bác đúng chỗ yếu của danh sách tính năng.** Câu bác bỏ ngắn nhưng là một luật chọn tính năng: "Practice trainer, Lesson pack… không phải ai cũng có nhu cầu, và không hấp dẫn lắm. Nếu bắt user phải mua bản Pro có khi họ chọn dùng bản Free." Hệ quả: một tính năng *ngách* không thể là thứ đứng sau paywall, vì paywall chỉ có giá trị khi thứ nó khoá là thứ **mọi** người muốn — mà nếu mọi người muốn thì khoá nó lại làm app tệ đi cho phần lớn người dùng. Đó cũng chính là hình dạng ScorePDF đang có (Label/Sort/Search/Metronome sau gói Pro) và là điểm mà đợt audit sinh ra Phase E kết luận StageScore hơn được. **Quảng cáo: khuyến nghị không, và lý do không phải khẩu vị mà là ba thứ đã ký.** (1) R22 và `PrivacyInfo.xcprivacy` khai app **không thu thập gì**; một SDK quảng cáo mang theo định danh thiết bị, prompt ATT trên iOS và một mục mới trong Data safety của Play — tức phải sửa cả tuyên bố ở repo web (`/privacy`) theo ADR 0012, đúng loại thay đổi mà `AGENTS.md` bắt hỏi người. (2) App này ẩn hết chrome để nhạc công đứng trên sân khấu đọc bản nhạc; một banner hay interstitial xuất hiện giữa lúc đang chơi là lỗi nghiêm trọng nhất mà sản phẩm này có thể mắc, và không có vị trí nào an toàn — Library thì ít người ở lâu, PdfMode thì tuyệt đối không. (3) Doanh thu quảng cáo ở lượng người dùng của một app mới là gần bằng không, nên nó đổi một lời hứa privacy lấy vài đô. **Subscription: phức tạp vừa phải về kỹ thuật nhưng sai thời điểm về kiến trúc.** Riêng in-app purchase một lần thì đơn giản (một `productId`, một cờ trên máy, không server) — nhưng subscription thật cần chỗ để trả lời "người này còn hạn không" xuyên thiết bị, tức cần account, tức phá lời hứa offline-only của v1 và dựng một tầng mà repo web **đã có sẵn** đường thanh toán. Nên khuyến nghị: v1 **miễn phí toàn bộ**, và nếu cần thu tiền sớm thì một **unlock một lần** (không hạn, không account, không server, hợp offline) chứ không phải subscription; subscription chờ sau khi ADR 0012 gộp repo, để một tài khoản Backing & Score dùng chung cho web và app thay vì hai hệ thanh toán không biết nhau. Chưa có ADR nào cho việc này — khi Orchestrator chốt thì nó là quyết định khó đảo (giá, tên gói, ranh giới free/paid xuất hiện trong store listing) nên phải là ADR ở G2, không phải một dòng log. |
| 2026-07-29 | **Bug thật do musician báo: "nhiều Score, mỗi Score cả cuốn sách 100 trang, bấm sao lưu là app đơ một lúc" — và chẩn đoán cho ra bốn nguyên nhân, không cái nào là 'library quá lớn'.** Toàn bộ `createBackup` là `ZipFileEncoder()..create(path)` rồi `addDirectory(root)`, và đọc source `archive 4.0.9` thì: (1) **mọi file bị deflate kể cả PDF**, vì không chỗ nào truyền `level` và `ZipEncoder.add` mặc định `CompressionType.deflate` (`zip_encoder.dart:213`) — mà PDF là dữ liệu **đã nén sẵn**, nên nó đốt gần hết CPU của tác vụ để đổi lấy gần như không byte nào; (2) việc nén chạy **trên isolate UI** — `await` ở đó chỉ chờ I/O, còn nén là CPU đồng bộ; (3) `addDirectory` mở đầu bằng `dir.listSync(recursive: true)` (`zip_file_encoder.dart:145`), quét toàn cây đồng bộ; (4) người dùng đang xem một **spinner vô định** mà chính spinner đó cũng đứng vì UI bị chặn, không phần trăm, không nút huỷ. Điều này cho G4 một dấu hiệu chẩn đoán sắc hơn "cảm giác nhanh hơn": **vòng xoay có ngừng xoay hay không**. `restoreBackup` có **đúng cùng lỗi** ở chiều ngược lại, và đó là đường **phá huỷ** nên đơ ở đó đáng lo hơn. Một khuyến nghị ở lượt trước **sai và đã sửa**: cache thumbnail không nằm trong backup để mà loại bỏ — nó ở `<cache>/score-thumbs` (`library_screen.dart:179`), ngoài library root; bài học cũ lặp lại, đoán về layout trên đĩa mà không đọc chỗ khởi tạo thì sai. Ngược lại, đọc source cũng cho một món quà: `archive` **đã có** `onProgress` và một `filter` trả `include/skip/cancel` (`zip_file_progress.dart`), nên tiến trình, huỷ và cả **cái móc cho sao lưu chọn lọc** không phải viết từ đầu. **Chốt tách hai slice, Orchestrator đồng ý.** Slice này — Spec **0050** `proposed`, tier **M** — chỉ đổi *cách* sao lưu: mức nén **theo loại file** (`store` cho `.pdf`/`.png`, `deflate` cho `.json`, vì JSON là text nên `store` tất cả sẽ làm file phình thật), toàn bộ job ra khỏi isolate UI, tiến trình **theo byte** không theo số file (một PDF 100 trang nặng hơn 200 JSON cộng lại, nên đếm file sẽ nhảy tới 90% rồi đứng im — đúng cảm giác đơ đang phải chữa), huỷ được, ghi `*.zip.part` rồi `rename` để không bao giờ để lại một ZIP cụt trông như bản backup, và `formatVersion` **không** tăng vì backup cũ phải còn phục hồi được. **Việc Orchestrator hỏi — cho chọn sao lưu những gì — cố ý để slice sau và nó là tier L, không phải vì UI khó mà vì `restoreBackup` hôm nay *thay thế* cả library** (hộp thoại nói thẳng "This replaces all Scores… cannot be undone"): phục hồi một bản chọn lọc bằng ngữ nghĩa đó sẽ **xoá những Score không được chọn**. Nên slice sau phải chốt trước bằng ADR ở G2 + Security Review: phục hồi là **trộn** hay bản chọn lọc chỉ để **gửi đi**; marker ghi rõ đây là bản một phần và gồm những gì; và `formatVersion` lên **2** để bản app cũ *từ chối* file mới thay vì phá library — đúng việc mà kiểm tra version ở `library_backup.dart:113` tồn tại để làm. Tách vậy thì slice hiệu năng ship được một mình và slice chọn lọc có mặt phẳng sạch để bump format. |
| 2026-07-29 | Spec **0050** accepted (G3) với cả tám khuyến nghị như bản nháp: mức nén theo phần mở rộng (`store` cho `.pdf`/`.png`, `deflate` cho `.json` và phần còn lại); cả job ra `Isolate.run`; tiến trình theo byte kèm tên Score; huỷ ghi `*.zip.part` rồi `rename`, không để lại artefact; restore cùng cách chữa, Cancel vô hiệu sau khi đã `rename` library; backup cũ (deflate) vẫn phục hồi được, `formatVersion` giữ 1; test khoá cấu trúc không khoá thời gian, "vòng xoay vẫn quay" thuộc G4; không dọn thêm marker/`formatId`/ngữ nghĩa thay-thế — chọn lọc là slice sau tier L. Building. |
| 2026-07-29 | Spec **0050** built. `LibraryBackup` tự đi cây async, `store` (`CompressionType.none`) cho `.pdf`/`.png`, deflate cho phần còn lại; cả job trong `Isolate.run` với tiến trình theo byte qua `SendPort` và huỷ qua `LibraryBackupCancelToken`; ghi `*.zip.part` rồi `rename`; restore cùng pattern, Cancel vô hiệu sau bước đổi tên library. UI: hộp thoại phần trăm + nhãn Score + Cancel thay spinner vô định. Test 0027 xanh không sửa; `library_backup_progress_test.dart` khoá mức nén, tiến trình đơn điệu, huỷ sạch, ZIP deflate cũ restore được. 365 tests xanh (361 + 4), analyze sạch. Chờ G4 trên thiết bị thật. |
| 2026-07-29 | **G4 pass cho Spec 0050** (Spec `done`). Backup không còn đơ UI trên thư viện thật; tiến trình + Cancel + confirm trước khi zip đều ổn. P2.11 ghi thêm 0050 trên parity table. Phần chọn lọc backup vẫn để slice sau (tier L). |
| 2026-07-29 | **Orchestrator chốt hướng sau Phase E: thêm Discover — tìm và chơi nhạc do Backing & Score sản xuất, free cộng một tầng subscriber — trong khi StageScore vẫn là app offline độc lập.** Câu hỏi mở đầu là dùng PDF thay MusicXML có khả thi không; trả lời là ranh giới thật không phải PDF-vs-MusicXML mà là *tính năng có cần biết nốt nào* hay *chỉ cần biết chỗ nào trên trang, ở giây thứ mấy* — WaitMode và MidiLane cần vế đầu, còn toàn bộ H5 (BackingTrack + SyncMap) chỉ cần vế sau. Đợt kiểm repo web sau đó cho thấy vế sau **đã được xây xong ở bên kia**: `MeasureBox` (pageIndex / measureNumber / systemIndex / toạ độ tỉ lệ 0..1 / beatSplits) cộng `TimemapEntry` (timeMs ↔ measure, beatTimestamps, tempoAtBeat, startsAtBeat cho nhịp lấy đà), công cụ soạn `useTappingSystem` + `SyncWorkspaceClient`, phần nặng đẩy lên R2 qua `heavyDataR2Key`, `/api/mobile/v1/*` chạy edge, và ranh giới free/trả tiền suy ra từ `products` row mở bằng entitlement `global_catalog_vip`. **Hai điều đã nói sai trong chat và sửa lại ở ADR:** bảng `arrangements` trong `catalog.ts` không phải bảng đang chạy (`admin-arrangements.ts` làm `import { projects as arrangements }`, và `drive.ts` ghi "gộp từ bảng arrangements cũ"), và `/api/mobile/v1/storefront` phục vụ `projects` chứ chưa có endpoint nào cho luồng Discover — pattern có sẵn, endpoint thì chưa. Chỗ căng thật là subscription cần mạng còn sân khấu thì không có sóng; lối thoát là **kiểm quyền lúc tải, không kiểm lúc chơi**, cộng search chạy trên máy từ manifest đã cache nên không truy vấn nào rời thiết bị. ADR **0017** drafted `proposed` (G2, tier L — cần Security Review): ba chỗ duy nhất được chạm mạng, tải về là trở thành Score bình thường thừa hưởng cả Setlist/Bookmark/PageOrder, PageOrder là ràng buộc của playhead (dùng lại `measureMap` của web, đừng viết lại), backup lưu tham chiếu chứ không nuốt asset có bản quyền nên bump `formatVersion` chung một lần với slice backup chọn lọc. Năm câu hỏi G2; tải trọng nhất là **subscription có tài khoản hay ẩn danh qua receipt** — ẩn danh giữ được lời hứa offline và gần như không phải sửa tuyên bố privacy, đổi lại mất đồng bộ đa thiết bị với web. Điểm đáng ghi về monetization: hướng này tháo được thế bí của log phía trên, vì nó bán **nội dung** chứ không khoá **tính năng** — mọi thứ PdfMode làm hôm nay vẫn miễn phí, nên không lặp lại hình dạng ScorePDF. Không đụng code; Discover dứt khoát không thuộc v1. |
| 2026-07-30 | **Vòng grilling G2 cho ADR 0017 tìm ra thứ cả bản nháp lẫn đợt kiểm repo trước đó bỏ sót: Discover đã có một cài đặt, đã lên TestFlight kèm IAP, trong chính repo web.** `apps/mobile` là app Ionic + Capacitor tên "Backing Score", `appId: com.backingscore.app`, có project iOS và Android thật, dùng `@revenuecat/purchases-capacitor`; commit `2026-05-13` ghi "Finalize iOS TestFlight deployment and IAP integration", commit cuối chạm vào nó là `2026-05-28`. PRD của nó (`docs/v11/01_mobile_architecture.md`) mở đầu mục tính năng cốt lõi bằng đúng danh sách ADR 0017 đề xuất — Khám phá (Discover), Interactive Player, Thư viện cá nhân, Chế độ Ngoại tuyến, Cửa hàng — và `src/pages` đã có `Library.tsx` cùng `Setlists.tsx`, tức hai khái niệm StageScore sở hữu đang có một cài đặt thứ hai dưới một bundle ID anh em. **Orchestrator chốt: một sản phẩm, app Capacitor đã chết và StageScore thay thế nó** — nên ADR đổi bản chất từ "thêm tính năng vào app offline" sang "chuyển một luồng đã dựng sang shell khác", xoá rủi ro Apple 4.3 mà bản nháp chưa nhắc, đổi lấy việc rút lui một bundle đã có tester (câu hỏi G2 mới số 6 và 7). Bài học lặp lại của ADR 0016 nhưng ngược chiều lần trước: đợt kiểm kê sinh ra 0017 đọc rất kỹ **backend** của repo web và không hề nhìn `apps/` — một danh sách schema và endpoint là bằng chứng về những gì hệ thống *lưu*, không phải về việc đã có ai *dùng* chúng. **Bốn lỗi khác trong ADR, tất cả theo hướng đánh giá thấp thứ bên kia đã có:** `/api/mobile/v1/` có **chín** route chứ không phải sáu, nên câu "chưa có endpoint nào cho luồng Discover" là sai — `projects/[id]` đã kiểm quyền rồi trả nội dung, thứ thiếu là một *gói tải hình dạng StageScore* chứ không phải endpoint; `sheetOverlays` (bookmarks + sequence, phục vụ qua `overlays`) chính là dòng ADR 0014 ghi StageScore **không có**, và `NavigationSequence` là chuỗi **id bookmark** trong khi `PageOrder` là chuỗi **trang**, nên quyết định 7 mới xử lý được nửa vấn đề `measureMap`; danh mục còn có `books` chia chapter nên "một mục = một Score" không phải luôn đúng; và có **hai** targetType mở toàn danh mục, `global_catalog_vip` cộng `b2b_educator_vip`. **Một mâu thuẫn nội tại:** quyết định 5 cấm mô hình quyền thứ hai, nhưng khuyến nghị "ẩn danh qua receipt" của câu hỏi 1 chính là mô hình thứ hai — `computeHasProjectAccess` trả `false` ngay khi không có `session.user`, và `SubscriptionProvider.tsx:46` buộc RevenueCat vào account bằng `Purchases.logIn({ appUserID: user.id })`; quyền còn tồn tại dưới hai tên, `premium` phía RevenueCat và `global_catalog_vip` phía DB. **Một xung đột giữa hai quyết định:** quyết định 4 hứa "đã tải là chơi vĩnh viễn" nhưng quyết định 8 chỉ lưu tham chiếu, nên lời hứa đó có phạm vi **một thiết bị** — khôi phục backup trên máy mới cần mạng và quyền còn hạn, người hết hạn sẽ thấy Score còn đó, annotation nguyên vẹn, mà bản nhạc không mở được. Cũng ghi rõ: `Score` hôm nay có đúng sáu trường không có dấu nguồn gốc, và vì `LibraryBackup` đi cả cây từ `libraryRoot` nên loại được asset có bản quyền đòi **tách layout trên đĩa**, không chỉ bump `formatVersion` — quyết định 2 và 8 là một việc. Câu hỏi 4 (đếm mục dùng được) chuyển từ "hỏi Orchestrator" sang "chạy query trước khi accept", vì `pdfMeasureMap` nằm trong cột JSON `payload` và repo web có sẵn script Turso trong `scratch`; một mình con số đó có thể làm cả ADR vô nghĩa. Phần lớn bảng kiểm kê **đứng vững** khi đối chiếu code: `MeasureBox`, `TimemapEntry`, `heavyDataR2Key`, comment `measureMap` khớp từng chữ, cột `projects`, `rcStoreProductId`, webhook RevenueCat vẫn inert vì `REVENUECAT_WEBHOOK_SECRET` chưa đặt, và hai đính chính về `arrangements` đều đúng. ADR giữ `proposed` — bốn câu hỏi G2 cũ cộng hai câu mới vẫn chờ. Không đụng code. |
| 2026-07-30 | **Hai chốt của Orchestrator sửa lại khung của ADR 0017 ngay trong vòng G2 — và phép kiểm đi kèm tìm ra một lỗ hổng phân quyền thật.** Chốt thứ nhất: app Ionic `com.backingscore.app` là **app giai đoạn thử nghiệm, tính năng chưa hề được chốt**, nên nó không đặt tiền lệ nào — không phải feature set để bê sang, không phải quyết định UX đang có hiệu lực, không có người dùng để di cư. Cách đọc đúng là đọc như một spike đã chạy được: phần đáng tham khảo hẹp và xác định được (`OfflineStorageService`, 175 dòng — `resolveFileUrl` / `downloadAndCacheFile` / `downloadProjectAssets` / `isProjectDownloaded` / `deleteCachedFile`), phần không đáng tham khảo lại là phần lớn nhất (Interactive Player xoay quanh MusicXML với cursor sync). Điều này rút lại khung "StageScore thay thế nó" mà lượt grill trước vừa viết, cùng cả đoạn hệ quả về di cư người dùng TestFlight; việc còn lại chỉ là dọn cấu hình RevenueCat đang gắn với bundle cũ trước khi StageScore mở IAP. Chốt thứ hai thành **quyết định 9 mới: Discover chỉ đi nhánh PDF**, và nó chi phối tám quyết định trên chứ không đứng cạnh — `Score` của StageScore *là* một PDF (`relativePath` trỏ `.pdf`) nên quyết định 2 chỉ thực hiện được với nội dung PDF; Verovio/MusicXML là ADR 0003 và bị 0008 chặn tới H3+; `MeasureBox` với toạ độ tỉ lệ chỉ có nghĩa trên trang in. Mục chỉ có MusicXML **không hiện trong Discover**, chứ không hiện rồi báo lỗi lúc tải. Phép đo làm quyết định này đắt hơn tưởng: `defaultDAWPayload()` đặt `notationData.type = "music-xml"` cho mọi project mới và cột `projects.notation_type` mặc định `'none'` — nhánh PDF là **đường thiểu số theo thiết kế của repo bên kia**, nên câu hỏi 4 (đếm mục dùng được trên production) chuyển từ phép kiểm cho vui sang điều kiện sống của cả ADR. **Lỗ hổng:** `/api/r2/download/[...fileId]` lấy session ra rồi không dùng — đặt thẳng `let isAuthorized = true` với chú thích "R2 Download Access is mostly capabilities-based via fileId token", trả 302 tới presigned URL sống 3600s, `Access-Control-Allow-Origin: *`. Lập luận capability không đứng được vì key có cấu trúc suy ra từ project id (`projects/123/annotations.json` là ví dụ của chính ADR) mà `storefront` phát công khai. Nghĩa là entitlement ở `projects/[id]` đang canh **metadata** chứ không canh **bytes**, và paywall của Discover sẽ là trang trí nếu ship nguyên trạng — hạng mục số một của Security Review, cộng một câu hỏi G2 mới (số 7: ai enforce quyền tải và ở đâu), vì ba hình dạng khả dĩ đổi cả API mà các Spec sau phải gọi. Một đính chính nhỏ đi kèm: chú thích `NotationData.fileId` nói "Appwrite storage file ID" đã cũ — `offlineStorage.ts` gọi nó là R2 fileId và tải từ R2, nên bản thân tệp PDF cũng nằm trên R2 chứ không chỉ phần JSON nặng. ADR giữ `proposed`, giờ có 9 quyết định và 8 câu hỏi G2. Không đụng code. |
| 2026-07-30 | **Orchestrator trả lời hai câu G2 nặng nhất của ADR 0017, và câu trả lời đảo ngược trình tự triển khai.** *Danh mục hiện có bao nhiêu mục dùng được?* → **Không có mục nào** — chưa PDF nào được public; nếu tính năng được duyệt thì nội dung sẽ được **đầu tư sản xuất mới**, trước mắt là nhạc cổ điển và etude luyện tập soạn bằng MuseScore rồi export PDF, **BackingTrack và SyncMap tạo sau**. *Bản khắc là của ai?* → **của team**, tác phẩm hết bảo hộ soạn lại nên quyền sạch, không phải xin phép ai; phần còn lại chỉ là kỷ luật sản xuất là đừng nhập scan của ấn bản hiện đại vào cùng kho. **Hệ quả lớn nhất: trình tự bản nháp mở bằng thứ không có nội dung để chạy.** Bước 1 cũ là Transport + `BackingLane` "chứng minh phần UX đắt nhất trước" — nhưng BackingTrack tạo sau, nên bước đó sẽ chạy trên dữ liệu tự bịa và chứng minh được đúng bằng không. Trình tự viết lại theo nội dung có thật: (1) manifest + search cục bộ + tải PDF **free**, không tài khoản không tiền — bước duy nhất có nội dung ngay khi kho đầu tiên xong, và nó tự đứng được như một sản phẩm; (2) subscription + entitlement khi đã đủ bài đáng trả tiền, và đây mới là bước cần câu hỏi 7 (enforce quyền tải) trả lời xong; (3) Transport nhận `BackingLane`; (4) `MeasureAnchor` + playhead. Lợi ích phụ đáng kể: bước 1 không chạm Transport, không chạm tiền, không chạm tài khoản, nên **không mang theo phần lớn rủi ro mà tier L của ADR này tồn tại để canh**. Ba ghi chú nữa từ dữ kiện mới. **Đường tới hạn chuyển từ code sang sản xuất** — không Spec nào đẩy nhanh được tốc độ soạn nhạc, nên phải chốt trước "bao nhiêu bài thì đủ để mở", vì danh mục mỏng là ấn tượng đầu chỉ có một lần. **Pipeline phải đặt `notationType = 'pdf'` chủ động**, vì `defaultDAWPayload()` đặt `music-xml` và cột mặc định `'none'` — một mục nhập theo đường mặc định sẽ không lọt bộ lọc của quyết định 9 dù PDF hoàn toàn ổn, đúng loại lỗi im lặng. Và **giữ file `.mscz` làm bản gốc lưu trữ, không chỉ giữ PDF đã export**: sửa nốt sai thì re-export, đổi khổ trang cho tablet thì re-export cả kho, và đến lúc làm SyncMap thì công cụ hiện có bên repo web (`useTappingSystem`) là gõ tay từng ô nhịp trên PDF trong khi MuseScore có đường xuất vị trí ô nhịp bằng CLI (`mposXML`/`sposXML`, thứ musescore.com dùng cho cursor) — cần kiểm trước khi dựa vào, nhưng nếu đúng thì đó là khác biệt giữa gõ tay vài trăm bài và chạy một script, và chỉ giữ PDF thì lựa chọn đó đóng vĩnh viễn. Quyết định 9 rẻ đi hẳn nhờ câu trả lời này: lọc theo PDF không vứt đi gì cả, vì không có gì đang tồn tại. **Câu hỏi G2 mới, và là câu nặng nhất còn lại (số 9): nhạc cổ điển và etude vốn đã miễn phí ở khắp nơi thì người ta trả tiền cho cái gì** — đúng hình dạng lập luận Orchestrator đã dùng để bác bản Pro. IMSLP có hàng chục nghìn bản public domain tải ngay được, nên ba chỗ khác biệt phải được *chọn* chứ không để mặc: bản khắc sạch dàn trang cho tablet, danh mục chọn lọc xếp thành lộ trình, và BackingTrack + SyncMap. Chỗ thứ ba mạnh nhất nhưng lại đến muộn nhất, nên tính tiền ở bước 1 là đang bán hai chỗ đầu. ADR giữ `proposed`: 9 quyết định, 9 câu hỏi trong đó 4 đã trả lời. Không đụng code. |
| 2026-07-30 | **Orchestrator chốt hai câu G2 nặng nhất còn lại của ADR 0017 — và cả hai đều thu nhỏ rủi ro của bước đầu tiên.** *Người ta trả tiền cho cái gì?* → **bước 1 miễn phí hoàn toàn, bán từ bước 3 trở đi** (quyết định 10 mới). *Subscription có tài khoản không?* → **không** (quyết định 5 viết lại). Câu 9 được quyết bằng một phép thử đáng giữ lại làm công cụ: *thứ này có tái tạo được bằng cách ghép một nguồn miễn phí với chính StageScore — vốn cũng miễn phí — hay không?* App này **là** trình đọc PDF cho nội dung IMSLP, tức nó tự cấp phần phân phối cho đối thủ của mình. **Bản khắc sạch** trượt: Mutopia và các bản typeset trên IMSLP đã sạch và free; bản khắc có bán được tiền nhưng Henle/nkoda bán **thẩm quyền biên tập** hàng chục năm, thứ brand mới chưa có. **Lộ trình** trượt, và đây là phát hiện mới từ đợt kiểm repo web: bên đó **đã sở hữu và đã tính tiền** cho khái niệm này (`courses`/`lessons`/`enrollments`/`progress` với `difficulty` + `priceCents`, cộng `books` chia chapter và `TeacherHub`), còn nội dung dạy của một lesson là **TipTap JSON** (`contentRaw`) mà StageScore không render được — nên rơi vào app, lộ trình chỉ còn lại **thứ tự**, thứ StageScore đã cho không qua Setlist/Label/Sort. Chỉ **BackingTrack + SyncMap** qua được, với Tomplay là bằng chứng tồn tại. Thêm một **điểm khác biệt thứ tư mà ADR bỏ sót và mạnh nhất ở bước 1: nội dung cắt vừa khít app** — ngắt trang đặt vào dấu lặng để không lật giữa câu nhạc, PageOrder giải sẵn cho D.C., Bookmark theo chương, JumpLink đặt sẵn; bản scan không thể có vì trang in đã cố định, nguồn `.mscz` làm việc này rẻ, và nó **chụp ảnh được** cho store listing khác với "được chọn lọc". Nên ranh giới tính tiền không nằm giữa hai nhóm bản nhạc mà giữa **bản nhạc và phần đệm**: PDF free vĩnh viễn, paywall đầu tiên khoá BackingTrack; ba điểm trượt phép thử là công cụ thu hút, việc của chúng là làm thư viện luyện tập dọn vào sống trong app để BackingTrack được chào trên bài người ta **đang tập rồi**. Phản chứng đã cân nhắc và bác: MuseScore.com có tính tiền để truy cập nhạc cộng đồng đăng và nó chạy được, nhưng bằng khối lượng cực lớn cộng công cụ phát — tài sản không có ở bước 1. **Câu trả lời "không tài khoản" buộc phải viết lại quyết định 5, vì quyết định 5 đang cấm đúng thứ vừa được chọn** ("không phát minh mô hình quyền thứ hai"). ADR nay **cố ý nuôi hai mô hình**: web account-based tuyệt đối (`computeHasProjectAccess` và `mobileHasBookAccess` đều trả `false` khi không có `session.user`; `SubscriptionProvider.tsx` gọi `Purchases.logIn({ appUserID: user.id })`), StageScore ẩn danh qua RevenueCat anonymous app user ID. Bốn cái giá được ghi thành lời: mua bên này không dùng được bên kia; quyền theo **store account** chứ không theo thiết bị nên Restore Purchases cứu được ca đổi máy còn ca mất thật là **đổi nền tảng** iOS↔Android; webhook không có `userId` để ghi `entitlements` row nên server **không `SELECT` được**, phải hỏi RevenueCat hoặc thẩm định receipt — đổi hình dạng câu hỏi 7; và câu hỏi 2 tự tan. Hai quyết định này cộng lại làm **trình tự còn ba bước** (bước subscription cũ nhập vào bước có phần đệm) và dồn gần như toàn bộ thứ Security Review phải soi sang bước 2: bước 1 không Transport, không tiền, không tài khoản, không có byte nào cần canh. Quyết định 4 nhẹ hẳn — PDF free nên khôi phục máy mới chỉ cần mạng, không cần quyền. Hai cảnh báo mới ghi vào Hệ quả: **"không tài khoản" ≠ "không thu thập gì"** (SDK RevenueCat gửi anonymous ID + metadata thiết bị lên bên thứ ba, purchase history phải khai ở App Privacy và Data safety — nhưng từ bước 2), và **tín hiệu duy nhất để biết nên sản xuất bài nào là số lượt tải tổng hợp phía server**, cần chốt trước bước 1 vì đó là lúc sinh ra tín hiệu và là la bàn duy nhất cho "bao nhiêu bài thì đủ". Một chỗ trong ADR đã lệch và được sửa: bullet `TRANSPORT-ARCHITECTURE.md` vẫn nói "bước 1 phải đi qua Transport thật" trong khi bước 1 nay không phát gì. ADR giữ `proposed`: 10 quyết định, 10 câu hỏi trong đó 5 đã trả lời (còn 3, 6, 7, 8 và câu 10 mới — bán lẻ từng BackingTrack hay một gói mở tất cả, khuyến nghị một gói). Không đụng code. |
| 2026-07-30 | **Orchestrator chốt monetization, đảo lại khuyến nghị ngày 2026-07-29: có quảng cáo, ai không muốn thấy thì mua — và Discover (ADR 0017) chuyển `hold`.** ADR **0018** drafted `proposed`, tier **L** (dữ liệu rời máy + thanh toán → cần Security Review trước khi build). Trước hết phải ghi vế **ủng hộ**, vì nó thật: quảng cáo thoát được đúng cái bẫy đã dùng để bác bản Pro hôm 2026-07-29 — không tính năng nào bị khoá nên không ai bị làm cho tệ đi vì thiếu năng lực, và thứ bán ra ("đừng làm phiền tôi nữa") thì **mọi** người hiểu ngay chứ không cần là người có nhu cầu ngách. **Bốn phép đo định giá quyết định, đọc từ repo chứ không phải cảm nhận, và dòng đầu là dòng nặng nhất: bản release Android hôm nay *không có quyền mạng nào*** — `INTERNET` chỉ nằm ở `src/debug/AndroidManifest.xml`, manifest `main` không khai gì; nghĩa là "app không thể gọi mạng" đang là một **tính chất kiểm được từ bên ngoài** (`aapt dump permissions`), và thêm SDK quảng cáo đổi nó thành một **lời hứa phải tin**. Ba dòng còn lại: `PrivacyInfo.xcprivacy` khai `NSPrivacyTracking=false` với `NSPrivacyCollectedDataTypes` **rỗng**; `STORE-LISTING.md` (copy đã accepted 2026-07-28) viết thẳng "no account, no sign-in, **no ads**, and nothing collected" cộng dòng giá "free, no in-app purchase in v1"; và repo web nói "The app makes **no network requests of its own**" (`privacy/page.tsx:102`) cùng "collects nothing about you or your music" (`support/page.tsx:52`). Tổng cộng **sáu** nơi phải sửa cùng một đợt, hai trong số đó ở repo khác và không có CI nào nối (ADR 0012) — và đây là lần **đăng lại privacy policy thứ hai trong ba ngày** sau R22, đúng thứ R24 đã cảnh báo. **Một ước lượng đổi cả thiết kế:** bề mặt an toàn duy nhất là Library, mà Library là màn hình quá cảnh, nên ~1–3 impression/người/ngày với eCPM banner non-personalized hạng Music cho ra chừng một bữa cà phê mỗi tháng ở mức nghìn DAU — mà app mới thì chưa có nghìn DAU. Kết luận **không** phải "đừng làm" (Orchestrator đã quyết) mà là biết banner đang làm việc gì: nó là **lý do để mua**, không phải nguồn thu — nên mọi đánh đổi trong ADR đều nghiêng về giữ app tử tế, vì phía bên kia cán cân gần như trống. Chín quyết định, ba cái đáng nhớ nhất: (1) **quảng cáo chỉ sống ở Library**, cấm ở `PdfMode`/PerformanceMode/Setlist đang chạy, và bất biến này có **test guard** theo idiom test brand 0042 + guard token 0044 — cần test chứ không cần kỷ luật vì hai bề mặt dùng chung `Scaffold`/`AppAppearance`, nên một slice sau thêm banner vào component dùng chung sẽ không lộ ra ở review; (2) **một định dạng duy nhất là banner tĩnh**, cấm đích danh bốn thứ mà mạng quảng cáo sẽ chào mời — **app-open** (bắn mỗi lần quay lại foreground, tức gồm cả lần quay lại sau khi màn hình khoá giữa buổi diễn), interstitial, rewarded, và native trộn vào danh sách Score; (3) **SKU tên `remove_ads`, không phải "Pro"** — đây là phần **duy nhất không sửa được sau khi ship**, vì giá và mô tả thì đổi được còn *thứ người ta tin mình đã mua* thì không, và một SKU "Pro" sẽ ràng buộc luôn BackingTrack nếu 0017 sống lại. Còn lại: không chặn đường Library→Score và slot cao **0** khi không có fill (offline là trạng thái bình thường, không phải ca lỗi); **non-personalized, không ATT** (mất eCPM, đổi lấy không prompt + không mục Tracking công khai trên trang App Store — nhưng vẫn phải có CMP cho EEA/UK, một màn hình mới đứng **trước** Library); cờ đã mua **không đi theo backup** (một file backup gửi qua lại sẽ mang theo quyền đã mua); Restore Purchases bắt buộc theo Apple 3.1.1, đặt trong About sheet 0042. Một hệ quả ít ai nghĩ tới nhưng đáng một test: **người đã mua thì tầng quảng cáo không được khởi tạo chút nào** — không SDK init, không consent, không request — tức app quay về đúng trạng thái không-chạm-mạng của hôm nay. **Một phương án được ghi vào ADR để G2 bác có ý thức chứ không phải bỏ sót:** thẻ "Mở khoá StageScore" tự chạy trong Library, không SDK, không mạng, không đổi một chữ nào trong sáu tuyên bố — nó làm đúng công việc mà phần trên vừa nói banner đang làm, và chỉ mất phần doanh thu đã ước lượng là rất nhỏ. Tám câu hỏi G2; hai câu chặn: **"mua app" là free+IAP hay app trả tiền trước / hai SKU** (ADR đọc là free+IAP), và **ads vào v1 hay 1.1** — khuyến nghị **1.1**, vì v1 đang ở cửa store với blocker toàn việc người, screenshot Library sẽ phải chụp lại nếu có banner, và dồn SDK + consent + IAP + sáu tài liệu + Security Review vào đúng lần submit đầu tiên là lúc một lần từ chối của Apple đắt nhất. Discover `hold` chứ không cắt: mười quyết định và đợt kiểm kê hai repo giữ nguyên giá trị, lý do dừng là đường tới hạn của nó là **tốc độ sản xuất bản nhạc** còn danh mục hiện là số không, trong khi hướng vừa chốt không cần bản nhạc nào. R7 và `VISION.md` câu 5 đóng khi 0018 accepted. Không đụng code. |
| 2026-07-30 | **Ba câu chặn của ADR 0018 được trả lời ngay trong lượt, cả ba theo khuyến nghị — và câu thứ hai đổi nhiều thứ hơn một cái ngày tháng.** *"Mua app" là gì?* → **free + một IAP tắt quảng cáo** (không phải app trả tiền trước, không phải hai SKU rời). *Cá nhân hoá?* → **không**, không ATT không IDFA; quyết định 5 giữ nguyên, phần còn mở của nó không phải khẩu vị mà là một dữ kiện phải tra — `NSPrivacyTracking` có được giữ `false` hay không, đọc từ manifest privacy Google công bố cho SDK chứ không suy đoán. *v1 hay 1.1?* → **1.1**, thành **quyết định 10** mới, và nó **đảo một phần quyết định 8**: sáu tuyên bố ("no ads" trong listing, hai `PrivacyInfo.xcprivacy`, hai trang ở repo web, hai form R11) không những chưa cần sửa mà **không được** sửa sớm — sửa trước là làm cho tài liệu nói sai về chính bản v1 đang nằm trên store. Ba hệ quả đã ghi vào ADR và checklist. **R7 chuyển `done` cho v1** ở dạng "miễn phí, không IAP" — đúng thứ `STORE-LISTING.md` dòng 5 vẫn viết, nên **không một chi tiết nào của lần submit đầu tiên phải đổi**; ADR 0018 là câu trả lời cho 1.1 chứ không cho v1, và R7 thôi là ô trống. **R12 nhận một ràng buộc nhỏ mà rẻ bây giờ**: chụp khung Library sao cho khi banner xuất hiện ở 1.1 thì chỉ mất một ảnh chụp lại, không mất cả bộ sáu. **Và gần như toàn bộ tier L rời khỏi lần ra mắt** — v1 không SDK bên thứ ba, không thanh toán, không consent, không byte nào rời máy, nên Security Review của 0018 canh 1.1 và một lần từ chối của Apple (nếu có) sẽ rơi vào bản cập nhật thay vì bản ra mắt. `STORE-LISTING.md` nhận thêm một khối cảnh báo "đừng sửa sớm" ngay đầu file, vì đây đúng loại việc mà ba tuần nữa sẽ có người sửa với thiện chí. ADR 0018 giữ `proposed`: 10 quyết định, 8 câu hỏi trong đó 3 đã trả lời (còn 4 giá, 5 đường thanh toán, 6 mạng quảng cáo, 7 quan hệ với BackingTrack sau này, 8 chấp nhận mất câu "no ads"). Không đụng code. |
| 2026-07-30 | **Orchestrator xét lại câu 2 trong cùng ngày: quảng cáo vào thẳng `1.0.0`, không đợi 1.1 — ngược khuyến nghị của agent.** Quyết định 10 viết lại, và đây không phải đổi một cái ngày. **Một chỗ nhẹ đi thật:** cảnh báo "đừng sửa sáu tuyên bố sớm" sinh ra từ giả định v1 lên store trước, giả định đó nay sai nên nó biến mất — **chưa có bản nào trên store** để mà nói sai về nó, sáu chỗ chỉ phải đúng vào **đúng ngày** Apple và Google đọc URL chính sách, nên sửa **một lần trọn gói** thay vì viết đúng hai lần cho hai app khác nhau. Khối cảnh báo vừa đặt ở đầu `STORE-LISTING.md` lượt trước bị thay bằng khối nói ngược lại. **Bốn chỗ nặng lên, và ba là việc giấy tờ chờ bên thứ ba chứ không phải việc code — đây là phần đáng giá nhất của lượt này vì không cái nào lộ ra từ phía codebase.** (1) **Bán IAP đòi hồ sơ thuế + ngân hàng đã *active* ở cả hai store**: Paid Applications Agreement của Apple chưa active thì **không tạo được sản phẩm IAP**, chứ không phải chỉ là chưa nhận được tiền; Play cần merchant account; pháp nhân VN còn W-8BEN/W-8BEN-E. Lead time dài nhất trong toàn v1 và **không phụ thuộc dòng code nào** → thành **R25**, khuyến nghị khởi động **ngay bây giờ song song với G2**, vì nếu ADR bị bác thì hồ sơ vẫn còn dùng được, còn làm muộn thì nó chặn ngày phát hành. (2) Cần tài khoản AdMob và **`app-ads.txt` ở gốc `backingscore.com`** — thiếu thì fill tụt và một số nguồn không mua; file nằm ở **repo web**, không CI nào nối (ADR 0012) → thành **R26** và thành mục 7 trong bảng sáu-nay-là-bảy tuyên bố. (3) **Security Review và G2 nay chặn ngày phát hành** thay vì chặn một bản cập nhật. (4) **Từ chối rơi vào lần submit đắt nhất** — từ chối bản cập nhật thì app vẫn đang bán, từ chối bản đầu thì chưa có gì; ba loại hay gặp đúng ở hình dạng này là consent EEA, khai "Contains ads" lệch hành vi thật, và metadata sản phẩm IAP. **Đường tới hạn đổi chủ:** tới hôm nay blocker của v1 toàn việc người chạy song song được (R5, R6, R12, R16); nay có một chuỗi **tuần tự** đứng trước chúng — G2 → Security Review → build → G4 → sáu tài liệu → submit — không khúc nào rút ngắn bằng cách làm song song vì mỗi khúc là đầu vào của khúc sau. Header của `RELEASE-CHECKLIST.md` sửa theo, **R7 chuyển từ `done` cho v1 sang `blocker`**, R11 thôi là việc chép lại (nay phải khai dữ liệu SDK thu thập + nhãn "Contains ads" + purchase history, và vẫn còn một dữ kiện phải tra chứ không đoán: `NSPrivacyTracking` có được giữ `false` với non-personalized hay không). **Hai ràng buộc mới chỉ lộ ra lúc sắp bấm máy và lúc sắp submit**, thành câu hỏi G2 số 9 và 10. Số 9: **ảnh chụp store không được chứa quảng cáo của bên thứ ba**, nên ảnh Library (mục 5 của R12) phải chụp ở **trạng thái đã mua** — khuyến nghị vậy — hoặc đóng khung cho banner ra ngoài, mà ảnh Library vốn đã là ảnh nhiều chi tiết nhất bộ sáu. Số 10: **`STORE-LISTING.md` mất trạng thái "copy đã accepted"** — khối "Yours, on the device" nói đúng ba vế (không account, không quảng cáo, không thu thập gì) mà quyết định này làm sai hai; bản viết lại đã draft trong ADR, giữ phần còn thật và nói thẳng phần mới thay vì giấu xuống cuối ("Your scores stay on your tablet… A banner in the library keeps StageScore free — one purchase removes it for good"), và nó cần Orchestrator accept lại vì là copy đã qua gate. Ba câu hỏi cũ đổi mức khẩn: **4 (giá) nay chặn lịch** vì tạo SKU đứng trước ngày submit, **5 (đường thanh toán) và 6 (mạng quảng cáo) nay chặn Spec**, và 6 chặn luôn R26. ADR 0018 giữ `proposed`: 10 quyết định, 10 câu hỏi trong đó 3 đã trả lời. Không đụng code. |
| 2026-07-30 | **Ba câu còn chặn của ADR 0018 chốt xong, thành quyết định 11 — và một trong ba đi ngược khuyến nghị theo cách kéo thêm một blocker vào v1.** *Mạng quảng cáo?* → **AdMob** (`google_mobile_ads`, UMP làm CMP). *Giá?* → **hạng ~4,99 USD**, VN đặt riêng ~119.000₫ chứ không để store tự quy đổi. *Đường thanh toán?* → **RevenueCat**, ngược khuyến nghị `in_app_purchase` trần. Cái được của RevenueCat là thật: nếu Discover mở lại thì ADR 0017 quyết định 5 đã chọn đúng nó với anonymous app user ID, nên không phải dựng hạ tầng thanh toán thứ hai, và Restore Purchases không phải tự viết. Cái mất cũng thật và **cộng vào chứ không đứng riêng**: thêm một SDK bên thứ ba gửi dữ liệu ra khỏi máy, tức thêm một mục ở cả hai form privacy và một hạng mục nữa cho Security Review, ngay bên cạnh AdMob. **Chi phí không hiện ra lúc chọn: câu hỏi 6 của ADR 0017 nhảy từ "dọn dẹp về sau" thành blocker của v1** — cấu hình RevenueCat, sản phẩm IAP và entitlement `premium` đang gắn với `com.backingscore.app`, bundle của prototype Ionic; với `in_app_purchase` trần thì chuyện đó không liên quan, với RevenueCat thì nó chắn đường, nên thành **R27**. Ba ràng buộc viết thành lời để không đụng vào mô hình danh tính của repo web, cả ba đọc từ code prototype: **không bao giờ gọi `Purchases.logIn`** (bên đó gọi `Purchases.logIn({ appUserID: String(data.user.id) })` ở `SubscriptionProvider.tsx:46` vì quyền gắn account — mô hình của web, không phải của app này); **entitlement tên `no_ads`, không dùng lại `premium`** (prototype đọc `entitlements.active['premium']` dòng 122, và `premium` bên web nghĩa là mở toàn danh mục — một tên cho hai nghĩa là loại lỗi chỉ lộ ra khi đã có người mua); và **project RevenueCat riêng**, vì customer gắn `users.id` với customer ẩn danh không sống chung sạch trong một project, còn webhook `/api/webhooks/revenuecat` bên web tuy đang inert (thiếu `REVENUECAT_WEBHOOK_SECRET`) nhưng khi bật sẽ nhận event StageScore **không có `userId`**. ADR 0018 nay có **11 quyết định, 10 câu hỏi trong đó 6 đã trả lời** — còn 7 (quan hệ `remove_ads` với BackingTrack sau này, phải chốt trước khi tạo SKU), 8 và 10 (duyệt lại copy listing, gộp làm một), 9 (ảnh Library chụp ở trạng thái đã mua). Đủ để viết Spec sau khi ADR accepted ở G2. Không đụng code. |
| 2026-07-30 | **Spec 0051 drafted `proposed` (tier L) cho tầng quảng cáo + `remove_ads` — và bốn thứ đọc từ repo đổi thiết kế của nó, không cái nào lộ ra từ ADR 0018.** (1) **Mọi prefs của app ghi vào trong `libraryRoot`** (`page_color_filter_prefs_store.dart:10`) còn `LibraryBackup` đi cả cây từ đó (0050), nên viết cờ `no_ads` theo đúng thói quen của codebase sẽ **đưa nó vào file backup** — phá quyết định 6 của ADR, và **không test nào hôm nay đỏ**; cache phải nằm ngoài `libraryRoot`, và cũng không được nằm trong `<cache>` vì OS xoá được và người đã trả tiền thấy quảng cáo quay lại là lỗi tệ nhất của slice. (2) **Quyết định 1 đòi một widget test mở Score, và test đó không viết được** — `PdfModeScreen` không pump được vì pdfrx cần native viewer, đúng lý do `brand_reach_test.dart` (0042) và `design_token_guard_test.dart` (0044) đều là test **nguồn**; guard đổi hình thành allowlist nguồn cộng widget test trên Library, thêm hai thứ rẻ là cấm `Purchases.logIn` ở mọi file và cấm chuỗi `'premium'` trong `lib/monetization/`. (3) **Library vẫn mounted bên dưới trong suốt buổi diễn** vì PdfMode là route đẩy chồng, nên một banner có **auto-refresh** — mặc định bật được ở **console AdMob**, tức không nằm trong diff và không ai thấy nó sai — sẽ gọi mạng đều đặn lúc nhạc công đang chơi, đúng thứ quyết định 3 cấm mà không màn hình nào cho thấy; chữa ở cả console lẫn code (dispose banner khi route đẩy lên). (4) FAB của Library neo góc dưới phải, nên banner đáy là quyết định bố cục chứ không phải chi tiết. **Hai chỗ ADR tự mâu thuẫn hoặc bỏ trống, đều thành câu hỏi G3 có khuyến nghị:** hệ quả viết "màn hình consent đứng **trước** Library" trong khi quyết định 3 cấm mọi thứ của tầng quảng cáo đi trước first paint — UMP chỉ đòi consent trước một **ad request**, nên khuyến nghị Library vẽ trước, form sau; và ADR đặt tên SKU, đặt Restore vào About, nhưng **không nói mua ở đâu** — khuyến nghị một đường "Remove ads" gắn liền với banner, tự biến mất khi slot cao 0, cộng một hàng trong About. **Chỗ dễ hỏng nhất được viết thành một câu hỏi riêng: mặc định lúc chưa biết.** Cả hai mặc định đều sai — "chưa mua" thì người đã trả tiền thấy banner **nháy** mỗi lần mở app offline (mà offline là trạng thái bình thường của app này), "đã mua" thì offline không ai thấy quảng cáo bao giờ; nên cache là nguồn sự thật lúc khởi động, mạng chỉ *cập nhật* nó, và một lần gọi **thất bại** không bao giờ hạ cờ. 12 câu hỏi G3, đáng nhớ thêm: một Spec chứ không tách đôi (không nửa nào G4 được một mình, và `noAds` là **đầu vào** của việc có khởi tạo SDK quảng cáo hay không, nên build entitlement trước); ad unit id từ một chỗ với debug luôn dùng unit test của Google (ship nhầm unit test thì doanh thu bằng 0 và không có gì báo, chạy unit thật trong debug là invalid traffic — AdMob khoá tài khoản chứ không nhắc); `maxAdContentRating: 'G'`; không banner khi Library trống; và không dọn gì thêm trong `library_screen.dart` (1407 dòng) để Security Review đọc đúng phần cần đọc. **Thuật ngữ miền mới:** `Entitlement` — quyền gắn **store account**, StageScore có đúng một tên `no_ads`, kèm cảnh báo nó khác `premium` của repo web (ADR 0014). Phạm vi cắt rõ: hai `PrivacyInfo.xcprivacy` **trong repo này** là việc của slice, còn bốn tuyên bố ở repo web cộng hai form store thuộc R11/R26 và checklist submit — nhưng G4 không được đi trước chúng vì cả bảy phải đúng cùng một ngày. Tiêu chí G4 có hai phép kiểm không nhìn thấy bằng mắt: `aapt dump permissions` trên AAB release phải ra **đúng** `INTERNET` + `AD_ID` và không gì khác, và một network log 5 phút trong lúc mở một Score phải **trống**. **Spec giữ `proposed` và không được build**: tier L cần ADR 0018 accept ở G2 cộng Security Review trước khi viết dòng đầu tiên; G3 accept cho Spec là chưa đủ. Không đụng code. |
| 2026-07-30 | **Orchestrator uỷ quyền agent trả lời nốt bốn câu G2 còn lại của ADR 0018 và toàn bộ G3 của Spec 0051 — nhưng thứ đáng giá nhất của lượt này không phải câu trả lời nào, mà là một ràng buộc vòng của AdMob làm đổi nghĩa của "quảng cáo vào v1".** Bốn câu G2 chốt theo khuyến nghị, thành **quyết định 12**: câu 7 **không** (người mua `remove_ads` không được gì thêm nếu BackingTrack bán sau này) kèm một ràng buộc suy ra được theo *chiều ngược* mà chưa ai viết — lời hứa là "không quảng cáo" chứ không phải "không quảng cáo ở Library", nên nếu Discover sống lại và có quảng cáo ở đó thì `no_ads` phải tắt luôn ở đó; câu 8+10 **chấp nhận** mất câu "no ads" và duyệt copy mới, với lý lẽ đáng giữ lại là khối copy mạnh vì **cụ thể** chứ không vì dài, nên giữ nguyên hai vế còn đúng và nói thẳng vế mới **tại chỗ** thay vì giấu xuống cuối mô tả; câu 9 **trạng thái đã mua**. **Phát hiện thành quyết định 13, đọc từ tài liệu AdMob:** từ tháng 1/2025 mọi app mới tạo trong AdMob **bắt buộc xác minh bằng `app-ads.txt`**, mà xác minh đòi AdMob crawl developer website **từ listing đã publish**, và app chưa xác minh thì *"won't be able to fully serve ads"* — cộng một bước **app readiness review** nữa phía sau. Không có thứ tự nào cho phép v1 lên store mà đã phục vụ quảng cáo, nên **v1 ra mắt với tầng quảng cáo không phục vụ gì**, doanh thu bắt đầu sớm nhất 24 giờ sau khi listing live. Ba hệ quả và phần lớn là tin tốt: **"slot cao 0" không phải ca biên mà là trạng thái ngày ra mắt**, nên nó thành tiêu chí G4 quan trọng nhất chứ không phải mục phụ; **rủi ro bị từ chối vì quảng cáo nhẹ đi thật** vì cả hai store review một bản build không hiển thị quảng cáo nào (vẫn phải khai "Contains ads" theo **năng lực**, không theo thứ reviewer thấy); và **Marketing URL thôi là chuyện trình bày** — AdMob crawl theo *hostname* của developer website, `https://backingscore.com/stagescore` đúng hostname nên `app-ads.txt` ở gốc là đúng chỗ, nhưng bỏ trống trường đó là mất doanh thu quảng cáo vĩnh viễn mà không có cảnh báo nào. **Câu `NSPrivacyTracking` — thứ ADR dặn "đọc manifest, đừng đoán" — hoá ra tự giải bằng suy luận chứ không bằng phép tra:** khai `true` thì **bắt buộc** có ATT prompt (App Review 5.1.2), mà quyết định 5 đã cấm ATT, nên `false` là ô duy nhất điền được — và cái giá của nó là một **ràng buộc lên code**, app phải khoá cứng non-personalized, không nhánh nào bật lên được. Việc đọc manifest vẫn còn nhưng là việc khác: đọc `PrivacyInfo.xcprivacy` **bên trong** `GoogleMobileAds.xcframework` *sau khi* thêm dependency rồi sinh Privacy Report từ bản archive; và `google_mobile_ads` phải từ **`11.2.0`** trở lên, dưới mức đó là `ITMS-91061` mà cách chữa là nâng version chứ không phải sửa code. **Một đính chính vào quyết định 5:** "không IDFA" đúng trên iOS nhưng vì lý do khác (không ATT thì IDFA không lấy được), còn non-personalized ads **vẫn dùng advertising ID** cho frequency capping và báo cáo tổng hợp theo chính tài liệu Google — nên **trên Android AD_ID vẫn được dùng**, và Data safety phải khai theo sự thật đó chứ không khai "không cá nhân hoá nên không thu gì". Cũng từ đó: người EEA **từ chối** consent thì đường đúng là **limited ads**, không phải tắt hẳn và không phải hỏi lại — bản nháp không biết chế độ này tồn tại. Spec 0051 nay **13 câu G3 đã trả lời**, bốn câu đổi nội dung sau tra cứu; thêm ba điều kiện dự án đã kiểm chứ không giả định (`minSdk 24` ≥ 21, iOS target `14.0` ≥ 13.0, `launchMode="singleTop"` đúng thứ RevenueCat đòi để giao dịch không bị huỷ khi người dùng sang app ngân hàng) và một thứ không sinh ra từ `pubspec.yaml`: capability **In-App Purchase** trên target `Runner`. `STORE-LISTING.md` **phục hồi trạng thái copy accepted** với khối "Yours, on the device" viết lại, dòng giá mới, và mục Data safety đổi từ "no data collected" sang bảng ba thứ phải khai. `RELEASE-CHECKLIST.md` nhận một mục mới — **runbook ba track cho R25/R26/R27** với thứ tự, điều kiện tiên quyết và thời gian chờ thật (Apple: ký Paid Apps → form thuế US bắt buộc cho **mọi** developer → banking chỉ nhập được **sau** khi nộp form → active thường trong ~24 giờ nhưng xử lý form thuế có thể tới **90 ngày**; chưa active thì **không tạo được** sản phẩm IAP) — cộng một bảng nói rõ **code thật ra chờ rất ít**: nhờ Spec 0051 bọc cả hai SDK sau port có fake, toàn bộ phase 1 (entitlement, cache, guard, About, mọi test) **không cần một tài khoản nào**. Không đụng code. |
| 2026-07-30 | **Security Review đầu tiên của repo chạy cho ADR 0018 + Spec 0051 (`docs/security/0051-...`) — pass có điều kiện, và bốn phát hiện nặng nhất đều nằm *ngoài* bảy hạng mục bắt buộc.** Đây là review **thiết kế** chứ không phải review diff (chưa có dòng code nào), và định dạng file là đề xuất vì ADR 0013 mượn khái niệm Security Review từ repo web mà chưa có tiền lệ ở đây. **S1 (cao, chặn build): RevenueCat đang được khởi tạo cho cả người không bao giờ mua gì.** ADR viết rất đúng vế "người đã mua thì tầng quảng cáo không được khởi tạo", nhưng không ai viết **vế soi gương** — mở app là gửi anonymous ID + metadata thiết bị cho bên thứ ba trước khi có giao dịch nào, và UMP canh quảng cáo chứ không canh RevenueCat nên người ngoài EEA không có cổng nào đứng trước. Chữa bằng **khởi tạo lười** (chỉ `configure` khi cache nói đã mua, hoặc khi người dùng chủ động bấm Remove ads / Restore) và cái giá đúng bằng **không**, vì ca duy nhất bị ảnh hưởng — mua máy A cài máy B — vốn **đã** cần Restore theo quyết định 7; khởi tạo sớm không cứu ca đó, nó chỉ đổi quyền riêng tư của tất cả mọi người lấy không gì cả. **S2 (cao, chặn submit): "age rating 4+/Everyone" và "target audience" là hai form khác nhau.** Play hỏi *Target audience and content* riêng; khai nhóm dưới 13 thì app rơi vào **Families policy** — `AD_ID` phải gỡ khỏi manifest, chỉ ad SDK đã chứng nhận, và không phục vụ được theo cách ADR thiết kế. Một app nhạc rating Everyone, người điền form đang nghĩ tới học sinh: cú click sai hoàn toàn tự nhiên và hệ quả chỉ lộ ra khi quảng cáo không chạy. Khai **13+** có chủ đích, đã ghi thành khối cảnh báo ngay đầu `STORE-LISTING.md`. **S3 (trung bình): quyền `INTERNET` sau slice này không còn ai canh** — rủi ro thật không phải hai SDK hôm nay mà là dependency thứ ba của một năm sau, vì quyền đã có sẵn nên một plugin mới gọi mạng sẽ không làm đỏ gì cả; `aapt dump permissions` thủ công ở G4 sẽ mục sau một lần, nên cần script trong `tool/` cộng luật allowlist của guard không được nới nếu không có ADR. **S4 hạ mức nhờ một phép kiểm:** cache entitlement giả mạo được, nhưng `UIFileSharingEnabled` **không có mặt** và `LSSupportsOpeningDocumentsInPlace` = `false` nên Documents không duyệt được từ Finder — đường giả mạo đòi jailbreak/root. Chấp nhận (bảo vệ ~5 USD bằng một biện pháp phá lời hứa offline là lỗ), **kèm một điều kiện không bỏ được**: `restoreBackup` không bao giờ được tạo hay sửa file cache — quyết định 6 lo chiều đi, đây là chiều về, và bỏ sót nó là con đường giả mạo *không* cần root. **S5:** cổng duy nhất trước ad request là `canRequestAds()` của UMP, hỏi lại mỗi lần, app **không lưu** trạng thái consent — hình dạng hỏng thường gặp là tự nuôi một `bool hasConsent` cho "khỏi hỏi lại", và đó là rủi ro pháp lý chứ không phải kỹ thuật. **S6:** `.p8` và service-account JSON **chỉ** vào dashboard RevenueCat, còn hai SDK key là **public theo thiết kế** nên phải đặt tên `publicSdkKey` — "bảo vệ" chúng vào một file gitignored sẽ làm clone mới không build được, đúng cái bẫy `android/key.properties` đã đặt một lần (R1). **S9 là quan sát chứ không phải lỗi, ghi để không ai "sửa":** `<documents>` nằm trong iCloud/iTunes backup nên cờ đã mua sống sót qua khôi phục thiết bị — điều đó **đúng**, và nó khác hoàn toàn `LibraryBackup` là file nhạc công gửi cho nhau. **S10 — review cố ý chưa đóng:** hạng mục bắt buộc số 1 (hai SDK thật sự gửi gì) **không kiểm được** trước `pub get` vì manifest nằm trong framework; phải mở lại đúng một lần sau khi thêm dependency và trước khi archive. Ba điều kiện chặn build (S1, S4, S5) **đã áp thẳng vào Spec 0051** cùng hai tiêu chí G4 mới. Không đụng code. |
| 2026-07-30 | **Orchestrator lùi quảng cáo về 1.1 — lần đổi thứ ba của cùng một câu trong một ngày, nhưng lần này là dữ kiện mới chứ không phải đổi ý, và điều đó đáng ghi cho rõ.** Hai lần trước cân nhắc trên cùng một bộ dữ kiện; quyết định 13 (AdMob không phục vụ quảng cáo cho app chưa lên store) chỉ được phát hiện **sau** khi chọn v1, và nó rút cạn phần lợi của lựa chọn đó. Lập luận đóng lại gọn: ship quảng cáo trong v1 mua về **doanh thu bằng không trong ngày phát hành** — không phải "ít" mà bằng không — trong khi đặt trước ngày phát hành cả chuỗi tuần tự tier L cộng hồ sơ thuế có thể mất **tới 90 ngày**; trả vài tuần tới vài tháng để đổi lấy không đồng nào là trao đổi không có vế được. **Cái mất khi lùi có thật và được ghi thẳng:** bảy tuyên bố của quyết định 8 nay phải đúng **hai lần** — đúng cho v1 (miễn phí, không quảng cáo, không IAP, tức đúng như hôm nay) rồi viết lại cho 1.1; đó chính là lý lẽ đã dùng để chọn v1 lượt trước và nó không biến mất, nhưng nó là **công việc soạn thảo** còn thứ vừa tránh được là **thời gian chờ của bên thứ ba**, hai loại chi phí không cùng hạng. **Cảnh báo "đừng sửa sớm" quay lại và nghiêm hơn lần đầu:** kể từ khi v1 lên store, bảy tuyên bố đó mô tả **một app đang được phát hành**, nên sửa sớm là làm chúng nói sai về chính bản người ta đang tải. `STORE-LISTING.md` hoàn nguyên hoàn toàn — khối "Yours, on the device" về đúng ba vế cũ, dòng giá về "free, no in-app purchase in v1", mục Data safety về "no data collected" — cộng khối cảnh báo đặt ở **đầu file**. Hai thứ **giữ lại** dù lùi lịch vì chúng đúng ở cả hai bản: **target audience 13+** (Security Review S2 — Play hỏi riêng khỏi age rating, khai dưới 13 thì rơi vào Families policy và cả thiết kế quảng cáo sụp; khai đúng từ lần submit đầu vì đổi sau là re-review) và **điền Marketing URL ở v1** dù v1 không cần, vì 1.1 cần AdMob crawl đúng hostname đó để tìm `app-ads.txt`. **Đợt kiểm sức khoẻ v1 cùng lượt tìm ra hai dòng tài liệu nói sai:** `flutter analyze` sạch và **365/365 test xanh**, nhưng R20 ghi trang web "**Not deployed yet**" trong khi R10 ghi đã live và verified từ 2026-07-28 — gọi thẳng URL cho ra `/privacy`, `/support`, `/stagescore` đều **`200`**, nên R10 đúng và R20 cùng ba dòng trong bảng URL của `STORE-LISTING.md` đều stale; đã sửa cả bốn. (`app-ads.txt` trả `404` đúng như mong đợi — nó là việc của 1.1.) **`RELEASE-CHECKLIST.md` nhận một mục mới, "v1 submission runbook"**, và điều đáng nói của nó là v1 quay về đúng thứ nó vốn là: một danh sách việc người chạy song song được, với **hai thứ phải làm trước vì chúng chi phối phần còn lại** — khoá logo (R6: launcher icon và splash **sinh ra từ** nó, và nó nằm trong cả sáu ảnh chụp, nên khoá muộn là sinh lại icon cộng chụp lại toàn bộ) và tra nhãn hiệu (R5: hạng mục mở duy nhất còn có thể ép đổi tên, mà đổi tên sau khi publish thì mất listing, icon, copy trên site và URL store). R7 chuyển `done` cho v1; R26 và R27 gắn nhãn 1.1; R25 vẫn khuyến nghị **khởi động ngay** dù không còn chặn v1, vì nó không chờ ai ở đây cả. Security Review giữ nguyên giá trị, chỉ đổi phạm vi áp dụng sang 1.1 — không finding nào của nó chạm v1. Không đụng code. |
| 2026-07-31 | **Ba hạng mục Orchestrator-only của v1 submission đóng cùng lúc.** **R9:** chấp nhận fit height (0041 / P0.6 nửa) và max page DPI (0031 / P2.14 nửa) là `wont` cho v1 — đúng khuyến nghị đã khóa ở G3 của hai Spec đó; DPI chỉ reopen nếu độ nét chặn buổi diễn. **R6:** khoá logo hiện tại (`stagescore/assets/brand/stagescore-logo.png`); icon và splash đã sinh từ file này, screenshot R12 không còn chờ regen. **R5:** tên StageScore **đã đăng ký**; title trên App Store Connect *và* Google Play là **`StageScore — Sheet Music`**, tên khi cài (home screen / launcher) giữ **`StageScore`** — đúng `CFBundleDisplayName` và `android:label` đang ship, không đổi binary cho khớp store title. `STORE-LISTING.md` App Store name đổi từ `StageScore` sang cùng chuỗi với Play; `VISION.md` câu 1 và 8 đóng. Còn lại cho v1: R12 (chụp, cần hardware), R16 (smoke trên máy thật), R24 (migration `0028` ở repo web). Không đụng code. |
| 2026-08-01 | **R24 đóng:** Orchestrator xác nhận bảng `practice_sessions` đã drop trên production (migration `0028_chemical_emma_frost`). `/privacy` và DB khớp — không còn historical practice log. Còn lại cho v1: R12 và R16 (cả hai cần hardware). |
| 2026-08-01 | **R12 đóng:** Orchestrator xác nhận bộ screenshot store đã chụp xong (copy listing vốn accepted từ 2026-07-28). Còn lại cho v1 trước submit: **R16** (smoke release trên máy thật, cả hai nền tảng). |
| 2026-08-04 | **Play short description vượt giới hạn — bắt được trước submit, không phải sau.** Câu đã accepted từ 2026-07-28 dài **84** ký tự trên giới hạn **80** của Play, tức trường đó sẽ bị console **từ chối lúc dán vào**, ngay giữa buổi submit. Orchestrator chọn bỏ chữ `built` (78 ký tự) thay vì bỏ `reliable` hay đổi sang `charts` — giữ nguyên claim "reliable page turns" và vẫn vọng lại subtitle `Sheet music for the stand`. Nguyên nhân lỗi lọt được đáng ghi hơn bản thân câu chữ: bảng "Names and short fields" **chỉ ghi giới hạn, không ghi số đếm**, nên không ai có gì để đối chiếu — đếm lại cả bảng thì năm trường còn lại đều trong giới hạn (name 24/30, subtitle 25/30, Play title 24/30, promo text 135/170, keywords 92/100), tức đúng một trường sai và nó là trường duy nhất sát mép. Cột `Limit` đổi thành **`Chars / limit`** với số đo cho từng dòng, cộng một câu dặn đếm lại mỗi khi đổi giá trị. Không có bản nào trên store nên câu này chưa từng được publish; cảnh báo "đừng sửa file này cho quảng cáo" ở đầu file không liên quan — đây là sửa một trường quá độ dài, không phải viết lại tuyên bố nào. Không đụng code. |
| 2026-08-05 | **v1.0.0 đã nộp cho cả hai store, và Orchestrator mở hướng tiếp theo: nhạc công tự dựng SyncMap, BackingTrack và SongPack ngay trong app.** ADR **0019** drafted `proposed`, tier L. Năm ý tưởng ban đầu (thu âm từ mic/line-in; SyncMap có hoặc không có audio, nhiều bản mỗi Score; đóng gói thành song pack; một nơi riêng để bày pack; một PDF chứa nhiều Score) hoá ra là **một nước đi sản phẩm với một thứ tự phụ thuộc gần như ngược lại thứ tự chúng được nêu**. Bốn chốt ở G2 ngay trong lượt: **mở H5 giữ H3/H4 đóng**, **SongPack hai tầng** (gói một bài là đơn vị, gói nhiều bài là tập hợp các gói một bài), **công cụ soạn sống trong app**, và **màn hình riêng cho pack** — chốt cuối ngược khuyến nghị của agent. **Điều đáng ghi nhất là điều làm hướng này khác mọi ADR playback trước:** ADR 0007 giả định BackingTrack là asset có sẵn ai đó gắn vào, ADR 0017 giả định nó là nội dung team sản xuất rồi tải về — cả hai đặt phần sản xuất **ngoài** app. Hướng này đặt nó **vào trong**, và điều đó lật đúng lý do khiến 0017 chuyển `hold`: đường tới hạn của Discover là tốc độ sản xuất bản nhạc, còn tính năng này **không phụ thuộc danh mục nào**, nó chạy được vào ngày ship với thư viện nhạc công đã có. **Ranh giới để mở H5 mà không mở H3 là một câu hỏi chứ không phải một danh sách** (lấy lại từ log 2026-07-29 và nâng thành luật): tính năng chỉ cần biết *chỗ nào trên trang, giây thứ mấy* thì thuộc H5; cần biết *nốt nào* thì vẫn đóng. Theo đó SyncMap/BackingTrack/Transport/lật trang tự động mở, còn MusicXML/Verovio/MidiRealization/WaitMode/OMR đóng nguyên. **Bốn dữ kiện kỹ thuật tra ngày này đổi thiết kế, không cái nào lộ ra từ tài liệu repo.** (1) `flutter_soloud` **4.1.0** thêm `getEngineTime()` + `playScheduled()` + `createMixingBus()`, tức **Transport dựng được bằng Dart, không cần plugin native** — ADR 0004 phải thu hẹp phạm vi về nhánh WebView/MIDI, và ADR 0007 quyết định 6 có điều kiện *"nếu SyncMap thiết lập được mà không cần MusicXML"* nay đã thoả; repo đang pin `^4.0.13` nên chưa có, và **4.1.5 đổi `play`/`setPause`/`stop` sang ném lỗi thay vì nuốt** nên nâng version phải soi lại `MetronomeEngine`. (2) **Tam giác codec:** SoLoud không giải mã AAC/M4A, `record` (gói thu tốt nhất, backend native nên không đụng miniaudio) lại ghi AAC/M4A — gói ghi tốt nhất tạo ra file engine phát không đọc được; `flutter_recorder` ghi Ogg Opus mà SoLoud phát được nhưng README của chính nó đánh dấu iOS "under test". (3) **Không gói nào phơi ra dấu thời gian luồng vào**, `audio_session` có `inputLatency` nhưng tự đánh dấu `UNTESTED` — nên "thu trong lúc nghe click" **không tự khớp**, phải đo bằng cách thu lại chính click của mình. (4) Line-in với tới được bằng dependency đang có (`audio_session.setPreferredInput`, port `usbAudio`/`lineIn`); Android nhận USB như nguồn thu thường từ API 21 nhưng **chỉ UAC1, tối đa stereo**, và người dùng tắt được tự động định tuyến trong Developer Options. **Ba điểm thiết kế đáng giữ lại.** *Một PDF nhiều Score phải đi **đầu tiên*** — không vì nó dễ mà vì mọi thứ sau đều neo vào "trang nào thuộc Score nào", và đổi cái neo sau khi đã có SyncMap là làm hỏng thứ tốn công người nhất; kèm phân biệt **PageExtent** (phạm vi, đặt một lần) khác **PageOrder** (trình tự, sửa thường xuyên), trộn hai thứ là lỗi đắt nhất có thể mắc ở đây. Và nó **không phải tính năng mới mà là sửa một chỗ code đã trôi khỏi glossary từ Spec 0002**: `CONTEXT.md` luôn định nghĩa `PdfDocument` là thứ *thuộc về* Score chứ không *là* Score, chỉ code gộp chúng. *Neo theo **trang** trước, ô nhịp sau* — một bài 10 trang tốn ~10 lần gõ thay vì vài trăm, và cho ra đúng thứ đáng tiền là **lật trang rảnh tay**; playhead trên khuông cần neo mịn hơn và thuộc slice sau, cộng vào chứ không thay thế. *Nhập BackingTrack **trước** thu âm* — đảo thứ tự Orchestrator nêu, giữ nguyên phạm vi: nhạc công phần lớn đã có sẵn file, nhập rẻ hơn một bậc độ lớn, và thu âm sinh ra **đúng cái artefact** mà nhập sinh ra nên làm nhập trước là Transport đã qua G4 trước khi chạm phần khó nhất; món quà ở chiều ngược lại là **thu trong lúc nghe click thì SyncMap có sẵn theo cấu trúc**. **Hai cái giá được ghi thẳng thay vì giấu.** Công cụ soạn trong app **nhân đôi** `useTappingSystem`/`SyncWorkspaceClient` bên web, tức cộng vào đúng câu hỏi sản phẩm mà ADR 0014 để mở (4.463 dòng PdfViewer trùng lặp); điều kiện để chấp nhận được là **trùng giao diện thì được, trùng dữ liệu thì không** — SyncMap phải dịch không mất mát sang `TimemapEntry` + `MeasureBox`, nếu không thì thứ bị nhân đôi không còn là một màn hình mà là cả kho nội dung. Và màn hình riêng cho pack đi ngược phương án ADR 0017 đã **bác** ("kệ riêng tách khỏi library" — hai vòng đời cho một khái niệm); ADR 0019 nhận lựa chọn của Orchestrator kèm một bất biến: **lối vào thứ hai, không phải vòng đời thứ hai** — Score đến từ pack là Score bình thường, vào Setlist được, gán Label được, backup được, không thư mục riêng trên đĩa, không đường mở nào chỉ đi qua màn hình pack. **Ba việc gom lại:** `formatVersion` lên 2 **đúng một lần** cho ba thứ đang cùng chờ (sao lưu chọn lọc nợ từ 0050, Discover lưu tham chiếu theo 0017 dec 8, layout đĩa đổi vì PdfDocument tách ra); sao lưu chọn lọc **thôi là tuỳ chọn** vì một bản thu 4 phút là ~5,8 MB AAC nhưng ~42 MB WAV; và **audio session phải có đúng một chủ** vì sau nhóm này có ba plugin cùng chạm nó (SoLoud, gói thu, `google_mobile_ads` của 1.1) — hỏng ở đây hiện ra dưới dạng "thu âm im lặng trên vài máy" chứ không dưới dạng test đỏ. Trình tự chín slice (0052–0060), **hai spike đứng trước hai slice audio** và spike thu âm có quyền kết luận *chưa làm được* — đó là kết quả hợp lệ. **Slice 1 chạy song song được với việc chờ v1 qua review** (không audio, không mạng, không tiền); từ slice 4 trở đi thì không, vì nếu store từ chối thì đường sửa là quay lại đúng bản build hiện tại. Security Review cần cho slice 7 (quyền mic) và 8 (tệp rời khỏi máy, có thể chứa nội dung có bản quyền), không cần cho 1–3. Ước lượng thẳng: **6–12 tháng**, không nén được bằng song song vì chuỗi phụ thuộc là thật. Sáu câu hỏi G2 còn mở, nặng nhất là câu 6 (team dựng danh mục bằng công cụ nào) và câu 4 (SongPack chia sẻ chỉ qua share sheet OS — nới ra sau là một sản phẩm khác). Không đụng code. |
| 2026-08-05 | **ADR 0019 revision 1 sau ba câu chất vấn của Orchestrator — và câu thứ nhất bác một khuyến nghị của agent vì một lỗi lập luận thật, không vì khẩu vị.** *SyncMap phải chi tiết tới từng phách.* Bản nháp khuyến nghị neo theo trang trước với lý lẽ chi phí (một bài 10 trang tốn 10 lần gõ thay vì vài trăm); **lý lẽ đó định giá một cách soạn mà thiết kế này không dùng**. Nó giả định mọi thứ được gõ theo thời gian thực nên độ mịn nhân thẳng vào số lần gõ. Mô hình Orchestrator đưa ra tách làm hai tầng và ở tầng dưới độ mịn gần như miễn phí: **MeasureMap là hình học** (ô nhịp nằm ở đâu trên trang, **vẽ** ra) còn **SyncMap là thời gian** (ô nhịp vang lên lúc nào). Chia một RowBox thành 4 ô nhịp là **một** thao tác chứ không phải bốn, chia MeasureBox thành 4 phách cũng vậy — chi phí chỉ phát sinh ở chỗ *không đều*. Phần thưởng cũng lớn hơn bản nháp tưởng: lật trang thành "lật vào đúng phách có dấu lặng ở cuối dòng cuối", và chỉ báo vị trí chạy được trên **đúng dòng đang chơi**. Mô hình này **trùng gần như 1:1 với repo web** — `MeasureBox` (pageIndex/measureNumber/systemIndex/toạ độ 0..1) và `beatSplits` là đúng tên đúng nghĩa — nên phép dịch của quyết định 6 rút xuống thành một test round-trip sáu hàng, và nó là bằng chứng mạnh rằng mô hình của Orchestrator đúng chứ không chỉ khác. Ba thứ nhặt thêm khi viết. **Chiều rộng là *vị trí*, không phải *thời lượng*** — câu "kéo dãn width thể hiện độ dài của beat" đọc được hai nghĩa và chỉ một nghĩa đứng vững, vì khắc nhạc **không tỉ lệ với thời gian** (một ô nhịp nốt tròn và một ô nhịp mười sáu móc kép dài bằng nhau mà rộng khác hẳn); suy thời lượng từ chiều rộng cho ra triệu chứng "playhead lúc nhanh lúc chậm tuỳ bài", loại lỗi tốn nhiều ngày nhất để lần ra. **Lớp vẽ của Spec 0017–0019 đã dùng đúng hệ toạ độ MeasureMap cần** — tỉ lệ 0–1 trên số trang PDF 1-based, y hệt `MeasureBox` bên web — nên không phải phát minh hệ toạ độ, lớp overlay hay xử lý zoom; cả ba đã qua G4. Và **một hard stop mới, vì đây là con dốc trơn nhất của cả ADR**: dò tự động vạch nhịp hay dòng kẻ từ ảnh trang **là OMR**, thuộc ADR 0006 và H6, ADR này không mở; hai cách giảm công được phép là **chép layout từ trang trước** (phần lớn trang trong một bài có cùng số dòng cùng vị trí) và **mỗi lần chỉ map một bài** (hệ quả của quyết định 2). *Nhập SongPack có lẫn vào danh sách Score thì user có rối không?* — trả lời hai vế, và vế hai mới quan trọng: không cho hiện ở đó thì **hỏng thứ nặng hơn** (không xếp được vào Setlist cạnh PDF của chính mình, không search, không Label, không sort — đúng lý do ADR 0017 đã bác "kệ riêng"), còn nỗi lo thì có thật nhưng **tên chính xác của nó không phải "lẫn" mà là "mười hai hàng mới hiện ra cùng lúc mà không gì nói chúng đến với nhau"** — tức lỗi nguồn gốc và nhóm, chữa bằng bốn thứ rẻ: **nhập có xem trước** (không gì hiện ra bất ngờ), dấu nguồn gốc trên mỗi Score (trường ADR 0017 dec 2 vốn đã đòi), gom nhóm/lọc theo pack dùng lại cơ chế Label filter của 0021, và màn hình SongPack. Câu chốt: **pack không phải nơi *chứa* Score, nó là *thuộc tính* của Score.** Thêm một chi tiết phải chốt ở Spec: nhập lại cùng pack lần hai thì **hỏi, mặc định bỏ qua bài đã có** — ghi đè im lặng sẽ xoá annotation nhạc công đã viết lên bản nhập trước. *Làm sao user tách một PDF thành nhiều Score, và Library hiển thị thế nào?* — **một dữ kiện tra được đổi hẳn chi phí câu này: `pdfrx` có `PdfDocument.loadOutline()` trả về `PdfOutlineNode` mang cả `title` lẫn trang đích** (kiểm trong `pdfrx 2.4.7` / `pdfrx_engine 0.4.6`). Tuyển tập và fake book gần như luôn có mục lục, nên app **đề xuất sẵn cả ranh giới lẫn tên bài** — năm mươi bài tách xong trong một phút thay vì một buổi. Hai lối vào (hỏi lúc nhập nếu PDF nhiều trang; một mục `⋯` trên Score đã có, vì thư viện hiện tại đã đầy sách nằm dưới dạng một Score), màn hình tách là **lưới thumbnail** chứ không phải hộp nhập số trang (người ta nhận ra chỗ bắt đầu một bài bằng cách *nhìn thấy* tiêu đề — Spec **0048** đang `hold` đã nghĩ sẵn phần lớn hình dạng này). Trong Library mỗi bài là một hàng Score bình thường với số trang **của nó** và thumbnail **trang đầu của nó**; **PdfDocument không thành công dân hạng nhất** — cho tệp một chỗ đứng là dựng lại đúng mô hình trình duyệt tệp mà app cố ý không có, nguồn gốc chỉ hiện ở ScoreMenu dạng "Trang 12–19 của `Chopin Etudes.pdf`". **Ba hành vi đã ship phải đổi và cả ba là chỗ dễ mất dữ liệu:** xoá (0028) phải đếm tham chiếu, không thì hoặc xoá mất PDF của bài khác hoặc để lại tệp mồ côi; **Replace PDF (0024) nay ảnh hưởng mọi Score dùng chung tệp** và số trang mới có thể làm một PageExtent vô nghĩa; thumbnail phải dựng trang đầu của PageExtent chứ không phải trang 1, nếu không mười hai bài trong một cuốn có mười hai thumbnail giống hệt nhau. Cũng ghi thêm: **mọi thứ lưu xuống đĩa giữ số trang tuyệt đối của PdfDocument**, không giữ số tương đối trong PageExtent — số tương đối sẽ âm thầm dịch mọi annotation khi sửa PageExtent. Và **MeasureMap là dữ liệu tốn công người nhất app từng lưu** (annotation vẽ lại mất mười giây, MeasureMap mất một buổi), nên nó đổi trọng số của backup, Replace PDF và mọi đường có thể xoá nó. Trình tự từ 9 lên **11 slice** (0052–0062) vì SyncMap tách thành ba (MeasureMap, tính ra, gõ tay); **bốn slice đầu không một byte nào rời máy, không SDK nào** và cộng lại đã là sản phẩm đứng được (tách đúng bài, nhảy tới ô nhịp, metronome đi theo bài, lật trang rảnh tay). Slice 2 vốn có nguy cơ là slice hình-dạng-tầng nên **nhảy tới ô nhịp** được kéo vào chính nó. Ước lượng lên **9–15 tháng**. `MeasureAnchor` của ADR 0017 dec 6 **bị rút** — `MeasureBox` thay chỗ và trùng tên với bên kia; `CONTEXT.md` nay nhận sáu term ở G1, cộng một sửa: định nghĩa `SyncMap` hiện tại nói "khớp thời gian nhạc với thời gian audio" mà quyết định 3 vừa cho phép SyncMap **không có audio nào**. Tám câu hỏi G2 (thêm câu 7 `RowBox` hay `SystemBox` — khuyến nghị `SystemBox` cho đúng thuật ngữ khắc nhạc và khớp `systemIndex`; và câu 8: MeasureMap **chưa đầy đủ** có dùng được không — khuyến nghị có, vì bắt vẽ xong cả bản nhạc mới cho metronome đi theo bài là quá đắt). Không đụng code. |
| 2026-08-05 | **ADR 0019 revision 2: sáu câu G2 chốt, `VISION.md` viết lại, và một bổ sung một dòng của Orchestrator — *SongPack có thể đến từ tác giả khác, bán ra ngoài* — đổi nhiều thứ hơn mọi câu G2 cộng lại.** Sáu câu chốt: **sửa VISION ngay** (đã làm), **chờ chứ không cắt** nếu spike thu âm kết luận chưa làm được, **khoá tempo** khi có BackingLane, **chia sẻ chỉ qua share sheet OS**, **`SystemBox`** thay `RowBox`, **cho phép MeasureMap chưa đầy đủ**. `VISION.md` nhận G0 lần hai: đảo trụ cột (*Play along* lên trước *Smart Score*), bảng horizon viết lại để diễn đạt được "H5 mở, H3/H4 đóng", nói rõ thu âm trong app là ghi nhanh chứ không thay phòng thu, và **dịch sang tiếng Việt** theo ADR 0015 — lần đầu cần một quyết định từ VISION kể từ khi 0015 accepted. **Bổ sung về tác giả bên thứ ba nghe như một trường siêu dữ liệu nhưng nó đổi hạng của định dạng:** nhạc công tự xuất tự nhập thì SongPack là **tiện ích nội bộ**, hỏng thì sửa ở bản sau; từ lúc một người lạ **bán** một pack thì nó là **hợp đồng công khai** và mọi bản app về sau phải đọc được thứ họ bán hôm nay. Bốn thứ phải có ngay từ phiên bản đầu vì thêm sau là phá tương thích: **siêu dữ liệu tác giả và gói** (tên, giới thiệu, ảnh bìa, giấy phép tự khai); **hiển thị như *lời khai của gói* chứ không phải lời bảo chứng của app**, vì app không xác minh được tác giả có thật hay có quyền với bản nhạc bên trong — chi tiết từ ngữ với hệ quả pháp lý; **hai số phiên bản** (định dạng + **app tối thiểu**), thiếu cái thứ hai thì app cũ nhập nửa vời rồi im lặng bỏ qua phần nó không hiểu, mất SyncMap mất BackingTrack mà người dùng tưởng xong; và **không DRM, không kích hoạt, không gọi về máy chủ** — phải ghi thành lời **trước khi** tác giả bán hàng hỏi xin điều ngược lại, vì một lần kiểm giấy phép qua mạng là phá bất biến offline mà cả sản phẩm đứng trên. **Quyết định 13 mới, và đây là rủi ro lớn nhất của bổ sung này: app là *nơi nhận trung lập*, và ranh giới đó là ranh giới App Review.** Chừng nào app chỉ **nhận** thì nó cùng loại với nhập một PDF đã mua — không nghĩa vụ nào phát sinh; thứ đổi loại là app **dẫn người ta đi mua** (Apple 3.1.1). Bốn thứ nghe vô hại nằm ngoài phạm vi và phải bị từ chối bằng phản xạ: danh mục pack dù chỉ là danh sách tĩnh, nút "tìm thêm gói", liên kết tới gian hàng tác giả **kể cả khi nó nằm trong siêu dữ liệu do tác giả tự khai**, và bất kỳ chỗ nào nói nơi bán và giá. Nên trường liên hệ phải xử lý **như văn bản, không như liên kết** — tác giả điền URL gian hàng vào ô giới thiệu là chuyện chắc chắn xảy ra; hiển thị thành chữ là thuật lại, biến thành nút bấm được là dẫn đường. Kèm theo: **StageScore không ăn chia, không có quan hệ thanh toán với tác giả** — ngày nào có phần trăm doanh thu thì lập tức có nghĩa vụ IAP, kiểm duyệt nội dung và hỗ trợ hoàn tiền. Bảng phân biệt với Discover (ADR 0017) viết vào ADR vì hai thứ trông giống nhau mà ngược nhau ở cả năm chiều (nơi bán, thanh toán, mạng, ai làm nội dung, nghĩa vụ review); `no_ads` của 0018 dec 12 không mở khoá cái nào. **Ba hệ quả kỹ thuật mới.** *Nhập pack không được dùng lại thẳng cơ chế giải nén của `LibraryBackup`* — `restoreBackup` giả định tệp do chính người dùng tạo từ máy mình, giả định hợp lý cho việc nó làm và **hoàn toàn sai** cho pack mua từ người lạ; phải chặn **zip-slip**, chặn tỉ lệ nén bất thường và tổng dung lượng sau giải nén, kiểm định dạng/kích thước **ảnh bìa** trước khi decode, và chịu được PDF/audio hỏng mà không sập. *"Nhập lại pack lần hai" thôi là ca biên và thành đường đi chính* — tác giả sửa một nốt sai rồi phát hành 1.1 cho người đã mua, nên cập nhật phải giữ lại annotation/SyncMap/PageOrder của người dùng, và chỗ khó là **nếu tác giả đổi PDF làm số trang đổi** thì mọi annotation neo theo trang đều lệch; Spec phải trả lời, "cứ ghi đè" không chấp nhận được. *Công cụ soạn MeasureMap vừa thành công cụ **sản xuất*** — tác giả bên ngoài không có công cụ nào khác, họ dựng pack bằng chính app này, nên chất lượng của nó đặt **trần** cho chất lượng mọi pack từng tồn tại; tiêu chuẩn không còn là "đủ để nhạc công map bài của mình" mà là "đủ để người lạ làm ra thứ bán được", và phải biết từ Spec 0053 chứ không phát hiện ở slice cuối. Đây cũng là câu trả lời cho G2 câu 6 (Orchestrator hỏi ngược *cách nào tốt nhất cho user*): **team dùng công cụ trong app**, vì nếu team dựng danh mục bằng công cụ web thì người duy nhất phát hiện ra chỗ thô sẽ là một tác giả lạ đang cố làm thứ bán được — muộn nhất và đắt nhất có thể. **Thu âm tụt từ slice 9 xuống slice 11, SongPack lên thay chỗ** — thay đổi thực chất, không phải xếp lại cho gọn. Bốn dữ kiện cùng hướng: Orchestrator đã chốt "chờ" nếu spike hỏng, chính Orchestrator ghi nhận nhạc đệm làm bằng phần mềm chuyên nghiệp tốt hơn, tam giác codec chưa có lối thoát chắc, và nay tác giả bên ngoài sẽ mang vào bản đệm làm ở phòng thu. Thu âm là slice **yếu nhất và bấp bênh nhất**: để giữa chuỗi thì nó chặn SongPack đứng sau, để cuối thì cắt được hoãn được mà không gì mắc kẹt. Đổi lại SongPack — thứ vừa thành **hạng mục đòn bẩy lớn nhất cả roadmap** — về đích sớm hơn hai slice. Lý do nó là đòn bẩy lớn nhất: **người ngoài sản xuất và tự bán thì danh mục lớn lên mà ta không sản xuất, không dựng cửa hàng, không nhận nghĩa vụ IAP** — đúng bài toán đã làm ADR 0017 phải `hold` (danh mục bằng không, tốc độ sản xuất là đường tới hạn), giải mà không tốn gì; cái giá là không kiểm soát chất lượng và không thu đồng nào, đổi lại cũng không gánh chi phí nào. Nếu 0017 mở lại thì phải nhìn thẳng: Discover khi đó **cạnh tranh với chính những tác giả** mà định dạng này mời vào. **Màn hình pack: khuyến nghị tên đảo lại.** Bản trước nghiêng **Studio** với lập luận *bàn làm việc* (bài nào đã map, còn thiếu gì); lập luận ấy yếu đi vì phần lớn nội dung rồi sẽ là **gói người khác làm xong, mua về để chơi** — gọi kệ sách của mình là "Studio" thì sai. Nay khuyến nghị **`Packs`**, và lý do mạnh nhất không phải nghĩa của từ mà là chỗ nó đứng: **Scores** và **Setlists** đều là term trong `CONTEXT.md`, một tab thứ ba không phải term sẽ là ngoại lệ duy nhất và buộc đẻ thêm một từ chỉ sống trên thanh điều hướng. Phản bác cũ của chính agent (*"phần lớn thứ trong đó chưa phải pack"*) không còn đúng sau bổ sung này. Lời hứa viết thành một câu để Spec bám vào: *"những gói bạn có — mua về hoặc tự dựng — và trong mỗi gói có gì."* Màn hình bày **pack có trạng thái** (mua về thì đầy đủ, tự dựng thì còn lỗ hổng), không bày Score chưa từng định thành pack. Một câu G2 mới mở: siêu dữ liệu tác giả có cần **chữ ký số** không — khuyến nghị **không** cho bản đầu (không có đường phân phối khoá công khai, lòng tin do nền tảng bán hàng gánh), mở lại nếu có bằng chứng thật về mạo danh. Security Review đổi trọng tâm: **slice 8 (SongPack) nay nặng hơn slice 11 (thu âm)**. ADR giữ `proposed`: 13 quyết định, 6/8 câu G2 đã chốt, còn câu 5 (tên màn hình) và 6 (công cụ của team) đang chờ Orchestrator duyệt khuyến nghị, cộng câu chữ ký số. Không đụng code. |
| 2026-08-05 | **Spec 0052 drafted `proposed` (tier L) cho slice 1 của ADR 0019 — và đợt đọc repo tìm ra một câu của ADR nói sai về chính hiện trạng nó mô tả.** Quyết định 2 viết *"mọi thứ lưu xuống đĩa — annotation, Bookmark, MeasureBox — giữ số trang tuyệt đối của PdfDocument"* bằng giọng mô tả. Đọc code thì **ba quy ước đang cùng tồn tại**, không phải một: annotation (`annotation_store.dart:24`) và `PageOrderEntry.sourcePage` (`page_order.dart:32`) neo theo **trang PDF tuyệt đối**, nhưng `JumpLink.originPage`/`destinationPage` neo theo **vị trí trong PageOrder** (`jump_link.dart:16,19` nói thẳng ra thế) và `Bookmark.pageNumber` cũng vậy vì nó ghi từ `_pageNumber` = `_navPage.clamp(1, _pageOrder.length)` (`pdf_mode_screen.dart:1153-1157`, `bookmarks_sheet.dart:67`) — `Bookmark.pageNumber` lại là trường **duy nhất không có doc comment**, và đó chính là lý do dữ kiện này suýt lọt. Nên câu của ADR không phải mô tả mà là **một luật mới**, và áp thẳng nó lên Bookmark/JumpLink là **di trú có mất mát**. Khuyến nghị của Spec là **thu hẹp luật thay vì di trú**, với hai lý do đọc từ code chứ không từ khẩu vị: `PageOrder.duplicate()` (`page_order.dart:96-105`) cho phép **một trang PDF xuất hiện nhiều lần** trong chuỗi biểu diễn — đó là cách app giải D.C. và đoạn lặp — nên một JumpLink nói *"tới ô này thì nhảy sang ô kia"* mà hai ô có thể là **cùng một tờ giấy**; chuyển sang trang tuyệt đối là làm nhoè đúng phân biệt mà tính năng ấy sống nhờ. Và `PageOrderEntry.blank()` là một ô **không có trang PDF nào**, nên phép chuyển không phải song ánh. Luật đúng: **thứ neo vào tờ giấy** (annotation, page scale, `sourcePage`, MeasureBox sắp tới) giữ trang tuyệt đối; **thứ neo vào chuỗi biểu diễn** (Bookmark, JumpLink) giữ vị trí PageOrder — thứ quyết định 2 thật sự cấm là **quy ước thứ ba**, tức "số trang tương đối trong PageExtent", và luật này cấm đúng nó. **Năm dữ kiện khác đổi thiết kế slice.** (1) Một kho **thứ năm** cũng neo theo trang mà ADR không liệt kê: `PageScalePrefs.pageScales` khoá `"$scoreId:$sourcePage"` (`page_scale.dart:34,40-41`) và nằm trong file **app-level** dùng chung, nên tách một Score là mồ côi mọi override scale của cuốn sách — ADR ghi ba hành vi đã ship phải đổi, đây là cái **thứ tư**. (2) Thumbnail khoá theo `scoreId` + **mtime** (`score_thumbnails.dart:85-89`): mtime **không đổi khi PageExtent đổi**, nên sửa extent sẽ không làm mới ảnh — PageExtent phải vào **khoá**, không chỉ vào tham số render. (3) `loadOutline()` **có thật** và `PdfDest.pageNumber` cho **thẳng** số trang 1-based (`pdf_dest.dart:4-15`, dựng từ `pageIndex + 1`), nhưng `dest` **nullable** khi `FPDFDest_GetView` trả `type == 0`, và cây outline **đệ quy** — một tuyển tập chia chương cho cây hai tầng chứ không phải danh sách phẳng. (4) `library.json` **không có số phiên bản nào** (`score_library.dart:234-238`), nên di trú phải nhận ra manifest cũ bằng **sự vắng mặt của một trường**. (5) `_validateMarkerAt` từ chối theo luật *mới hơn thì từ chối* (`library_backup.dart:529-535`), nên bump lên `2` chặn được app cũ — nhưng **chiều ngược lại chưa ai viết**: bản mới đọc backup `version: 1` sẽ **qua cửa** và đổ manifest đời cũ vào code đang mong đợi `PdfDocument`, nên di trú phải chạy **sau mỗi lần restore**, không chỉ lúc mở library. **Ba khuyến nghị đáng nhớ ngoài câu 1.** *Không di chuyển một file PDF nào lúc di trú* — `PdfDocument.id` nhận đúng `scoreId` cũ, `relativePath` giữ nguyên, import mới ghi vào `documents/<docId>.pdf`, hai layout sống chung vĩnh viễn vì **đường dẫn là dữ liệu** (`absoluteFile` đã luôn đọc `relativePath`); di chuyển là bước duy nhất trong cả slice có thể hỏng giữa chừng trên thư viện vài GB và để lại một library mở ra không thấy gì. *Không hỏi "một bài hay tuyển tập" trong luồng nhập* — ADR quyết định 11 va phải một dữ kiện nó không biết là import nhận **nhiều file một lượt** (`allowMultiple: true`, `library_screen.dart:741-746`), nên câu hỏi đó thành mười hai hộp thoại liên tiếp; thay bằng **một** thanh gợi ý sau khi nhập cho file trông như tuyển tập (có mục lục ≥ `2` đích, hoặc > `30` trang), giữ trọn lời hứa "người chỉ nhập một bài không thấy thêm bước nào". *Tách một Score đã có thì Score gốc **còn**, thành bài thứ nhất* — giữ `id`, giữ annotation, giữ Label, giữ **chỗ trong mọi Setlist**; phương án xoá-rồi-tạo-mới làm bản nhạc im lặng rơi khỏi mọi Setlist nó đang nằm trong. Kèm một chốt dữ liệu: annotation nằm ngoài PageExtent mới thì **giữ trên đĩa, không hiển thị** — vì neo tuyệt đối nên thu hẹp rồi nới lại là hiện lại nguyên vẹn, còn PageOrder thì phải nhất quán nên ô trỏ ra ngoài bị bỏ và người dùng được báo **số lượng trước** khi xác nhận. `Score.pageCount` chuyển sang **tính ra** từ extent (số học thuần, không mở tệp) còn `PdfDocument.pageCount` **lưu lại** — giữ cả hai dạng lưu trữ là mời một cặp giá trị mâu thuẫn vào đúng thứ Library hiển thị trên mọi hàng. Một ranh giới viết thẳng vào Spec vì đây là con dốc trơn: **đọc mục lục là đọc siêu dữ liệu có cấu trúc của tệp, không phải nhìn vào trang giấy** — dò ranh giới bài từ *ảnh* trang là OMR, thuộc ADR 0006 và H6, và không được lẻn vào đây. 10 câu hỏi G3, Security Review **không cần** (ADR 0019 ghi rõ chỉ slice 8 và 11 cần). Baseline đo lại trước khi viết: **365 test xanh**, `flutter analyze` sạch. Không đụng code. |
| 2026-08-05 | **Spec 0052 accepted (G3) với cả mười khuyến nghị như bản nháp — và việc accept nó buộc ADR 0019 nhận revision 3 ngay trong lượt, vì bốn chỗ của ADR nói sai về code đang ship.** Không chỗ nào đổi **hướng** của ADR; cả bốn sai theo cùng một hướng và đó là phần đáng ghi: **chúng nói về hiện trạng bằng giọng mô tả trong khi thật ra đang đề xuất một luật mới, và đều cho rằng hiện trạng sạch hơn thực tế.** (1) **Quyết định 2** tưởng có một quy ước số trang, thực tế có **ba** — nay viết thành **hai không gian**: thứ neo vào tờ giấy (annotation, page scale, `PageOrder.sourcePage`, MeasureBox sắp tới) giữ trang PDF tuyệt đối; thứ neo vào chuỗi biểu diễn (Bookmark, JumpLink) giữ vị trí PageOrder như hôm nay; quy ước **thứ ba** — tương đối theo PageExtent — vẫn bị cấm, tức ý định ban đầu còn nguyên. Lý do không di trú hai kho kia là hai tính chất của code đang ship, không phải khẩu vị: `PageOrder.duplicate()` cho **một trang PDF xuất hiện nhiều lần** trong chuỗi (đó là cách app giải D.C. và đoạn lặp) nên một JumpLink có thể trỏ hai ô là **cùng một tờ giấy** — quy về trang tuyệt đối làm nhoè đúng phân biệt tính năng ấy sống nhờ; và `PageOrderEntry.blank()` là ô **không có trang PDF nào**, nên phép chuyển không phải song ánh. Ràng buộc kèm theo: **mỗi model có số trang phải có doc comment nói nó thuộc không gian nào** — `Bookmark.pageNumber` là trường **duy nhất không có comment** và đó chính là lý do dữ kiện này suýt lọt qua cả ADR lẫn hai vòng chất vấn. (2) **Quyết định 11** đếm **ba** hành vi đã ship phải đổi, thực tế **bốn**: cái thứ tư là **page scale theo trang** (Spec 0031), và nó là kho neo theo trang **duy nhất nằm trong một file dùng chung** — `PageScalePrefs.pageScales` khoá `"$scoreId:$sourcePage"` trong `page_scale_prefs.json` app-level, nên tách một Score làm mọi override scale của cuốn sách **mồ côi ngay lúc tách**. Cùng quyết định ấy còn hai chỗ: thumbnail phải đưa PageExtent vào **khoá cache** chứ không chỉ vào tham số render, vì **mtime không đổi khi PageExtent đổi** nên sửa extent sẽ không làm mới ảnh; và **câu hỏi "một bài hay tuyển tập" phải đứng sau khi nhập, không nằm trong luồng nhập**, vì import nhận **nhiều file một lượt** (`allowMultiple: true`) nên câu hỏi đó thành mười hai hộp thoại liên tiếp — thay bằng **một** thanh gợi ý cho file trông như tuyển tập (mục lục ≥ `2` đích, hoặc > `30` trang), giữ trọn lời hứa "người chỉ nhập một bài không thấy thêm bước nào". (3) **Quyết định 10** chỉ nhìn một chiều của phép kiểm version: `_validateMarkerAt` từ chối theo luật *mới hơn thì từ chối* (`version > formatVersion`), nên bump lên `2` chặn được app cũ nhưng bản **mới** đọc backup `version: 1` sẽ **qua cửa** và đổ manifest đời cũ vào code đang mong đợi `PdfDocument`; từ chối nó là mất dữ liệu với đúng người có backup, nên di trú phải là **hàm của library root**, idempotent, gọi ở **cả hai** chỗ — lúc mở library và **ngay sau mỗi lần restore**. Kèm dữ kiện là `library.json` **không có số phiên bản nào**, nên di trú nhận ra manifest cũ bằng **sự vắng mặt của một trường**. (4) `loadOutline()` được xác nhận có thật và `PdfDest.pageNumber` cho **thẳng** số trang 1-based, nhưng hai hình dạng phải chịu được mà chữ "cây mang title và trang đích" không lộ ra: `dest` **nullable** (một nút mục lục có thể không có trang đích) và cây **đệ quy** — bỏ sót vế đầu là crash trên tệp thật, bỏ sót vế sau là mất toàn bộ bài của một tuyển tập có chương. Ranh giới OMR được viết lại ở đúng chỗ dễ mờ nhất: **đọc mục lục là đọc siêu dữ liệu có cấu trúc của tệp, không phải nhìn vào trang giấy** — dò ranh giới bài từ *ảnh* trang vẫn thuộc ADR 0006 và H6. Ba khuyến nghị khác của Spec cũng thành quyết định: **không di chuyển một file PDF nào lúc di trú** (`PdfDocument.id` = `scoreId` cũ, `relativePath` giữ nguyên, import mới ghi `documents/<docId>.pdf`, hai layout sống chung vì **đường dẫn là dữ liệu**) — di chuyển là bước duy nhất có thể hỏng giữa chừng trên thư viện vài GB; **tách một Score đã có thì Score gốc còn, thành bài thứ nhất**, giữ `id`/annotation/Label/**chỗ trong mọi Setlist**, vì phương án xoá-rồi-tạo-mới làm bản nhạc im lặng rơi khỏi mọi Setlist; và **annotation ngoài PageExtent mới thì giữ trên đĩa, không hiển thị** — neo tuyệt đối nên thu hẹp rồi nới lại là hiện lại nguyên vẹn, còn PageOrder phải nhất quán nên ô trỏ ra ngoài bị bỏ và người dùng được báo **số lượng trước** khi xác nhận. `Score.pageCount` chuyển sang **tính ra** từ extent (số học thuần, không mở tệp), `PdfDocument.pageCount` **lưu lại** — giữ cả hai dạng lưu trữ là mời một cặp giá trị mâu thuẫn vào đúng thứ Library hiển thị mọi hàng. **Slice 1 vẫn chưa được build:** ADR 0019 còn `proposed`, tier L cần G2 accept trước dòng code đầu tiên và G3 của Spec không thay thế được — đó là hạng mục duy nhất còn chặn, không phải một dependency kỹ thuật nào. Không đụng code. |
| 2026-08-05 | **Ba câu G2 cuối của ADR 0019 chốt, cả ba theo khuyến nghị — không còn câu nào mở ở G2.** Màn hình thứ ba tên **`Packs`** (`Studio` bị bác); **team dựng danh mục bằng công cụ trong app**; **không chữ ký số** cho siêu dữ liệu tác giả. Phần đáng ghi không phải ba lựa chọn mà là **hai ràng buộc suy ra từ chúng**, cả hai chặn đúng chỗ mà lựa chọn ấy sẽ bị "sửa cho gọn" về sau. **(1) `Packs` không phải số nhiều của term `SongPack`, và lập luận chọn nó lại dựa vào chuyện đó.** Lý lẽ mạnh nhất cho `Packs` là hai tab hiện có — **Scores**, **Setlists** — đều là số nhiều của term trong `CONTEXT.md`, nên một nhãn không phải term sẽ là ngoại lệ duy nhất; nhưng term ở đây là `SongPack`, nên nhãn `Packs` **chính là** cái ngoại lệ mà lý lẽ ấy muốn tránh. Hai đường thoát đều tệ hơn: đổi term thành `Pack` làm mất chữ phân biệt nó với mọi thứ khác gọi là "gói" (và `SongPack` đã nằm trong format id, phạm vi Security Review, bảng Trình tự), còn dán `Song packs` lên thanh điều hướng thì dài và không ai nói thế. Luật chốt: **`SongPack` trong `CONTEXT.md`/code/tài liệu, `Packs` chỉ trên thanh điều hướng** — đúng hình dạng ADR 0010 đã đặt cho thương hiệu (*Backing & Score* cho người đọc, `backingscore` trong identifier), nên là tiền lệ có sẵn chứ không phải ngoại lệ mới; định nghĩa `SongPack` trong `CONTEXT.md` phải ghi kèm cái nhãn để người đọc `library_screen.dart` biết hai cách viết là **cố ý**. **(2) "Không chữ ký số" nghe như bớt việc nhưng nó *tăng* trọng số của Security Review slice 8.** Không có gì chứng minh được pack chưa bị sửa trên đường đi, nên phần kiểm tra khi đọc tệp — zip-slip, tỉ lệ nén và tổng dung lượng sau giải nén, kiểm ảnh bìa trước khi decode, chịu được PDF/audio hỏng — thôi là lớp phòng thủ *đầu tiên* và thành lớp **duy nhất**; mọi byte trong pack là dữ liệu không tin được. Hệ quả cụ thể cho đường "cập nhật pack đã cài" (quyết định 8): app **không xác minh được** bản 1.1 đúng là của tác giả đã bán bản 1.0, nên thứ giữ rủi ro trong tầm là cập nhật vẫn phải là **lựa chọn có xem trước** và vẫn giữ lại phần của người dùng — Spec 0059 không được biến nó thành đường tự động. Câu 6 cũng được ghi thành lời đầy đủ vì nó đặt **trần chất lượng**: tác giả bên ngoài không có công cụ nào khác, nên chất lượng công cụ soạn trong app đặt trần cho mọi pack từng tồn tại, và nếu team dùng công cụ web thì người duy nhất phát hiện ra chỗ thô sẽ là một tác giả lạ đang cố làm thứ bán được — muộn nhất và đắt nhất có thể; điều này **không** cho phép bỏ phép dịch hai chiều ở quyết định 6. ADR 0019 nay **13 quyết định, 9/9 câu G2 đã chốt, phần "Còn mở" đã xoá** — nhưng **vẫn giữ `proposed`**: chữ ký G2 là việc của Orchestrator và trả lời hết câu hỏi không thay thế được nó (đúng tiền lệ Spec 0051, nơi 13/13 câu G3 đã trả lời mà header vẫn ghi "chữ ký `accepted` còn chờ"). Đó vẫn là hạng mục duy nhất chặn build slice 0052. Không đụng code. |
| 2026-08-05 | **Spec 0052 build xong nửa sau — màn hình tách, PdfMode tôn trọng PageExtent, dòng nguồn gốc trong ScoreMenu. `flutter analyze` sạch, **477** test xanh (365 trước slice, 416 sau nền móng).** Nửa trước đã có `PdfDocument`/`PageExtent`, di trú, đếm tham chiếu, thumbnail theo extent, page scale, backup `formatVersion: 2`. Lượt này thêm sáu thứ, và **năm chỗ đáng ghi đều là chỗ code buộc phải nói ra một phân biệt mà Spec chỉ ngụ ý.** (1) **`PageOrder` học được khái niệm "trang đầu của bài"**, tức `sourceFirstPage` mới bên cạnh `sourcePageCount` — nếu không có nó thì một PageOrder plain của bài trang 121–150 sẽ đánh số ô 1..30 rồi đi tìm trang 1..30 của tệp, và triệu chứng là mở bài thứ ba ra thấy bài thứ nhất. Kèm `restrictedTo` + `slotsOutside`, và `PageOrderStore.loadStored()` **tách khỏi** `loadOrIdentity` vì "chưa có tệp nào" và "một chuỗi nhạc công đã dựng" là hai câu trả lời khác nhau cho cùng câu hỏi mà việc đổi extent đặt ra — chuỗi plain thì không có gì để mất, nên không phải hỏi. (2) **Bàn giao là hàm rời, không phải method của `ScoreLibrary`** (`split_handover.dart`): page scale nằm trong file prefs app-level và PageOrder nằm trong file per-Score, không cái nào là việc của manifest, và kéo chúng vào là buộc **cái writer duy nhất phải atomic** phụ thuộc vào mọi store trong app. (3) **`loadPdfOutline()` theo đúng idiom `pdf_first_page.dart`** — trả `null` thay vì ném, nhận vào dạng hàm inject, nên toàn bộ test màn hình tách chạy **không có một PDF nào**; nút outline không có trang đích thì **giữ chỗ trong cây** chứ không bị bỏ, vì con của nó mới là các bài và bỏ nút là bỏ luôn con. (4) **Một `PdfPageGrid` dùng chung cho cả hai màn hình** (tách bài và sửa PageExtent) — hai màn hình hỏi hai câu khác nhau trên **cùng một lưới trang**, nên tách widget ra là thứ giữ chúng không trôi khỏi nhau. (5) **`Score.extentIn(int)` — và bug thật nằm ở đây.** Bản đầu viết `pageExtent?.clampedTo(count) ?? PageExtent.whole(count)`, mà `clampedTo` **cố ý trả `null`** khi extent không còn mô tả gì (Replace PDF đưa về tệp ngắn hơn), nên toán tử `??` biến đúng ca ấy thành **"bài này là cả cuốn sách"** — tức chính cái "im lặng tự chữa" mà comment của `clampedTo` cấm; test bắt được vì nó hỏi thẳng ca extent bắt đầu sau khi tệp đã hết. Hai ca `null` nay phân biệt rõ: **không có extent** → cả tệp; **có extent mà tệp không còn trang nào của nó** → `null`, và `loadOrIdentity` nhận `0` thì **giữ nguyên** chuỗi đã lưu, không xoá — mất tệp tạm thời không phải lý do xoá việc của nhạc công. Cùng lượt bỏ `resolvedExtent` (không còn ai gọi, hai hàm cùng trả lời một câu là mời chúng trả lời khác nhau). **Bốn quyết định UX nhỏ mà không đảo lại được nếu chọn sai:** trang 1 **không bỏ dấu được** trên màn hình tách (bỏ nó là làm các trang trước dấu đầu tiên rơi khỏi mọi Score); bài không đặt tên thì thừa hưởng tên cuốn sách kèm số thứ tự, và **chèn một dấu sớm hơn thì đánh số lại** — không thì "Book — 2" nằm sau "Book — 3"; thu hẹp PageExtent thì **báo trước số ô PageOrder sẽ bị bỏ** và bấm thoát được, vì được báo sau là được báo quá muộn (G3 số 5); và **một thanh gợi ý sau khi nhập**, không phải một hộp thoại trong luồng nhập. **Ba test viết thêm khi soát tiêu chí chấp nhận, cả ba ở chỗ dễ mất dữ liệu và cả ba trước đó chỉ được **comment** bảo đảm chứ không có gì kiểm:** thu hẹp rồi nới lại PageExtent thì file annotation **không bị ghi lại một byte** (đây là toàn bộ lý do annotation neo theo trang tuyệt đối); Replace PDF bằng tệp ngắn hơn thì `truncated`/`reset` **gọi tên đúng từng bài** thay vì im lặng sửa; và ScoreMenu của một Score trọn tệp **không hiện thêm dòng nào** (người chưa tách gì đi hết slice này mà không biết nó đã xảy ra). **Tiêu chí chấp nhận nay tick được phần lớn; ba ô để lại cho G4 và lý do được ghi ngay tại ô:** mở một bài thấy đúng trang của nó, một bài hoạt động như Score nhập thẳng (Setlist/Label/search/sort), và toàn bộ phép kiểm di trú trên **thư viện thật** — `PdfModeScreen` không pump được vì pdfrx cần native viewer (đúng lý do `brand_reach_test` và `design_token_guard_test` đều là test **nguồn**), nên hai nửa logic của nó được test rời là `Score.extentIn` và `PageOrder.forExtent` + `PageOrderStore`. Ô "không file PDF nào bị di chuyển" tick bằng lý lẽ **mạnh hơn** phép so mtime mà Spec đề ra: di trú là hàm thuần trên nội dung `library.json`, không nhận `Directory` nào, nên không có đường chạm tới một tệp. `CONTEXT.md` **không cần sửa** — Spec khai đúng một term mới (`PageExtent`) và nó đã vào ở nửa trước; không có từ nào mới được phát minh trong lượt này. Không dependency mới, không quyền mới, không byte nào rời máy (`pubspec.yaml` chỉ có build number `+4`→`+6` từ đợt nộp v1). |
| 2026-08-05 | **Manual test G4 đầu tiên của 0052 tìm ra một chỗ thiết kế sai, và nó là loại lỗi mà không test nào bắt được: code làm đúng thứ Spec viết, còn thứ Spec viết thì trình bày sai độ tin của chính nó.** Orchestrator nhập một tuyển tập thật — **148** trang, khoảng **28** bài — và app báo **2 pieces**, bài đầu bắt đầu ở trang bìa. Cả hai đều đúng theo code: PDF ấy mang đúng **2** bookmark, và `looksLikeCollection` vốn cũng đã thoả nhánh `pageCount > 30` nên thanh gợi ý sẽ hiện dù outline rỗng — nhưng nó chọn nhánh chữ *"has a contents list of 2 entries"*, tức **khai một con số như bằng chứng cho một thứ con số ấy không chứng minh được**. Spec 0052 nhận **revision 1** với hai câu G3 mới, cả hai chốt theo khuyến nghị ngay trong lượt. **Câu 11 — đề xuất từ mục lục thành opt-in.** Điều đáng ghi không phải lựa chọn mà là **cách chẩn đoán**: code đã *không* dựa vào Bookmarks theo nghĩa mạnh (lưới trang là cơ chế chính, mọi dấu sửa được, `Clear marks` bỏ cả đề xuất trong một thao tác — đúng như Ghi chú UX của Spec đòi), nên câu hỏi thật không phải "có dùng outline không" mà là **"ai gánh việc đánh giá độ tin của nó"**. Gieo sẵn thì một outline 2 mục hiện lên **y hệt** một outline 24 mục — cùng badge, cùng caption, cùng con số ở đáy màn hình — nên nhạc công phải tự phát hiện đề xuất là rác *trước* rồi mới đi tìm `Clear marks`: app giao cho họ phần khó nhất mà không kèm dữ kiện nào để làm. Chữa bằng cách **đưa con số vào nhãn của nút** — `Use contents list (2 entries)` — thì `2` trên một cuốn 148 trang tự nó là lời cảnh báo *trước khi* có gì bị gieo, và `24` tự nó là lời mời; lưới mở ra trắng, giá phải trả cho cuốn có mục lục tốt là **đúng một tap**. Hai phương án bị bác và lý do đáng giữ: **ngưỡng hợp lý** (chỉ gieo khi trung bình ≤ 20 trang/mục) là một hằng số đoán và **vẫn quyết định hộ**, chỉ là đúng thường xuyên hơn — sai ít hơn không phải cùng loại với không bao giờ sai; **bỏ hẳn outline** thì cắt mất đúng ca `loadOutline()` tồn tại để phục vụ. Thanh gợi ý sau khi nhập cũng thôi khai số mục và **chỉ nói độ dài tệp**, thứ nó biết chắc. **Câu 12 — đoạn đầu sách không thành bài.** Luật cũ ("trang 1 luôn là ranh giới") bảo vệ đúng thứ cần bảo vệ — **không mất trang** — nhưng nó **nhầm *không thuộc Score nào* với *bị mất***: trang sống trong `PdfDocument` chứ không sống trong `Score`, nên một trang không có Score trỏ tới vẫn nằm nguyên trong tệp, vẫn hiện trên lưới, vẫn nhận lại một dấu bất cứ lúc nào. Thứ luật cũ thật sự tạo ra là **một hàng rác trong Library** — bìa cộng trang mục lục thành "Book — 1" — trên đúng loại tệp mà cả slice này tồn tại để xử lý. Màn hình nay **nói ra** bằng một câu (*"Pages 1–2 are not in any piece."*) chứ không để người ta suy ra từ chỗ thiếu một badge. **Tầng dữ liệu không đổi một dòng:** `splitScore` vốn đã tính extent thứ nhất từ `marks.first.startPage` chứ không từ `1` — hằng số ấy chỉ từng sống trong UI, và đó là lý do sửa nó rẻ. Ràng buộc giữ nguyên: **Save vẫn đòi ≥ 2 bài**, nên "bỏ bìa rồi để cả cuốn thành một bài" không đi qua đường tách được; đó là đổi PageExtent, và một Score trọn tệp hôm nay chưa có lối vào màn hình `Pages…` — ghi lại thành **giới hạn đã biết**, không mở phạm vi để chữa. `flutter analyze` sạch, **483** test xanh (477 trước lượt này): thêm test cho mục lục không gieo dấu tới khi được bấm, nhãn nút đếm **thứ tệp chứa được** chứ không thứ nó được đưa, lưới trắng là "No pieces yet" với `Save` tắt, `Clear marks` mang nút mục lục trở lại (bỏ đề xuất là nói nó sai, không phải nói không bao giờ muốn thấy), và Library **không** sinh hàng nào cho phần đầu sách. Không dependency mới, không quyền mới, không byte nào rời máy. |
| 2026-08-05 | **Manual test G4 của 0052 sinh ra một slice thứ hai, và lần này Orchestrator đề nghị một hình dạng bị bác — nhưng lý do bác nó đã có sẵn trong ADR 0019 từ revision 1, viết cho một tính năng khác.** Sau khi tách, Library có 28 hàng và Orchestrator nói đúng một câu: *"nhìn vào đây không biết score này đến từ đâu"*, rồi đề nghị **nhóm chúng vào Score gốc**. Câu quan sát đúng theo nghĩa hẹp nhất có thể: `scoreOriginLine` **đã** tồn tại (`library/score_origin.dart`), **đã** có test riêng, **đã** hiện ở `PdfModeScreen` và `SetlistSession` — và **chưa** được gọi ở đúng màn hình người ta nhìn, nơi `subtitle` chỉ có `_recencyLine` cộng chip Label. Nửa đầu việc phải làm vì thế là **một chỗ gọi hàm**, không phải một tính năng. **Đề nghị nhóm bị bác bằng bốn câu hỏi mà một hàng đại diện cho cuốn sách bắt phải trả lời**, và ghi thẳng vào Spec để không ai đề nghị lại mà không trả lời chúng: bấm vào hàng ấy thì **mở gì** (mở cả tệp 148 trang chính là thứ việc tách ra để chấm dứt; xổ ra danh sách con thì nó là một **thư mục**, đúng mô hình trình duyệt tệp mà ADR 0019 quyết định 11 cấm); **xoá** nó là xoá 28 Score kể cả bài đang nằm trong Setlist, tức nút nguy hiểm nhất của app tự xuất hiện mà không ai xin; nó cần thumbnail, số trang, `lastOpenedAt`, Label — một thứ **hình dạng Score mà không phải Score**; và nó có vào Setlist/search/backup không — có thì **hai vòng đời cho một khái niệm** (đúng lý do ADR 0017 đã bác "kệ riêng"), không thì một công dân hạng hai không nhất quán. Thay vào đó dùng lại **nguyên vẹn** câu chốt mà ADR 0019 revision 1 đã viết cho SongPack: **cuốn sách không phải nơi *chứa* Score, nó là *thuộc tính* của Score** — bày theo thuộc tính thì được, một thực thể để quản lý thì không. Orchestrator chọn **chip lọc theo tệp nguồn**, dùng lại hình dạng filter của 0021. **Ba dữ kiện đọc từ repo đổi thiết kế, và một trong ba là căn cứ bác phương án "cứ sort là xong":** không chế độ sort nào giữ các bài của một cuốn cạnh nhau, và `Created` **tách cuốn sách thành 1 + 27** vì `splitScore` giữ `createdAt` của Score gốc cho bài đầu và cấp `now` cho các bài sau (`score_library.dart:229-246`); `Title` chỉ dính khi tên là mặc định `Book — n`, tên lấy từ mục lục thì tản. Hai dữ kiện còn lại định hình chi phí thật: **cả UI lọc được viết cho một chiều** (`_filterActive`, `_filterDescription()`, hàng chip, câu rỗng *"No scores match …"*) nên phần đắt của slice là **dạy bốn chỗ ấy rằng có hai chiều**, không phải phép lọc; và **chỉ tệp có ≥ 2 Score mới có "nguồn" đáng lọc**, nên danh sách nguồn **tự rỗng** với thư viện chưa tách gì — không cần một nhánh riêng để ẩn nó, và đó là cách slice này giữ lời hứa "người chưa tách gì không thấy một khác biệt nào". **Spec 0053 drafted `proposed`, tier M** (không đổi lược đồ, không di trú, không đường mất dữ liệu, `formatVersion` **không đổi**, không dependency, không quyền — G3 và G4, không cần ADR mới, không cần Security Review), 8 câu G3 chờ trả lời. Ba khuyến nghị đáng nhớ: dòng nguồn gốc là **dòng riêng đứng trên chip Label** chứ không nối vào `_recencyLine` (nối vào thì trộn thời gian với nguồn gốc trong một câu bị cắt đúng chỗ quan trọng nhất, và hàng có cả hai phải nhận **bốn dòng** trong một `ListTile` tính cho ba); dòng ấy gọi tệp bằng **tên tệp** chứ không bằng tên cuốn sách, vì 0052 G3 số 7 cho Score gốc **thành bài thứ nhất** nên "tên cuốn sách" đã bị đổi ngay lúc tách và không còn là dữ kiện đáng tin; và **lối vào chính là `⋯` của một bài** (*"Show all pieces of `Chopin Etudes.pdf`"*) chứ không phải danh sách trong hộp lọc, vì câu hỏi phát sinh lúc đang nhìn một bài và lối này không đòi ai nhớ tên tệp. Hai cắt có ý thức: **sort mode "Source" và dòng tiêu đề nhóm ở ngoài phạm vi** (lọc giải bài toán bằng **thu hẹp** thay vì **gom**; nếu G4 cho thấy vẫn cần thấy các bài cạnh nhau *mà không* thu hẹp thì đó là Spec sau), và **không gán Label tên cuốn sách lúc tách** vì đó là giả một Label ra từ dữ kiện đã có sẵn, làm bảng Label đầy tên tệp, mà Label thì persist còn nguồn gốc thì suy ra được. **ADR 0019 nhận revision 4 — chỉ đổi thứ tự, không đổi một quyết định nào:** slice mới chen vào vị trí 2 nên **mọi số Spec từ MeasureMap trở đi dịch một bậc** (MeasureMap `0053`→`0054`, SongPack `0059`→`0060`, thu âm `0062`→`0063`), Security Review đổi từ "slice 8 và 11, không cần 1–4" sang **"slice 9 và 12, không cần 1–5"**, và "bốn slice đầu không có byte nào rời máy" thành **năm**. Nó đứng trước MeasureMap vì ba lý do: rẻ, sửa thứ nhạc công đang chịu **ngay hôm nay**, và **cơ chế lọc của nó là thứ màn hình SongPack sẽ dùng lại** — dựng sau là dựng hai lần. Các dòng log cũ nói "Spec 0053" theo nghĩa MeasureMap **giữ nguyên**: log là bản ghi lịch sử, đọc chúng theo bảng Trình tự mới. Không đụng code. |
| 2026-08-05 | **Orchestrator uỷ quyền agent trả lời G3 của Spec 0053. Tám câu chốt theo khuyến nghị — và đợt đọc code để *kiểm* các khuyến nghị ấy sinh ra ba câu mới, cả ba ở chỗ mà cách làm hiển nhiên nhất sẽ hỏng im lặng.** Tám câu cũ không đổi nội dung: dòng nguồn gốc là **dòng riêng trên chip Label** (`isThreeLine` thành `labelNames.isNotEmpty \|\| originLine != null`, và dòng ấy dùng đúng style `subtitle` — một hàng bốn dòng với hai cỡ chữ là nhiễu chứ không phải thông tin); gọi tệp bằng **tên tệp**; chip **cùng hàng khác icon**; kết hợp bằng **giao**; **`⋯` là lối chính**; danh sách nguồn **≥ 2 Score, A–Z**; **không persist**; **search không tìm tên tệp**. **Câu 9 — hộp lọc phải rời khỏi `label_sheets.dart`.** `showLabelFilterSheet` sống trong tệp Label và chỉ import Label; nhét `PdfDocument` vào là để một tên hàm nói sai về việc nó làm, rẻ hôm nay và đắt ở slice SongPack khi có chiều thứ ba. Tách sang `lib/ui/library_filter_sheet.dart` thành `showLibraryFilterSheet`, tiêu đề *"Filter by Label"* → *"Filter"*, hai sheet còn lại không đụng. Hai ràng buộc đi kèm, cái thứ hai là thứ suýt lọt: `SegmentedButton` `Any`/`All`/`Untagged` vẫn chỉ nói về Label, và mục nguồn phải hiện **cả khi** mode là `Untagged` — nhánh ấy hôm nay **thay chỗ toàn bộ danh sách bằng một câu**, nên làm theo hình dạng có sẵn là làm *"những bài chưa gán nhãn của cuốn này"* thành câu hỏi không hỏi được. **Câu 10 — chip lọc mã hoá loại bằng `null`, và chip nguồn không được dùng lại chỗ ấy.** Hàng chip là `({String? id, String name})` với `id: null` nghĩa là `Untagged`, và `_removeFilterChip(null)` **xoá cả bộ lọc**; thêm chip nguồn theo hình dạng đó thì bấm `×` trên tên tệp xoá luôn chiều Label — đúng loại lỗi không lộ ra ở review vì code đọc vẫn hợp lý. Chip mang một loại tường minh (`label`/`untagged`/`source`). **Câu 11 — một dữ kiện của bản nháp nói sai theo hướng có lợi cho chính nó.** Bản nháp trích `scoresSharingDocument` là phép đếm "đã có sẵn" cho ngưỡng ≥ 2; đọc code thì nó là **`Future<int>` và đọc lại manifest mỗi lần gọi**, tức không dùng được ở `itemBuilder` của `⋯` mà không phá đúng Ràng buộc kỹ thuật của chính Spec ("không async trong đường vẽ một hàng"). Đếm từ `_scores` đang nằm trong State, **một** `Map<String, int>` dựng lại trong `_reload()` phục vụ cả hai lối vào — và điều đó cũng làm hai lối vào **không thể** bất đồng về việc cái gì là một "nguồn". Cùng loại lỗi ở dữ kiện 5: bản nháp trích `library_screen.dart:460-463` làm bằng chứng "bộ lọc không persist", nhưng đó là đường **restore backup** chứ không phải đường khởi động; kết luận vẫn đúng vì bộ lọc là State thuần không store nào ghi, nhưng chỗ trích thì sai — và nó nhắc ra một việc nhỏ dễ quên: khối reset ấy phải học thêm chiều nguồn, không thì restore xong bộ lọc còn nửa bên. **Một chỗ câu trả lời đi xa hơn khuyến nghị, và là chỗ duy nhất trong lượt này lệch khỏi bản nháp:** khuyến nghị cho câu 5 nói mục `⋯` hiện *"trên Score có `pageExtent` không trọn tệp — cùng điều kiện mà `scoreOriginLine` dùng"*. Hai điều kiện ấy gần trùng nhưng lệch đúng một ca, và ca ấy có thật sau khi xoá bớt bài: một bài không còn anh em nào vẫn **có** dòng nguồn gốc — đó là một dữ kiện về chính nó — mà **không** được có mục menu, vì một lối vào hứa "all pieces" rồi trả về đúng một hàng là một lời hứa sai. Vị từ của **dòng** là `scoreOriginLine != null` (mà `_isPiece` hôm nay đúng bằng nó); vị từ của **mục menu** và của **danh sách nguồn** là "tệp này có ≥ 2 Score". Câu rỗng cũng được chốt câu chữ vì nó là chỗ duy nhất nói ra rằng có hai chiều: *"No scores match Gig in `Chopin Etudes.pdf`"* — chữ **`in`** làm cả việc ấy. Spec 0053 nay **11/11 câu G3 đã trả lời** nhưng **giữ `proposed`**: chữ ký `accepted` là việc của Orchestrator và trả lời hết câu hỏi không thay thế được gate (tiền lệ Spec 0051). Đó là hạng mục duy nhất chặn build. Baseline giữ nguyên **483** test. Không đụng code. |
| 2026-08-05 | **Spec 0053 accepted (G3) nguyên bản và build xong trong cùng lượt. `flutter analyze` sạch, **502** test xanh (483 trước slice).** Nửa đầu đúng như Spec hứa là **một chỗ gọi hàm**: `scoreOriginLine` nay có chỗ gọi thứ ba trong `itemBuilder` của Library, `score_origin_test.dart` không phải sửa một dòng — nếu nó phải sửa thì tức là câu chữ đã bị nhân bản. Nửa sau là `source_filter.dart` (hàm thuần, không `Directory`, không `ScoreLibrary`) cộng việc dạy bốn chỗ trong `library_screen.dart` rằng có hai chiều lọc. **Bốn chỗ code buộc phải nói ra một phân biệt mà Spec chỉ ngụ ý, và ba trong bốn là chỗ dễ hỏng im lặng.** (1) **Hai vị từ, không một** — `isFilterableSource` (tệp có ≥ 2 Score) canh mục `⋯` và danh sách nguồn, còn `scoreOriginLine != null` canh dòng trên hàng; test khoá đúng ca làm chúng lệch: xoá hết anh em thì hàng **vẫn** nói *"Pages 1–8 of Chopin Etudes.pdf"* mà mục menu biến mất. (2) **Bộ lọc không được sống lâu hơn tệp nó gọi tên** — chỗ này không có trong Spec và chỉ lộ ra khi viết `_reload`: xoá bài cuối của cuốn đang lọc sẽ để lại một danh sách rỗng sau một chip không còn gì để tự gọi tên, nên `_reload` bỏ `_filterSourceId` khi không Score nào còn trỏ tới nó. (3) **Tệp không có `originalFileName` thì không được chào làm nguồn** — nó không có gì để viết lên chip; dòng nguồn gốc của nó vẫn hiện phần trang, đúng như `scoreOriginLine` đã làm từ 0052. (4) **Tooltip của nút lọc** cũng nói *"Filter by Label"*, cùng lỗi với tiêu đề sheet mà G3 số 9 đã bắt — đổi thành *"Filter"*, một dòng test sửa theo. Ba câu G3 mới đều đứng vững khi cầm vào code: hộp lọc tách ra `lib/ui/library_filter_sheet.dart` nhận **view model phẳng** `({id, name, pieces})` thay vì `PdfDocument`, nên tệp ấy không import một model thư viện nào — mục nguồn nằm **ngoài** nhánh `Untagged` như đã chốt; chip mang `_FilterChipKind` tường minh; và phép đếm dựng **một lần mỗi build** trong `_buildScoresBody`, không phải state thứ tư phải đồng bộ ở ba chỗ `_scores` được gán. `_filterDescription()` vẫn là nơi duy nhất trả lời "đang lọc gì" và nay ghép hai chiều bằng chữ `in`. **Còn lại cho G4:** thư viện thật 148 trang, đọc dòng nguồn gốc ở ba chỗ trong cuốn, đếm hàng sau khi lọc, và hai thứ test không kiểm được — máy hẹp nhất với một Label dài, và bộ lọc trắng sau khi **tắt hẳn** app. Không dependency mới, không quyền mới, `formatVersion` không đổi, không khoá prefs mới, không byte nào rời máy. |
| 2026-08-06 | **Orchestrator dùng Half Page thật và báo đúng thứ 0013/0041 không lường: overlay có separator kéo tay gây giật vị trí nốt nhạc khi PageTurn, vì cùng một trang đổi vai trò từ "peek nhỏ ở rìa" sang "current full-size ở giữa" trong một khung hình.** Spec **0056** drafted và **accepted** ngay trong chat (câu hỏi/chốt qua `AskQuestion`, không qua form G3 dài, vì Orchestrator là người trả lời trực tiếp): (1) hai mode Half Page (top/bottom, left/right) thôi là overlay tĩnh, chuyển thành **scroll liên tục theo đúng trục**, PageTurn ép cứng lướt **nửa viewport** bất kể Turn amount — xoá separator, slider peek-ratio, và khái niệm `freePeek` (peek không còn "tốn diện tích" vì không còn là overlay); (2) **default Layout cho install mới đổi từ Auto về One page** — install đã có `layout_prefs.json` không bị đụng. Cả hai đảo ngược quyết định đã khoá ở G3: 0041 decision 3 (Auto default cho new install) và 0013 + 0041 decision 2/10 (Half Page là overlay tách biệt khỏi Turn Amount) — 0056 supersede đúng hai phần đó, phần còn lại của 0013/0041 (Auto vẫn chọn được, Two pages fallback, fit-height `wont`, Match layout…) không đổi. Tier **M**: không SDK, không quyền, không byte rời máy — G3 (xong) + G4 (build + test xanh). Không đụng code trong lượt log này. |
| 2026-08-06 | **Specs 0053–0055 build xong và vào repo trong *một* commit, và lý do gộp là thứ đáng ghi hơn bản thân ba slice: hai slice trước bị slice thứ ba thay trước khi kịp có commit riêng.** 0053 đã có dòng log ngày 2026-08-05 với **502** test — nhưng nó chưa từng được commit, và đến lượt này thì UI của nó (chip lọc theo tệp nguồn, mục *Show all pieces of…*) cùng UI nhóm phẳng của 0054 (`BookHeaderRow`) **không còn tồn tại để commit riêng**: 0055 thay cả hai bằng một mô hình duy nhất — Score sở hữu Score con, một cuốn là **một hàng** dẫn vào màn hình Pieces, thay vì một **chiều lọc**. Ghi lại vì đây là ca đầu tiên trong repo mà lịch sử git **không** kể lại được thứ tự các quyết định, còn log thì có; ai đọc `git log` sẽ thấy ba Spec trong một commit và tưởng chúng được nghĩ ra cùng lúc. Nhìn lại thì đường đi 0053 → 0054 → 0055 đúng bằng ba lần trả lời cùng một câu hỏi của Orchestrator (*"nhìn vào Library không biết score này đến từ đâu"*) với ba mức cam kết tăng dần — dòng nguồn gốc, rồi nhóm phẳng, rồi phân cấp thật — và chỉ mức thứ ba sống sót. Cái giá thì đã trả bằng token chứ không bằng code chết (ADR 0016 đã ghi riêng: build 0054 tự sinh 10,8 triệu token), và bài học ở đó không phải "đừng thử" mà là **cắt Spec nhỏ hơn ở G3**. `flutter analyze` sạch, **554** test xanh (502 trước đó). `CONTEXT.md` nhận mở rộng định nghĩa `Score` (chứa được Score con) — không term mới, đúng như 0055 chốt là **không đẻ term `Piece`**. |
| 2026-08-06 | **Màn hình Pieces được thiết kế lại và toggle *piece notes* được đặt tên — commit thuần thị giác, nhưng chỗ đáng ghi là một luật UI đã có sẵn trong repo mà 0055 xây xong mới lộ ra là đã vi phạm.** Nút *Show piece notes* trên AppBar của `PdfModeScreen` là một icon layers trần, tooltip chỉ hiện khi **nhấn giữ** — tức mục đích của nó không đọc được bằng mắt, trong khi `ScoreMenuQuickBar` ngay trong cùng app đã dùng văn phạm **icon-trên-nhãn** cho Draw / Metronome / Bookmarks từ lâu. Sửa là dán nhãn *Piece notes* dưới icon, không phát minh gì. Màn hình Pieces đổi bốn chỗ, tất cả đều là chuyện *hàng này đang nói gì*: AppBar mang **tên cuốn sách** thay cho chữ *Pieces* chung chung; một hàng tóm tắt (bìa, số bài, số trang, liên kết *Open full score*) thay dòng tiêu đề trơ và kéo liên kết mở-cả-cuốn ra khỏi AppBar; mỗi hàng có huy hiệu **số thứ tự** trên thumbnail; và subtitle **dẫn bằng phạm vi trang** thay vì lặp lại nó sau dòng thời gian. `flutter analyze` sạch, **554** test xanh — không đổi, vì không hành vi nào đổi. |
| 2026-08-06 | **Spec 0056 build xong, và nó xoá nhiều hơn viết: `half_page_view.dart` (**561** dòng) biến mất, tổng cộng −1.149 dòng đổi lấy +249.** Half Page nay đi qua **chính engine liên tục của Scroll** thay vì có một view riêng, nên PageTurn lướt đúng nửa viewport theo trục của mode thay vì cắt cảnh một trang từ "peek nhỏ bị clip" sang "current full-size". Đây là loại kết quả đáng ghi vì nó ngược trực giác thường gặp: thứ Orchestrator báo là **giật khi lật** hoá ra không chữa bằng cách làm animation mượt hơn mà bằng cách **bỏ một view**. Suite **giảm** từ 554 xuống **545** — `half_page_peek_test.dart` (116 dòng) bị xoá cùng khái niệm nó kiểm (`freePeek`, slider peek-ratio, separator), và đó là dấu hiệu lành: test chết cùng tính năng chết thì không ai phải đoán chúng còn bảo vệ gì. **Một bug lộ ra trong lúc làm và nó là loại lỗi mà cả 0013 lẫn 0041 đều không thể bắt bằng test:** phép tính fit-zoom của side peek **thiếu hẳn** trong nhánh height-binds mà `pdfFitZoom` vốn đã có cho `fitHeight`, nên nó chia cho chiều rộng của **cả dải ngang** và render mọi trang thành thumbnail tí hon thay vì một trang một lúc — triệu chứng chỉ hiện trên máy thật với một tỉ lệ màn hình nhất định. Build lên **1.0.0+7** để **thay bản đang nằm trên store**: v1 đã nộp ngày 2026-08-05, nên đây là lần đầu repo sửa một thứ đã ở trong tay reviewer, và luật của `AGENTS.md` (từ slice 4 trở đi không chạy song song với chờ duyệt) vừa được áp dụng đúng theo chiều ngược — sửa bug layout thì được, mở tính năng H5 thì không. |
| 2026-08-06 | **Spec 0055 nhận revision 1 — *Edit pieces* — và nó lấp một ngõ cụt mà bản gốc tạo ra bằng chính câu trả lời G3 của nó.** Câu 10 chốt resplit chạy trên **con**, và `_canSplit` giấu *Split into pieces…* ngay khi gốc có con; cộng lại thành: **tách sai một cuốn 148 trang một lần là không có đường quay lại**, chỉ còn sửa từng bài một. Revision 1 mở đường ấy mà **không** đảo câu 10 — kết quả vẫn là anh em dưới cùng một gốc, cây vẫn một tầng. **Ba câu chốt trong lúc build, và cả ba đều là câu hỏi về mất dữ liệu chứ không về UI.** (13) *Edit pieces là lối riêng, không phải nới `_canSplit`* — một nút làm hai việc thì **không viết được lời xác nhận cho nó**, vì tách lần đầu không xoá gì còn vẽ lại thì xoá được cả một bài đầy mực; đó là lý do đủ, không cần tới lý do nào về menu. (14) *Khớp bài cũ với mark mới theo đúng cặp `(firstPage, lastPage)`* — khớp theo **tên** thì đổi tên là mất dữ liệu, khớp theo **vị trí** thì chèn một dấu ở đầu sách làm mọi bài trượt một bậc và **mọi `id` đổi chủ**, tức mực của bài 3 hiện lên trong bài 4; khớp theo phạm vi trang thì đổi tên và đảo thứ tự đều vô hại. (15) *Bài không còn phạm vi nào khớp thì bị xoá, có hỏi trước nếu nó không rỗng* — "không rỗng" đếm cả năm thứ nhạc công sẽ tiếc (mực, bookmark, jump link, Label, chỗ trong Setlist), và **file annotation đọc không được tính là có dữ liệu**: cảnh báo thừa rẻ hơn xoá nhầm. **Hai chỗ code buộc phải nói ra một phân biệt.** `ScoreLibrary.editPieces` **thay** cả bộ con trong khi `splitScore` chỉ biết **thêm** — hai hàm, không phải một hàm có cờ, vì bất biến của chúng khác nhau; Score gốc không bị đụng ở cả hai. Và UI chạy `planPieceResplit` **một lần khô** (với `nextId` trả chuỗi rỗng) chỉ để biết `removedIds` **trước khi** hỏi, rồi mới gọi `editPieces` thật — nếu không thì lời xác nhận phải đoán xem cái gì sắp mất, mà đoán sai ở đúng chỗ này là mất mực người ta ngồi vẽ. Dọn dẹp chia đúng như `deleteScore` đang chia: `editPieces` lo overlay theo `scoreId`, còn page scale handover / Label / Setlist / thumbnail cache là việc của caller — giữ nguyên ranh giới ấy thay vì kéo mọi store vào một hàm manifest. Cùng lượt `⋯` của hàng gốc nhận thêm mục *Pieces…*, vì tap vào hàng đã dẫn vào màn hình Pieces từ 0055 mà menu thì chưa có lối nào ngoài *Open full score*. `SplitScoreScreen` được **dùng lại** (thêm `initialMarks` và `appBarTitle`) chứ không nhân bản thành màn hình thứ hai. `flutter analyze` sạch, **559** test xanh (545 trước lượt này). Còn lại cho G4: vẽ một bài, vẽ lại ranh giới, kiểm mực của bài giữ nguyên phạm vi vẫn còn. Không dependency mới, không quyền mới, `formatVersion` không đổi. |
| 2026-08-06 | **Spec 0058 (MeasureMap) accepted ở G3 — 15/15 câu.** Câu 1–10 theo khuyến nghị (`beatSplits` như web; `measureNumber` liên tục; mặc định `4/4`+`120`; chép trang trước + lối phụ chọn trang; ScoreMenu *Measure map…*; dialog nhảy tới ô; loại trừ Draw; soạn gốc được nhưng nghiêng piece; xoá cả map có confirm; baseline ~570). Câu 11–15 chốt trong chat: dialog số ô sticky phiên; xoá/đổi N **không chia đều cả system** — giữ hình học ô không đụng (giảm N / xoá → ô liền trước mở rộng; tăng N → cắt ô phải nhất); tempo/time sig + dialog phạm vi (*This measure only* / system / page / rest / Next N); BeatBox mặc định ẩn, *Edit beats* chỉ ô đang chọn. Tier L, Security Review không cần. **Chưa build** — chat build riêng (ADR 0016). |
| 2026-08-06 | **ADR 0019 revision 7 — chỉ đổi số: MeasureMap thành Spec `0058`.** Hai Spec ngoài ADR (0056 Half Page, 0057 localization) chen trước và tiêu thụ đúng hai số bảng Trình tự đã dành cho MeasureMap và SyncMap-tính-ra. Đây là lần thứ ba số ngoài chen vào dải đã dành (sau 0053, 0055). **Spec 0058 drafted `proposed` (tier L)** cùng lượt: MeasureMap = SystemBox → MeasureBox → BeatBox; nhảy tới ô nhịp để slice tự đứng; chép layout trang trước; cấm OMR; round-trip với web. Dữ kiện đọc từ repo web đổi một khuyến nghị G3: web **không** có type `BeatBox` — phách lưu bằng `beatSplits: number[]` (N−1 tỉ lệ trong hộp). Khuyến nghị StageScore lưu giống vậy, UI vẫn nói BeatBox. Mười câu G3 chờ Orchestrator. Không đụng code. |
| 2026-08-06 | **Spec 0057 (localization, 9 ngôn ngữ) build xong — chuẩn bị thay bản đang review trên hai store bằng một bản có tiếng Việt.** Hạ tầng chuẩn của Flutter: `flutter_localizations` + `intl` + `l10n.yaml` + 9 file ARB (`en` là chân lý, tám bản còn lại dịch bằng AI), `MaterialApp` đọc `localizationsDelegates`/`supportedLocales`/`locale:` từ một setting **Language** mới trong menu `⋯` (`AppLocalePref`/`AppLocalePrefsStore`, cùng hình dạng và cùng thư mục gốc với `AppAppearance`). Domain term (Score, Setlist, Bookmark, JumpLink, PageTurn…) giữ nguyên tiếng Anh trong mọi bản dịch — quy tắc ghi thẳng trong Spec vì nó áp cho cả tám file ARB không phải `en`. **Đợt soát trước khi coi slice là xong tìm ra bản build đầu chỉ làm nửa việc của Test plan:** mọi hàm/extension không có `BuildContext` (label getters, `relative_day.dart`, `score_origin.dart`, …) đã nhận tham số `AppLocalizations` đúng như Spec đòi, và test cũ được sửa qua một helper `testL10n()` — nhưng **không có test riêng nào cho `AppLocalePrefsStore`** (round-trip, fallback khi file hỏng) và **không có widget test nào ép `locale:`** để xác nhận cơ chế mà `main.dart` thật sự dùng để áp dụng setting Language. Thêm `app_locale_prefs_test.dart` (round-trip, `System` mặc định, file hỏng/rỗng fallback) và `locale_override_test.dart` (ép `MaterialApp.locale` qua bốn locale, xác nhận nút Cancel/Save dịch đúng; locale không được hỗ trợ như `th` vẫn resolve về một locale trong danh sách 9 thay vì crash; cả 9 locale tải được và `piecesScreenPieceCount(1)`/`(2)` không rỗng — bắt đúng loại lỗi câu ngô nghê "1 pieces" mà ICU đáng ra phải chặn). `flutter analyze` sạch, **570** test xanh (559 trước hai file mới). Còn lại cho G4: đổi ngôn ngữ hệ thống trên máy thật qua vài locale, xác nhận không sót chuỗi tiếng Anh nào ngoài domain term, và xác nhận lựa chọn Language sống sót qua khởi động lại app. Không dependency mới ngoài `intl`/`flutter_localizations` (cả hai từ chính Flutter SDK), không quyền mới, không byte nào rời máy, `formatVersion` không đổi. |
| 2026-08-06 | **Spec 0058 (MeasureMap) build xong. Baseline **570** → **589** test; `flutter analyze` sạch.** Model `MeasureBox` + `beatSplits` (web-compatible), `SystemBox` gom theo `systemIndex`, persistence `measure_maps/<scoreId>.json` cùng idiom annotation, hook `clearScoreOverlays` / backup label, ScoreMenu *Go to measure…* + *Measure map…*, overlay soạn loại trừ Draw, sticky số ô phiên (không prefs), jump + highlight ~1s. `CONTEXT.md` nhận MeasureMap/SystemBox/MeasureBox/BeatBox; SyncMap siết lại (không bắt buộc BackingTrack). Không dependency mới, không quyền mới, không byte rời máy, `formatVersion` không đổi. Còn lại cho G4: map etude thật, piece vs full score, backup cảm nhận, trần chất lượng soạn trước SongPack. |
| 2026-08-07 | **Spec 0058 revision 1 (G4) — select + resize/move SystemBox.** G4 lộ hai chỗ thô: không resize được dòng sau khi vẽ, và *Set measure count* / *Delete system* ký sinh vào selection MeasureBox vì không có chỗ chọn SystemBox. Revision thêm câu G3 **16–18**: selection `none` \| `measure` \| `system` loại trừ lẫn nhau; tap **khung** chọn system (long-press ô = select parent system); menu tách (system: count/delete system; measure: xoá ô / tempo·time sig / Edit beats); resize 4 mép + move thân chỉ khi system đang chọn — ngang scale tỉ lệ ô (giữ chỗ in không đều), dọc cùng y/height, clamp trang, min size. `flutter analyze` sạch, **595** test xanh (589 trước rev.). Không dependency / quyền mới. Còn lại cho G4: cảm nhận resize trên etude thật. |
| 2026-08-07 | **Spec 0058 G4 pass — slice MeasureMap đóng (`status: done`).** Orchestrator chấp nhận sau build + rev. 1 (select/resize/move SystemBox). Suite **595**. Slice H5 kế tiếp theo ADR 0019: **0059** SyncMap tính từ MeasureMap / metronome. |
| 2026-08-07 | **Spec 0058 revision 2 — `beatSplits` = N mốc nội tại (không tính hai vạch nhịp mép ô).** Orchestrator: mục đích Edit beats là kéo từng vạch **vào đúng nốt của phách** — 4/4 cần **4** vạch trong ô. Không phải N−1 biên vùng (web) và không phải vạch dán mép phải `1.0`. StageScore lưu N tỉ lệ tâm (mặc định `(i+0.5)/N`), cả N kéo được; wire web vẫn N−1 biên (đổi centres ↔ midpoints). Map cũ N−1 / bản nháp `…1.0` migrate lúc load và lúc bật Edit beats. **Tài liệu đồng bộ cùng lượt:** `CONTEXT.md` (MeasureBox/BeatBox), Spec **0058**/**0059** (SyncMap không đọc `beatSplits` cho thời lượng), ADR **0014** bảng field, ADR **0019** revision 8 (đính chính 3a/3e/6 — không đổi tách hình học↔thời gian). |
| 2026-08-07 | **Spec 0059 — Orchestrator bác G3 câu 2 cũ (chỉ sửa metronome sheet); kéo Playback controls + playhead vào slice.** Câu 2 chốt: ScoreMenu **show/hide Playback controls**; controls có **Play / Pause / Stop**; **vẽ playhead** chạy theo SyncMap × `beatSplits`. Chỉ báo vị trí thôi thuộc 0060 trong bảng Trình tự ADR 0019 — nay vào 0059; **0060** còn lật trang rảnh tay tinh. Khuyến nghị kèm (2b–2f, chưa chốt hết): mặc định ẩn controls; không QuickBar; map rỗng → disabled; Stop → về đầu rồi ẩn playhead; vào Measure map → Pause. Spec 0059 rewritten; hàng slice 6–7 ADR 0019 cập nhật. Không đụng code. |
| 2026-08-07 | **Spec 0059 — count-in + câu hỏi nhịp lấy đà.** Orchestrator: có SyncMap + Playback thì metronome cần **đếm trước 1–2 ô**. Spec nhận **count-in 0/1/2 ô** (mặc định 1; chỉnh ở metronome sheet; sau Pause không đếm lại — khuyến nghị). **Nhịp lấy đà:** hai ca — (A) ô pickup **ngắn** + time signature đúng độ dài → đã chạy với công thức 0059, không cần field mới; (B) ô **đủ rộng** nhưng nhạc bắt đầu giữa ô → cần `startsAtBeat` (khớp web), khuyến nghị làm trong 0059 kèm UI tối thiểu. Không đụng code. |
| 2026-08-07 | **Spec 0059 accepted (G3) — mọi khuyến nghị.** Orchestrator chấp nhận toàn bộ câu 1–11b + 2b–2g (kèm 2g: hide controls lúc Play → Pause). Status `accepted`; **được build**. Tóm tắt khóa: SyncMap hàm thuần không ghi đĩa; Playback controls mặc định ẩn, không QuickBar, map rỗng disabled; playhead về đầu rồi ẩn khi Stop; count-in mặc định 1 ô ở metronome sheet, không đếm lại sau Pause; pickup A + B (`startsAtBeat`) trong slice; tempo = BPM nốt đen. Chat build riêng (ADR 0016). Không đụng code trong lượt accept. |
| 2026-08-07 | **Spec 0059 build xong. Baseline **596** → **621** test; `flutter analyze` sạch.** `lib/sync_map/`: hàm thuần MeasureMap→SyncMap (tempo BPM nốt đen, `beatTimestamps` chia đều — không đọc `beatSplits` cho thời lượng), `startsAtBeat` trên MeasureBox + UI *Starts at beat* (1-based trên UI / 0-based trên đĩa·web), round-trip subset `TimemapEntry`, playhead mapper `timeMs`×`beatSplits`, `SyncMapPlayback` (Play/Pause/Stop + count-in 0/1/2; click qua `MetronomeEngine.playClick`), prefs `playback_prefs.json`. PdfMode: ScoreMenu show/hide Playback controls (map rỗng disabled), thanh controls, playhead overlay (Listenable hẹp), đưa trang vào tầm nhìn; vào Measure map / hide controls lúc Play → Pause. `CONTEXT.md` nhận PlaybackControls và siết Playhead/SyncMap. Không dependency / quyền mới, không byte rời máy, `formatVersion` không đổi. Còn lại cho G4: Play trên etude đã map, pickup A+B, count-in/Pause, sang trang, soạn MeasureMap giữa Play. |
| 2026-08-07 | **Spec 0059 G4 UI — Playback controls hai kiểu + hết bị chrome đè.** Orchestrator: chrome hiện che controls. Chốt **docked** (seek một hàng, luôn trên PageNav) và **floating** (pill Play/Pause+Stop kéo được, persist vị trí; không seek trên pill). Pref `style` + `floatNorm*` trong `playback_prefs.json`. |
| 2026-08-07 | **Playback settings rời Metronome sheet → ScoreMenu.** Orchestrator: Bar/Float + count-in không thuộc Metronome. Mục *Playback settings…* cạnh Show/Hide; Metronome chỉ tempo/mute/volume/beats. |
| 2026-08-07 | **Hygiene docs 0058/0059 khớp G4 đang chạy (chưa đóng G4).** Spec 0059 § D + checklist/test plan: badge count-in vàng cam rồi `measure.beat`; Stop→Play nhảy trang đầu ngay; Playback settings / docked·floating. Spec 0058 ghi follow-up khóa PageTurn khi soạn MeasureMap. `CONTEXT.md` PlaybackControls. Không đổi `status` 0059 — chờ Orchestrator G4 pass. |
| 2026-08-07 | **Playhead appearance trong Playback settings.** Orchestrator: playhead cũng cần chỉnh màu / size / opacity. Prefs `playheadColorValue` / `playheadWidth` / `playheadOpacity` trong `playback_prefs.json`; UI + preview trong *Playback settings…*; overlay đọc prefs. Không đổi `status` 0059. |
| 2026-08-07 | **Spec 0059 G4 pass — slice SyncMap+Playback đóng (`status: done`); Spec 0058 xác nhận lại (rev. 2 + follow-up PageTurn).** Orchestrator chấp nhận sau build + chỉnh UI: Playback settings (Bar/Float, count-in mặc định 0, playhead màu/độ dày/chiều cao/độ đục), docked không bị chrome đè, floating pill, badge `measure.beat` slot cố định, Stop→Play nhảy trang đầu. Suite **~635** (+ G4 UI sau baseline build 621). Slice H5 kế: **0060** lật trang rảnh tay tinh. |
| 2026-08-07 | **Spec 0060 drafted `proposed` (tier M) — lật trang rảnh tay tinh.** 0059 chỉ nhảy khi `pageNumber` playhead đổi (muộn, bỏ qua TurnAmount, không theo hệ trong cùng trang). ADR 0019 nợ phần thưởng hình học tới phách: lật trên cửa sổ nghỉ cuối hệ. Proxy không OMR = phách cuối của ô cuối trên hệ cuối của bước sắp rời; look-ahead đầu phách đó; tái dùng TurnAmount + animation; delay/reverse không; theo hệ trên scroll; pref trong Playback settings (mặc định bật lúc Play); không auto sang Score Setlist. Mười hai câu G3 chờ Orchestrator. Không đụng code. |
| 2026-08-07 | **0060 → `hold`; Spec 0061 drafted `proposed` (tier L) — FormMap (lặp / volta / D.C. / coda).** Orchestrator: bài có quay lại và nhảy cóc (CODA) quan trọng hơn lật trang tinh; SyncMap 0059 nối MeasureBox tuyến tính nên không biết chỗ lặp / bỏ qua. Tách **FormMap** khỏi MeasureMap (hình học) và PageOrder (trang); bung → SyncMap **latent** + neo physical (họ web `unrollMeasures` / `measureMap`, không MusicXML). ADR **0019 revision 9**: siết 3c (*tính ra* = MeasureMap + FormMap); Trình tự chèn 0061 trước 0060 hold; SoLoud+ dịch số (`0062`…`0068`). Mười hai câu G3 chờ. Không đụng code. |
| 2026-08-07 | **Spec 0061 accepted (G3) — mọi khuyến nghị.** FormMap lớp riêng; từ vựng đủ (repeat/times, volta, Segno/Coda/To Coda, D.C./D.S., Fine); SyncMap `measure` latent + physical derive; rỗng = 0059; UI *Form map…*; marker+jump; badge physical; Play không theo PageOrder; `formatVersion` không tăng; chỉ ADR 0019 rev. 9. Status `accepted`; **được build**. Chat build riêng (ADR 0016). Không đụng code trong lượt accept. |
| 2026-08-07 | **Spec 0061 build — FormMap.** `lib/form_map/` (model + persist `form_maps/<scoreId>.json` + unroll MeasureMap×FormMap→SyncMap); `SyncMapEntry.physicalMeasure`; playhead/badge physical; ScoreMenu *Form map…* (disabled khi MeasureMap rỗng); soạn select-only trên ô đã map; form hỏng → từ chối Play. Overlay/backup/`clearScoreOverlays` nhận `form_maps`. `CONTEXT.md` + ADR 0014 bảng latent/physical. `formatVersion` không đổi. Baseline ~636 → suite tăng unit unroll. Còn G4: bài thật có lặp+coda; soạn sai form; backup/restore. |
| 2026-08-07 | **Spec 0061 G4 pass — slice FormMap đóng (`status: done`).** Orchestrator chấp nhận sau build + chỉnh UX: volta gom vào *Add repeat…*; hỏi thay repeat chồng; mô tả Marker/Jump (không glyph nhạc); palette FormMap indigo + chip màu theo loại; chip gọn. Suite xanh; `formatVersion` không đổi. 0060 vẫn `hold`. |
